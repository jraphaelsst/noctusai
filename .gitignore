# Standalone apps (not yet migrated to products/)
# Root-anchored — `products/adconnect/` IS tracked (post-migration scaffold).
/adconnect/

# Dependencies
node_modules/
__pycache__/
*.pyc
venv/
.venv/

# Environment
.env
.env.local
.env.*.local
# NOTE: .env.example is NOT ignored — it's a template with placeholder values, not secrets

# Build
dist/
build/
*.egg-info/

# IDE
.vscode/
.idea/``
*.swp
*.swo
.DS_Store

# Claude / MCP local config (hardcoded user-specific paths)
.mcp.json
.claude/settings.local.json
.claude/snapshots/

# Artifacts (Gemini)
.gemini/

# Playwright
test-results/
playwright-report/
blob-report/
playwright/.cache/

# Seed backups (local safety net — git handles real versioning)
.backup/

# Local rollback snapshots (not committed — for manual recovery only)
KNOWLEDGE-BASE.backup-*/
CLAUDE.md.backup-*

# Benchmark / reference projects
Benchmark Systems/

# Seed version stamps — regenerated by `pip install -e` via setup.py hooks.
# See `seed-inheritance-hardening` Phase 3 + `projects/seed-core-consolidation` §7.1.
seed/backend/framework/noctusai_seed/_version_static.py
seed/backend/lib/noctusai_lib/_version_static.py

