# Seed Product — Live Reference Implementation

## Concept

The seed product (`products/seed/`) is a **real, running product** — the simplest possible implementation that proves the entire shared stack works end-to-end. It's the first stage of any new product's lifecycle, like an embryo that grows into a full product.

The template (`templates/product-seed/`) is **auto-generated from the seed** — a snapshot with placeholders for find-and-replace scaffolding.

## Architecture

```
products/seed/                    ← LIVE running product (source of truth)
    backend/  (port 8004)
    frontend/ (port 8100)
    schema: "seed"
    .backup/                      ← last known-good snapshot (gitignored)

templates/product-seed/           ← AUTO-GENERATED from products/seed/
    Same structure but with {{PLACEHOLDERS}}
    Generated by: scripts/sync-seed-template.sh
    .backup/                      ← last known-good snapshot (gitignored)
```

Git handles full version history (`git log -- products/seed/`).
`.backup/` is a local safety net — one snapshot, not committed.

## Checklist: Build the Live Seed Product

### Phase 1: Backend (DONE)
- [x] Create `products/seed/backend/` directory structure
- [ ] `app/config.py` — SeedSettings extending BaseAppSettings (port 8004)
- [ ] `app/database.py` — Supabase client targeting schema "seed"
- [ ] `app/dependencies.py` — get_current_user, get_user_role (with resolve_sso_role), get_org_id
- [ ] `app/main.py` — FastAPI app with configure_app, module-scope router imports
- [ ] `app/rate_limit.py` — shared limiter instance
- [ ] `app/routers/__init__.py`
- [ ] `app/routers/health.py` — health check endpoint
- [ ] `app/routers/notificacoes.py` — notification proxy (shared map_notification_to_pt)
- [ ] `app/routers/team.py` — team management (shared invitation helpers)
- [ ] `app/schemas/__init__.py`
- [ ] `app/services/__init__.py`
- [ ] `requirements.txt` — minimal deps
- [ ] `tests/conftest.py` — using shared MockSupabaseClient
- [ ] `tests/routers/test_health.py` — basic health check test
- [ ] `tests/routers/test_team_router.py` — invitation flow tests

### Phase 2: Database (DONE)
- [x] `migrations/001_seed.sql` — minimal schema:
  - `seed.status_pagina` (page status)
  - `seed.invitations` (team invitations)
  - RLS policies
  - Seed data for initial pages
- [ ] Register "Seed" product in `public.products` table

### Phase 3: Frontend (DONE)
- [x] Create `products/seed/frontend/` with Vite + React + TypeScript
- [ ] `package.json` — deps matching other products
- [ ] `vite.config.ts` — with shared aliases (@noctusai/shared, @/)
- [ ] `tsconfig.json` — matching other products
- [ ] `tailwind.config.ts` — extending shared base
- [ ] `postcss.config.js`
- [ ] `index.html`
- [ ] `src/main.tsx` — React root with QueryClientProvider
- [ ] `src/App.tsx` — routing: landing, login, forgot-password, accept-invite, sso, authenticated routes
- [ ] `src/integrations/supabase/client.ts` — createProductSupabase("seed")
- [ ] `src/store/authStore.ts` — createAuthStore()
- [ ] `src/lib/api-client.ts` — createApiClient with token refresh
- [ ] `src/components/auth/AuthProvider.tsx` — createAuthProvider
- [ ] `src/components/layout/Layout.tsx` — AppShell + Sidebar + Header + page status + SSO
- [ ] `src/components/NotificationBell.tsx` — shared NotificationBell wrapper
- [ ] `src/hooks/useNotificacoes.ts` — createNotificationHooks
- [ ] `src/pages/Landing.tsx` — minimal landing page
- [ ] `src/pages/Login.tsx` — shared LoginForm
- [ ] `src/pages/ForgotPassword.tsx` — shared ForgotPasswordPage
- [ ] `src/pages/AcceptInvite.tsx` — shared AcceptInvitePage
- [ ] `src/pages/SSOCallback.tsx` — shared SSOCallback
- [ ] `src/pages/Dashboard.tsx` — minimal dashboard
- [ ] `src/pages/Equipe.tsx` — team management
- [ ] `src/pages/NotFound.tsx` — 404 page
- [ ] `src/index.css` — imports shared tokens.css

### Phase 4: Integration (DONE)
- [x] Added to `start.sh` (port 8004 backend, port 8100 frontend)
- [x] 9 backend tests passing
- [x] Frontend builds clean
- [x] Added to CLAUDE.md architecture table
- [ ] Create frontend `.env` with VITE_ vars (on deployment)
- [ ] Register "Seed" product in `public.products` table (on deployment)
- [ ] Run `001_seed.sql` migration in Supabase (on deployment)
- [ ] Verify SSO from Core works (on deployment)
- [ ] Verify direct login works (on deployment)

### Phase 5: Auto-Sync Mechanism (DONE)
- [x] `scripts/sync-seed-template.sh` — copies seed → template with placeholder substitution + validation
- [x] `scripts/install-hooks.sh` — installs post-commit hook (shareable across machines)
- [x] `.git/hooks/post-commit` — detects seed changes, runs sync, amends commit to include template
- [x] `.gitignore` updated: `.backup/` and `.seed-syncing` excluded
- [x] Backup strategy: `.backup/` inside each scope (git-native history + local safety net)

### Phase 6: Documentation
- [ ] Update CLAUDE.md: add seed product to architecture table
- [ ] Update 07-SHARED-LIBRARY.md: note seed as reference implementation
- [ ] Update ROADMAP: mark seed product as complete

## Auto-Sync Rules

1. **Source of truth**: `products/seed/` — the running product
2. **Derived artifact**: `templates/product-seed/` — generated, never edited directly
3. **Backup before sync**: `.backup/` inside each scope (last known-good snapshot, gitignored)
4. **Version history**: git handles it natively (`git log -- products/seed/`)
5. **Validation**: after sync, verify all expected placeholders exist
6. **Trigger**: automatic via post-commit hook on any commit touching `products/seed/`
7. **Setup**: run `bash scripts/install-hooks.sh` once after cloning the repo

## Placeholder Mapping

| Seed value | Template placeholder |
|------------|---------------------|
| `Seed` / `Seed Product` | `{{PRODUCT_NAME}}` |
| `seed` (slug) | `{{PRODUCT_SLUG}}` |
| `seed` (schema) | `{{SCHEMA_NAME}}` |
| `8004` | `{{BACKEND_PORT}}` |
| `8100` | `{{FRONTEND_PORT}}` |
| `Sprout` (Lucide icon) | `{{PRODUCT_ICON}}` |
