"""Omie ERP connector MCP.

`mcp/omie` is a connector MCP server (the `mcp/<vendor>` family) for Omie,
the Brazilian ERP — composed on `mcp/_kit`. See `README.md` for the tool
surface and configuration.
"""
from __future__ import annotations

__all__: list[str] = []
