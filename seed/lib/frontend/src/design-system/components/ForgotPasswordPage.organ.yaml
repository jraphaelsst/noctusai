name: ForgotPasswordPage
path: seed/lib/frontend/src/design-system/components/ForgotPasswordPage.tsx
phase: pilot-seed
registered_at: "2026-05-29"
validation_status: validated

known_facts:
  - "8 product consumers; second in the auth trio. Always rendered as the destination of LoginForm's 'Esqueceu a senha?' link."
  - "Supabase-only path (no session-auth variant) — password reset flow inherently requires Supabase resetPasswordForEmail; the loginPath prop lets consumers wire back to their own login route."
  - "4-state machine: form → loading → success ('Email enviado!') → error. States mapped in source comments; sequential state model, no parallel states."
  - "Uses renderLink render-prop pattern so products supply their own router Link component without creating a React Router / Next Router dependency in the seed lib."
  - "Zero external deps beyond React + sonner + supabase + lucide; no react-hook-form."

errors_encountered: []

drifts_surfaced: []

alternatives_considered: []

manual_validation_log: []

integration_test_status: unknown

e2e_test:
  path: null
  status: pending
  last_run: null
  runs_in_ci: false

bugs_fixed_during_dev: []
