# NoctusAI workspace marker — the primary noc monorepo.
# Identifies this directory as a workspace for the MCP toolkit's
# workspace-aware tools (status, file_proposal, scaffold_product).
# See KNOWLEDGE-BASE/CONTEXT/PATTERNS/seed-workspace.md.
workspace_kind=primary
workspace_name=noctusai
# noctusai_home is DERIVED from this marker file's own directory for the primary
# workspace (see mcp/noctusai/workspace.py) — never hardcode an absolute machine
# path here: it would make REPO_ROOT resolve to the author's laptop on every other
# checkout (CI / fresh clone). Override per-machine with the NOCTUSAI_HOME env var.
bootstrap_version=1
created_at=2026-05-03T00:00:00Z
