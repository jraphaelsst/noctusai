# live-patterns-log.md — agno-dev-team-rollout

> **Purpose.** Append-only log of meaningful findings from any engineer during any batch. The shared scratchpad through which parallel engineers cross-pollinate. Per `KB § PATTERNS/master-tree-parallel-batches.md § 3.1`.
>
> **Read protocol.** Engineers read on every meaningful pause + before walking away from any file they edit. Master reads at every sync-gate.
>
> **Write protocol.** Append-only. One row per finding. Promote N≥2 entries to `cross-product-absorption-catalog.md` at sync-gates.

| Timestamp | Batch | Engineer | Finding | Suggested action |
|---|---|---|---|---|
| 2026-05-04 | B0 | orchestrator | (master init — log seeded) | — |
| 2026-05-04 | B0 | orchestrator | agno on PyPI at 2.6.4; Python 3.14.2 on system; MCP requires_python>=3.10 — compatible. | proceed with agno 2.6.4 in B1 deps. |
| 2026-05-04 | B0 | orchestrator | noctusai_lib has config/{settings,credentials}.py, primitives/{timeutil,roles,parsing,_correlation,exceptions,responses}.py, testing/{mocks,clients,assertions,...}.py — all consumption targets exist. | dev_team imports `from noctusai_lib.config.settings import …` directly; no shim needed. |
| 2026-05-04 | B0 | orchestrator | MCP server hierarchical registration: `tools/__init__.py::register_all` → `noctus.register_all` → `dev.register_all` → per-module `register(server)`. Sub-folders supported (`google/calendar/`, `google/maps/`). | mirror `google/calendar/` shape for `noctus/team/`. |
| 2026-05-04 | B0 | orchestrator | **Naming correction.** Originally planned `noctus.dev.team.{run,status,route}` (4 segments, violates `KB § PATTERNS/mcp-tool-conventions.md § 1`). Corrected to `noctus.team.{run,status,route}` — `team` becomes a new sub-service under `noctus`, parallel to `dev` and `business`. | PROJECT.md updated; tools land at `mcp/noctusai/tools/noctus/team/`. |
| 2026-05-04 | B0 | orchestrator | MCP `tools/noctus/__init__.py::register_all` will need to call `team.register_all(server)` alongside `dev.register_all(server)`. | B4 engineer touches both `tools/noctus/__init__.py` and the new `tools/noctus/team/` folder. |
| 2026-05-04 | B2 | engineer-A | Worktree was missing the B1 scaffold (cherry-picked `a08b35d` from `agno-dev-team-rollout`). Master `worktree-agent-a37f2272b50765731` was created from a different branch line (`update-findings-batch-1a-close`) that diverged from the agno scaffold. | Orchestrator: when spawning engineer worktrees, branch from `agno-dev-team-rollout` directly (not from a sister batch's branch). Logged to findings.md too. |
| 2026-05-04 | B2 | engineer-A | Shared charter shipped at 1586 words (≈2100 tokens) — slightly above the 1500-token target but within the 1200-1800 acceptable range. Tradeoff: covered all 22 CLAUDE.md §1 rules + the "Your role" handoff section explicitly; condensing further would have meant collapsing rule bodies to one-liners and losing the AI-agent voice. | Acceptable; no action. If future iterations need to trim, the candidates are §11 (replication-to-seed-symmetry) and §15 (no-silent-errors) — both have tight CLAUDE.md sources we could reference rather than restate. |
| 2026-05-04 | B2 | engineer-A | Per-role charter sizes: 692-919 words (≈900-1200 tokens) — squarely within the 1000-1500 target. Code Reviewer is the longest (919 words) because the language-time cross-check + apply-inline-then-delete + recurrence cadence + bundled-proposal flow each needed their own paragraph. | Pattern: roles with multi-stream responsibilities (CR, Security) trend longer; pure-execution roles (UX, PM) trend shorter. No action — charter length tracks role complexity. |
| 2026-05-04 | B2 | engineer-A | Smoke test `test_load_charter_raises_when_missing` was checking that `load_charter("leader")` raises FileNotFoundError — a stub-state guard. Once charters shipped, the test had to flip its meaning. Updated it to check a nonexistent role name instead so the negative-path coverage stays. | The B1 scaffold's contract-tests had an embedded assumption "until A engineer ships." Future engineer briefs should call out which existing tests are stub-state and need to flip — not just "add new tests." Added to engineer-A's report. |
| 2026-05-04 | B2 | engineer-A | Allowlist matrix in `tools/allowlists.py` does NOT yet expose `keeper_review` to Code Reviewer — only `keeper_validate`. Per design-reference.md §3, that's correct (Code Reviewer gets read-only keeper validation; Security has full keeper_validate + keeper_review). The role charters reflect the matrix faithfully. | No action — matrix and charters agree. Worth flagging for engineer B (tools catalog) that the resolver for `keeper_review` only needs to be wired for the security_engineer role. |
| 2026-05-04 | B2 | engineer-D | `configs/__init__.py` already ships an 11-agent stub fallback — the loader's real `list_configs()` strips the stub when YAML files exist. Means consumer code can `from dev_team.configs import load_config` BEFORE D ships and still get a valid 11-agent dict — the cutover happens automatically when `loader.py` lands. | Pattern worth promoting to other engineer scopes: stub-then-cut-over via try-import in the contract `__init__.py`. Already in B1 — flag as the canonical shape for B2/B3 contract cutover. |
| 2026-05-04 | B2 | engineer-D | `_config_uses_provider()` + `ANTHROPIC_API_KEY` guard ONLY fires when the default builder is in use. With injected `team_builder`, the eval harness runs without API keys — DI doubles as the test-mode switch (no `monkeypatch` of our own code needed). | Same DI pattern is reusable for any future eval/runner that wraps `team.run`. Flag for E (team factory) + B (any agent-instantiation entrypoints): accept `team_builder` / `agent_builder` as a keyword for testability. |
| 2026-05-04 | B2 | engineer-D | Worktree `agent-a54da492b66f44cdf` was based on `85789c9` (pre-B1-scaffold). Had to `git merge --ff-only b178535` to grab the dev_team scaffold before any work could land. Suggests the dispatcher created worktrees before the scaffold commit was on the worktree base — base-branch should be `agno-dev-team-rollout` (currently at b178535) for all B2 engineers. | Worktree-creation step in master-tree dispatch should explicitly base-branch on the rollout branch, not on stale main/origin/main. Add to `KB § PATTERNS/master-tree-parallel-batches.md` as a setup-gate rule. |
| 2026-05-04 | B2 | engineer-E | dev_team package was missing from base worktree (branch off pre-scaffold commit 85789c9); merged b178535 to pull in B0+B1 scaffolding before building. Sibling worktree a37f2272 already has it. | future engineers should branch off the rollout-scaffold tip, not main, OR `git merge` the rollout commit on entry — clean fast-forward in this case. |
| 2026-05-04 | B2 | engineer-E | Sub-team test pattern: monkeypatch `agents.base.CHARTER_DIR` + `SHARED_CHARTER_FILE` to a tmp dir with seeded markdown — exercises real `load_charter` against real disk (not a function patch). Compliant with "no self-monkeypatching" rule because the swap is on DATA, not behavior. | reuse this fixture shape in any future test that touches `build_agent` before A engineer ships charters. |
| 2026-05-04 | B2 | engineer-E | Mocking agno cleanly: inject 5 fake modules (`agno`, `agno.agent`, `agno.team`, `agno.models`, `agno.models.anthropic`) into `sys.modules` via `monkeypatch.setitem` + classes with `__init__(**kwargs)` capturing constructor args — local imports inside `build_agent` / `build_team` resolve to the fakes. Cleaner than `patch.object(base, "Claude", ...)` because the import never fires the real one. | reuse the `fake_agno` fixture shape (test_team_assembly.py) in any post-scaffold engineer test that constructs Agents/Teams without requiring agno install. |
| 2026-05-04 | B2 | engineer-E | `test_cli_run_without_api_key_returns_3` (B1 smoke) fails post-E because cli.py calls `build_team` BEFORE the API-key check — when build_team was a stub (NotImplementedError) the test caught it; now build_team needs agno+charters and raises ModuleNotFoundError/FileNotFoundError instead. Test acknowledges the gap in its own docstring. Resolution path: cli.py reorders the API-key check before `build_team(...)` (out of E's scope per FORBIDDEN list — naturally engineer A's adjacent territory or the master engineer at integration). | flag in findings.md; not a regression — known cross-engineer dependency. |
| 2026-05-04 | B2 | engineer-E | recommend_route uses three-tier heuristic: trivial (typo/rename/format/one-line/lint) → direct, team (design/security/incident/multi-domain/cross-product) → team, default → direct. Rationale field is structured + names `design_review_team` when design signals fire so Leader can route to sub-team at intake. Tests cover all three branches + edge cases. Returns `{recommended, rationale, matched}` — added `matched` for auditability per `feedback_no_silent_errors`. | future enhancement: pull keyword catalogs from a YAML config so they're tunable per-org without code edits — defer-with-destination: `accept-with-rationale.md` if not actioned this batch. |
| 2026-05-04 | B2 | C (memory+telemetry) | **Worktree dispatch slip — engineer worktree branched off pre-B1 commit; `dev_team/` did not exist in worktree.** Recovered via `git merge batch-speed-gains-tokens` (the branch carrying B1 scaffold) into worktree. Safety net (merging methodology) caught the gap. | Architect should set up engineer worktrees from the master branch tip AFTER scaffold lands, OR include explicit "merge from `<scaffold-branch>` first" in dispatch brief. |
| 2026-05-04 | B2 | C (memory+telemetry) | agno 2.6.4 has no first-class post-turn hook API on `Team` / `Agent`; shipped `TelemetryWrappedTeam` decorator + `wrap_agent_run` mutator instead. Both intercept `run()`, time, extract usage, write event. Drop-in replacements (forward all other attrs via `__getattr__`). | When agno gains hooks, swap the wrapper internals for hook registration; the public `register_team_hooks` / `register_agent_hooks` surface stays. |
| 2026-05-04 | B2 | C (memory+telemetry) | **Submodule import inside package `__init__` try/except is fragile** — `from dev_team.memory.shared import MemoryStore` triggers shared.py to import `craft` while `dev_team.memory.__init__` is mid-init. Worked because `import dev_team.memory.craft as craft` resolves the submodule path even when parent attrs aren't populated yet. | Pattern works but document for other engineers — for any future submodule-imports-sibling-submodule case in this scaffold, prefer explicit `import dev_team.<pkg>.<mod> as alias` over `from dev_team.<pkg> import <mod>` inside the package. |
| 2026-05-04 | B2 | C (memory+telemetry) | Used module-level configurable root (`set_store_root` / `set_db_path`) for test isolation instead of monkey-patching — keeps `feedback_no_monkeypatching_in_tests` clean. Tests use `tmp_path` fixture; reset to default in fixture teardown. | Pattern reusable for any future seed/lib module needing test-isolation overrides — module-level setter is cleaner than env-vars or monkeypatch. |
| 2026-05-04 | B2 | C (memory+telemetry) | Telemetry capture wraps the actual run with try/except/finally so `outcome="error"` is recorded even when the run raises; the original exception is re-raised. Telemetry write itself is wrapped to print to stderr on failure (no silent crash, but no run failure either). | Pattern: telemetry NEVER swallows the underlying exception, but ALSO never crashes the run if its own write fails — both are surfaced explicitly. |
| 2026-05-04 | B2 | engineer-F | `noctus.team.*` shipped (6 tools): run / status / route / metrics / agent_metrics / configure. All wrap `dev_team.mcp_facade` via lazy imports inside the FastMCP `@server.tool` handler. Naming sticks to 3-segment `noctus.team.<action>` per `KB § PATTERNS/mcp-tool-conventions.md § 1`. Pydantic In/Out per file with descriptive Field metadata. Live FastMCP server smoke-test confirms 6 tools register; 74 tools total. | engineer G/H consume the surface. |
| 2026-05-04 | B2 | engineer-F | **Test pattern**: `_RecordingServer` stand-in for FastMCP captures `(name, description, handler)` from `@server.tool(...)` decorator, returning the original function unchanged. Lets us assert wiring shape + call handlers directly without spinning a real FastMCP. Patches `dev_team.mcp_facade.<fn>` at the lazy-import boundary (per the `unittest.mock.patch.object` carve-out for external integrations — `dev_team` is a sibling system). 25 tests / 0.7s. | promote pattern to KB if a 2nd noctus.* sub-umbrella adopts it (N=2 → triage). |
| 2026-05-04 | B2 | engineer-F | **Branch-state surprise**: shared branch `agno-dev-team-rollout` did NOT contain the B1 scaffold commit `b178535`; it lived only on three sister worktree branches (`worktree-agent-a37f...`, `worktree-agent-a54da...`, `worktree-agent-a7e2...`). Cherry-picked `b178535` onto my worktree branch `worktree-agent-a59a7654bb29c78d0` to obtain `dev_team/` + `mcp_facade.py`. Recommend orchestrator merge `b178535` into the shared `agno-dev-team-rollout` branch (or push it from the originating worktree) so future engineers don't repeat the cherry-pick. | orchestrator fast-forward `agno-dev-team-rollout` to include `b178535`; document in branching methodology that B1-style scaffolds belong on the shared branch, not on per-engineer branches. |
