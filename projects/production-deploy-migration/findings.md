# findings — production-deploy-migration

Living evidence + learnings log for the `noctusai.com` cutover. Append in-the-moment; synthesize at close. Five categories: **slips · errors · mistakes · lessons · knowledge**. This is the raw material for "automate the learning by evidence" (PROJECT §2, §6 Phase 7).

---

## Knowledge (verified facts about the live environment)

- **2026-05-21 — Apex is a Replit Deployment, not VPS/Coolify.** `dig TXT noctusai.com` → `"replit-verify=bee5257b-b67c-4f17-9947-2b55c3d3ef4c"`. HTTP headers on `https://noctusai.com`: `server: Google Frontend`, `via: 1.1 google`, `GAESA` cookie, `x-cloud-trace-context` → Replit runs on Google Cloud. Apex A record `34.111.179.208` (Google LB). The app = "Sistema de Permutas Imobiliárias".
- **2026-05-21 — DNS authority is name.com, NOT Cloudflare.** `dig NS noctusai.com` → `ns1cny / ns2ckr / ns3jkl / ns4hny .name.com`. So Cloudflare is currently NOT in the path for noctusai.com. "Keep it in Cloudflare" requires an NS change first.
- **2026-05-21 — VPS hosts only supporting services.** `n8n.noctusai.com` + `waha.noctusai.com` → `72.61.28.36` (Hostinger VPS, Docker). The web app is NOT here.
- **2026-05-21 — `legacy.noctusai.com` and `www.noctusai.com` don't exist** (no DNS records). Apex-only config on Replit.
- **2026-05-21 — CF Developer-Platform MCP scope.** Authenticates (account `afa8484e8c877416477a638957e5a989`). Tools cover Workers/Pages/D1/KV/R2/Hyperdrive + docs search ONLY. **No DNS, no zone, no Tunnel, no Access tools.** Cannot perform the DNS/tunnel work — those need the CF dashboard or the CF API (token).
- **2026-05-21 — Platform ships only EPHEMERAL tunnels today.** `start.sh tunnel` runs cloudflared quick-tunnels (`*.trycloudflare.com`, "URL EFEMERA — muda a cada restart"). Production needs a **named** tunnel (stable ID + stable CNAME). Archived single-product prod runbook exists: `archive/.../reference/scripts/deploy/{01-provision-vps,02-deploy-stack,03-setup-tunnel}.sh` + `DEPLOY.md`.
- **2026-05-21 — Registrar of record is name.com, NOT Replit.** `whois noctusai.com` → `Registrar: Name.com, Inc.`, `Registrar URL: name.com`. Replit's UI fronts a name.com-registered domain. ⇒ unlock / nameserver-change / EPP-code all happen at **name.com** (directly, or via Replit if Replit gates that access).
- **2026-05-21 — Domain is transfer-eligible but LOCKED.** WHOIS: `Creation Date 2026-01-09` (~132 days ago > the 60-day ICANN minimum), `Expiry 2027-01-09`, `Domain Status: clientTransferProhibited` = registrar lock is ON. Must unlock before any registrar transfer.
- **2026-05-21 — CF Registrar requires CF DNS first (verified via CF docs MCP).** A domain must already use Cloudflare authoritative DNS (full setup) before its *registration* can transfer to Cloudflare. Mandatory order: (1) nameservers→CF, then (2) registrar transfer. Also: registrant contact unchanged in the 60d before transfer; each transfer adds 1yr (`.com` ~at-cost). Caveat doc'd: platforms that double as host+registrar and block NS changes (Shopify/Wix/Block) force a transfer-to-an-intermediate-registrar-first workaround — does NOT apply here since the registrar is plain name.com.
- **2026-05-21 — Coolify confirmed on the VPS.** Probe of `72.61.28.36`: `:8000 → 302` (Coolify dashboard login redirect); `:80 → 404`, `:443 → 503` (HTTP/2, h3 advertised) = Traefik default backend for an unknown Host. ⇒ Coolify + Traefik present and routing by hostname.
- **2026-05-21 — The Permutas app = the in-repo `erp-imobiliario` lineage.** `grep -ri permuta` → a full `products/erp-imobiliario/frontend/src/components/permutas/*` tree + `pages/Permutas.tsx`, `PermutaDetalhes.tsx`, `hooks/usePermutas.ts`, `001_erp_imobiliario.sql`. So the live Replit "Sistema de Permutas Imobiliárias" is almost certainly an older/standalone deploy of `erp-imobiliario`. ⇒ legacy source is available in-repo (confirm exact version/repo with user).
- **2026-05-21 — Edge vs compute: where "all in Cloudflare" hits its ceiling.** Verified via CF docs MCP. Cloudflare CAN be 100% of the **edge** (registrar + DNS + TLS + Tunnel + WAF/CDN). It CANNOT be a drop-in host for the **compute** (the running fleet) without a re-architecture: Workers = 128MB/isolate + CPU-time limits (no long-running FastAPI); CF **Containers** IS now GA (Dockerfile-based, up to `standard-4` = 4 vCPU/12 GiB/20 GB, large aggregate limits) BUT it is **Worker + Durable-Object-fronted, scale-to-zero**, with no managed Redis/WAHA — i.e. not a `docker compose up` host for a 9-service always-on stack. ⇒ compute needs ONE server (Hostinger/any VPS) behind the CF tunnel; CF Containers = a future re-architecture project, not this migration.
- **2026-05-21 — Legacy app stack = `one-permutas` (Django, NOT FastAPI).** Repo `github.com/jraphaelsst/one-permutas` (separate from noc; the pre-absorption real-estate *matching system* later folded into `erp-imobiliario`). Stack: Django + DRF, **Celery** (Redis broker — bilateral-matching async), **Supabase Postgres** (SQLite fallback), React CRA frontend built into `backend/frontend_build/` + served by Django via **WhiteNoise**. **Single-port :5000** (Gunicorn). Start = `bash run.sh` (npm build → pip install → collectstatic → migrate → celery + gunicorn). Replit ran it as an `autoscale` deployment. ⇒ legacy redeploy = containerize (Node build → Python runtime, gunicorn + celery), run on the VPS, point `legacy.noctusai.com` at it via the tunnel. Stays **standalone** (different stack ⇒ NOT absorbed into the seed fleet).
- **2026-05-21 — secrets.txt was NOT pushed (so not publicly leaked).** GitHub API: repo `pushed_at` = 2026-03-17; no `secrets.txt` in the `main` tree. The user's "added a secrets.txt" did not reach the remote (uncommitted/unpushed). ⇒ derive the legacy app's required env-var NAMES from its code (settings.py / `os.getenv` / `frontend/.env.production` keys), not from a secrets file; secret VALUES are supplied by the user at deploy time. (`frontend/.env.production` IS committed — likely just public Supabase URL+anon key.)
- **2026-05-21 — Registrar reality: the domain is Replit-MANAGED via name.com (reseller).** User registered it "in Replit"; WHOIS shows registrar = name.com. ⇒ Replit is the management UI, name.com the ICANN registrar of record. To move the domain to Cloudflare you MUST get the **unlock + transfer (EPP) code** from the current custodian — Replit's domain settings (or name.com if direct access exists). There is **no Cloudflare-side / MCP bypass** — domain transfer is deliberately gated behind that code (anti-hijacking). The single Replit interaction to fetch the EPP code IS the act of leaving Replit, not staying attached.

## Lessons

- **2026-05-21 — Verify infra against the live DNS, never against the stated stack or the KB.** The user believed Hostinger+Coolify; the KB said the VPS hosts the app with Let's Encrypt; reality (TXT + headers) said Replit-on-GCP. `dig`/`curl -I` settled it in seconds. (Codebase-is-source-of-truth applies to infra too.)
- **2026-05-21 — Named tunnel ≠ quick tunnel.** The user remembered tunnels as a throwaway dev thing — that's the *quick* tunnel. The *named* tunnel is the persistent, production-sanctioned mode. The distinction is the whole answer to "why is tunnel a real deploy solution." (See PROJECT §2 / response 2026-05-21.)
- **2026-05-21 — "Move DNS to CF" ≠ "transfer the domain to CF".** Two separate operations: a **nameserver change** (delegates the DNS-host role; fast, free, reversible — and ALL the cutover needs) vs a **registrar transfer** (moves ownership/billing; ~5 days, optional). Never block the deploy on the slow one.
- **2026-05-21 — The management UI is not the registrar.** Domain "lived in Replit" but the registrar of record is name.com. Always `whois` to find the true registrar before planning unlock/transfer/NS work.
- **2026-05-21 — "Migrate ALL to Cloudflare" has a hard ceiling — and the honest answer evolves with CF's product line.** Don't answer "can CF host this?" from memory; check current capabilities (CF Containers went GA / grew 15× since early 2026). The durable distinction: **edge = yes, fully CF; compute = needs a server, or a rewrite onto CF's serverless model.** Honor the user's *intent* (fewer vendors) by maxing the CF edge + collapsing to ONE compute box, not by pretending CF replaces a VPS for a docker-compose fleet.

## Knowledge — deploy artifacts (2026-05-21)

- **Deploy artifacts authored by 4 parallel agents → `deploy/{fleet,tunnel,legacy}/` + `fleet-readiness-audit.md`.** All on disk, untracked (NOT committed). Cross-cutting learnings:
  - **Legacy `settings.py` hardcodes `SECRET_KEY` + `DEBUG=True` + `ALLOWED_HOSTS`** (not env-read) → needs a prod settings override (env-driven) before deploy; and since the key is in a PUBLIC repo it MUST be **rotated** (session/CSRF forgery). Top Phase-3 task.
  - **Tunnel ingress = single source of truth** (`deploy/tunnel/ingress.yml`); `seed` dropped (template). `protocol: http2` pinned (QUIC/NAT rule).
  - **Product Dockerfiles hardcode `FROM noctus-seed-*-base:dev`** (no tag ARG) → bases pin to `:dev` regardless of `NOCTUS_IMAGE_TAG`. Follow-up: parameterize via ARG for per-commit CI tagging.
  - **`VITE_*` are build-time `--build-arg`s and DIFFER per product** (core→`VITE_CORE_API_URL`; erp→both; rest→`VITE_CORE_URL`); real ARG is `VITE_SUPABASE_PUBLISHABLE_KEY` (not `.env.example`'s older `VITE_SUPABASE_KEY`). Supabase keys come from CI/build env, not `frontend/.env.production` (CRA flags only).
  - **dev-team is image-baked in prod** (`COPY dev_team /opt/dev_team` + `pip install -e` at build); only `ANTHROPIC_API_KEY` is runtime — no bind-mount in prod.

## Knowledge — Hostinger VPS + API (2026-05-21)

- **VPS specs (live, via Hostinger API).** Plan **KVM 2** — **2 vCPU / 8192 MB (8 GB) RAM / 102400 MB (100 GB) disk**, 8 TB bandwidth. OS template **Ubuntu 24.04 with Coolify**. State running. id `1303151`, IPv4 `72.61.28.36` (matches), DC 22, created 2026-01-27.
- **Sizing verdict** (audit rec 4 vCPU/8 GB/80 GB; min 2 vCPU/4 GB): RAM ✅ (8 GB), disk ✅ (100 GB > 80). **CPU ⚠️ = the constraint: 2 vCPU = the *minimum*, below the recommended 4.** Deployable now (slim `runtime` images skip the cold-boot vite-build CPU spike); fine for launch / low–moderate traffic. Risk: real WAHA (Chromium) + 8 products on 2 vCPU can be CPU-bound under load → consider in-place upgrade to KVM 4 (4 vCPU) if it gets busy.
- **noctusai.com is NOT in Hostinger's domain portfolio** (registrar = name.com) ⇒ Hostinger's domains/DNS API can't manage it; the name.com→Cloudflare path is unchanged.
- **Hostinger API surface mapped** (base `https://developers.hostinger.com`, Bearer): VPS (list/get/start/stop/restart/actions/metrics/snapshot), Domains (portfolio/availability/nameservers/lock/privacy), DNS (zones get/update/delete/validate, snapshots) + billing/hosting/email/docker. → `mcp/hostinger` connector v1 = VPS read + power actions + diagnostics (Engineer E); domains/DNS/etc = v2.
- **`mcp/hostinger/.env` holds the TEMP token** (gitignored, `git check-ignore`-verified). User rotates after the MCP is wired.
- **`mcp/hostinger` connector SHIPPED** (Engineer E): 8 tools (`vps.list/get/metrics/actions` + confirm-gated `vps.start/stop/restart` + `diagnostics.connection_status`), 18 tests green, **live read-only validation** passed against VM `1303151`. `.mcp.json` registration is user-gated (snippet in its README). Pending architect three-way sync (CLAUDE.md §2 pointer + MEMORY.md line + memory file).
- **`mcp/cloudflare` connector DISPATCHED** (Engineer F): v1 = zones + DNS records + Cloudflare Tunnel + diagnostics over CF API v4 (Bearer). Needs a **user-created scoped token** in `mcp/cloudflare/.env` (scopes: Zone:DNS:Edit, Zone:Read/Edit, Account:Cloudflare Tunnel:Edit); account_id already captured. No live validation until the token exists.
- **Learning — KB MCP-SERVERS path** is `KNOWLEDGE-BASE/MCP-SERVERS/` (NOT under `CONTEXT/`); my briefs said the wrong path, engineer corrected.
- **Learning — Cloudflare WAF 403s the default urllib User-Agent** (hit live on the Hostinger API, which sits behind CF). Connector `request_json` must send a browser-style UA. Lifted into the `_kit`-consolidation follow-up.
- **DRY recurrence (N=4 → MUST-formalize) FILED** → `projects/kit-connector-boilerplate-consolidation/` (shared `request_json` + confirm-gate + settings base into `_kit`; the browser-UA wrinkle rides along).

## Knowledge — VPS deploy mechanics (2026-05-21)

- **Code delivery = git, NOT rsync (user decision).** The platform code reaches the VPS via `git clone`/`git pull` from `github.com/jraphaelsst/noctusai`, using a **read-only deploy key** generated on the VPS (`ssh-keygen` there → add the pubkey to the repo's Deploy Keys, read-only). Redeploys = `git pull` + rebuild. Rationale: version-pinned + auditable on the box; no stateful rsync diffing. The earlier rsync attempt is **abandoned**.
- **`origin/main` is 6 commits behind local `main`** (`origin` = `d3107b27` 2026-05-20; local = `97d4ac7a`). The 6 unpushed commits include **social-wiring** (platform-auth + vista-seed-lift + fan-out) — one of the 3 Wave-1 deploy targets. ⇒ a git-clone on the VPS would build **stale social-wiring** unless we **push first** (FF push of already-committed work, confirm-gated). 11 working-tree files also dirty (the connector three-way-sync KB docs + this project's untracked files).
- **Secrets do NOT travel via git.** Root `.env` (29 vars) is gitignored → delivered **out-of-band** (one-time `scp` of the file to the VPS = secret delivery, not a code-sync). Service secrets (`POSTGRES_PASSWORD`, `WHATSAPP_API_KEY`, `WAHA_DASHBOARD_PASSWORD`) → `deploy/services/.env.services` created on the VPS from the live containers, never committed.
- **VPS deploy-state recon (read-only SSH, working).** `/opt/noctus` exists but EMPTY (no code yet); `git` + `docker 29.2.0` present; `noctus-net` bridge up; disk 16 GB used / 81 GB free of 96 GB. Running: full Coolify control-plane + the 4 stateful services.
- **Coolify-managed service wiring (for the raw-compose swap).** Volumes: `n8n-data→/home/node/.n8n`, `waha-sessions→/app/.sessions`, `waha-media→/app/.media`, `pgdata→/var/lib/postgresql/data`, `redis-data→/data` (all `r8co0gwsw8g4gggcc0sko8ko_*`). Traefik routes: `n8n.noctusai.com→n8n:5678`, `waha.noctusai.com→waha:3000` (these MUST move to the CF tunnel — Traefik dies with Coolify). pg = `postgres:16`, `POSTGRES_DB/USER=default`. **n8n is SQLite-backed** (no `DB_POSTGRESDB_*` env) → the postgres container's consumer is unidentified; verify before final teardown. WAHA had `NODE_OPTIONS=--max-old-space-size=16384` (16 GB ceiling on an 8 GB box — lowered to 2048 in the raw compose). Raw replacement authored at `deploy/services/` (compose + README + `.env.services.example`).
- **GitHub auth available in-session.** `gh` is logged in as `jraphaelsst` with `repo` scope → can add the VPS deploy key via `gh repo deploy-key add` (confirm-gated write) and the FF `git push` is doable from here (confirm-gated).

## Slips
- **2026-05-21 — Misread "no Coolify" repeatedly as "don't deploy *through* Coolify, leave it running" — the user meant REMOVE it entirely.** Cost repeated corrections + a rejected tool use + user frustration. Lesson → [[feedback_drop_coolify_decommission]]: "drop / no / migrating-out-of X" defaults to REMOVE X (confirm the strong reading), never "leave idle". Coolify recon (2026-05-21): stateful data is in named volumes `r8co0gwsw8g4gggcc0sko8ko_{n8n-data,pgdata,redis-data,waha-sessions,waha-media}` (survive container rm → migrate by reusing them); Coolify ALSO = the n8n/waha public reverse proxy (Traefik+LE) ⇒ its removal needs the CF tunnel to take over routing (couples final removal to the domain/DNS step). Control-plane: coolify, coolify-proxy, coolify-db, coolify-redis, coolify-realtime, coolify-sentinel.

## Errors
- _(none yet)_

## Mistakes
- _(none yet)_

---

## Tracked drift to fix (named destinations — not silent)

- **`KB § CONTEXT/05-INFRASTRUCTURE.md` is stale for the apex** — it implies the VPS hosts the app with Let's Encrypt; the apex is actually a Replit Deployment. **Destination:** fix in Phase 7 (after the new infra becomes the truth), as part of writing `KB § GUIDES/production-deploy.md`. Logged here so it is not silently dropped.
- **`one-permutas` reference brought in (gitignored).** Cloned to `projects/production-deploy-migration/reference/one-permutas/` (117M — upstream commits `venv/` 76M + `staticfiles/` 25M + `frontend_build/` 8.9M). Added to `.gitignore` so it never enters noc history; treated as read-only (not edited).
- **⚠️ LGPD / public-exposure (out-of-noc-scope → user's own repo).** `github.com/jraphaelsst/one-permutas` is **PUBLIC** (`visibility: PUBLIC`, last push 2026-03-17) and commits `db.sqlite3` (232K) + `venv/` + `staticfiles/`. If that sqlite holds real proprietário/corretor PII, it is a public data exposure. **Recommend (user action):** make the repo private OR purge `db.sqlite3`/`venv`/`staticfiles` from history + add a `.gitignore`. Surfaced, not auto-fixed — modifying the user's external public repo is outward-facing / hard-to-reverse.
