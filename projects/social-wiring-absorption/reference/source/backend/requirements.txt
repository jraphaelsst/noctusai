-e seed/lib/backend
-e seed/framework/backend
fastapi==0.115.0
uvicorn[standard]==0.30.6
supabase==2.9.1
pydantic==2.9.0
pydantic-settings==2.5.2
python-dotenv==1.0.1
# Transitive: pydantic EmailStr → email-validator at runtime; FastAPI Form/File → python-multipart
email-validator>=2.0.0
python-multipart>=0.0.6
httpx>=0.27.0
openai>=1.40.0
PyJWT==2.9.0
slowapi>=0.1.9

# Observability (optional)
sentry-sdk[fastapi]==1.40.0

# Caching (optional)
redis==5.0.1

# Testing
pytest-asyncio>=0.21.0

# YouTube Data API v3 (Phase 1 — youtube_service)
google-api-python-client>=2.120.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0

# Token encryption at rest (Phase 1 — credential_store)
cryptography>=42.0.0

# Google Drive download fallback (Phase 2 — gdrive_service)
gdown>=5.1.0

# PDF text extraction + page rasterization (Phase 5 — media_service handles
# inbound document attachments). pdfminer.six covers text-PDFs;
# PyMuPDF rasterizes scanned-image PDFs (e.g. CNH) so they can flow
# through the vision model.
pdfminer.six>=20231228
PyMuPDF>=1.24.0
