# LGPD Concerns — Rolling Log

> **Auto-generated** by `noctus.dev.lgpd_flag` (see
> `mcp/noctusai/tools/lgpd.py`). Each item is an unresolved concern
> discovered during development. They are **not blockers** — the code
> ships and this file tracks what must be reviewed before the feature
> can be called done.
>
> Mark an item `- [x]` when the concern is resolved (code changed or
> dismissed with rationale). Do not delete items — strike through or
> move to an "Archive" section at the bottom.
>
> Philosophy + the five questions: `KNOWLEDGE-BASE/CONTEXT/PATTERNS/lgpd.md`.

- [ ] **Vista CRM clientes (42.960 titulares) are now read live by the ERP Clientes tab. Data categories actually returned by tenant `oneconsu-rest`, re-derived from the live 400s on 2026-08-21 and NOT the pre-grant guess: identification (Codigo, Nome), contact (Celular), relationship (Status, DataCadastro, Corretor, Interesse), and demographics (DataNascimento, Sexo, EstadoCivil, Profissao). No CPF, no address, no e-mail — all four are rejected by this tenant. Three decisions remain OPEN and are the controller's, not an agent's: (1) the legal basis under LGPD art. 7 for NoctusAI processing Vista-held titulares as operator — legitimate interest vs the agency's existing basis; (2) whether a data-processing agreement / operator clause with Vista covers this read-through; (3) the retention period for erp.user_actions_log rows, which record WHICH admin opened WHICH client and when — those audit rows are themselves personal data about both parties and currently have no stated retention.** at `products/erp-imobiliario/backend/app/services/vista_showcase_service.py` — A granted vendor permission is not authorization to process. The read surface shipped under a deliberately conservative posture (below), which is what makes it safe to run while the paperwork questions are answered — but the questions are real and unanswered, and recording them is the difference between a considered risk and an unnoticed one. Flagged proactively at build time rather than discovered in an audit.
  - *Mitigation*: Shipped mitigations, all structural rather than procedural: (a) TWO-TIER MINIMISATION — the list projection requests 7 of the 11 available fields; the four demographic ones are fetched only for one named `codigo` when an admin opens that record. Enforced by the type system, not by convention: `ShowcaseCliente` has no field to hold a birth date, so a list response cannot carry one even if the tenant over-answers. Tested on both halves — the wire (what we ASK Vista for) and the shape (what we COULD serve). (b) NO PERSISTENCE — Vista payloads are never written to ERP; only call metadata reaches the audit log. (c) NO RAW PASSTHROUGH — unlike the imóvel/usuário/agência DTOs, the cliente DTOs carry no untouched payload and the envelope reports raw_available=false, so the UI offers no payload dump. (d) SHORT BROWSER CACHE — staleTime 15s / gcTime 60s, asserted in a hook test, so the browser does not become the cache the backend refuses to be. (e) ADMIN-ONLY at the backend dependency, not merely hidden in the UI. (f) DIFFERENTIATED AUDIT — list and detail calls write distinct `projection` values, so "listed fifty names" and "opened one person's profile" stay distinguishable after the fact, which is the question an audit actually asks. (g) The detail query cannot fire without a chosen codigo — pinned by a test.
  - *Flagged*: 2026-08-23
- [ ] **The card document-upload surface (lead-card-hub Phase 2, cliente_documentos) is designed to eventually accept identity documents (RG, CPF) attached to a cliente's card. These are sensitive personal-data categories under LGPD (documento de identificação) requiring an explicit data-category intake — lawful basis, retention justification, and access controls — before upload is enabled for them.** at `products/social-wiring/backend/migrations/057_card_hub_documentos.sql` — Migration 057 seeds the cliente_documento_tipos catalogue with 'rg' and 'cpf' rows explicitly marked identidade=true and ativo=false, so the upload endpoint (app/modules/card_hub/documentos_service.py::upload_documento) refuses them by construction until a human flips ativo=true after this intake is resolved. This flag is the durable record of that pending decision, filed at the same time the withholding was implemented (2026-08-18), per contract §2's explicit instruction: "Before any RG/CPF-bearing document type is enabled, file the data-category intake via noctus.dev.lgpd_flag."
  - *Mitigation*: Conservative default: cliente_documento_tipos seeds rg/cpf with ativo=false, so the upload allow-list check (documentos_service._require_tipo_documento) rejects them with a typed 400 naming "pendente de intake LGPD" until a human explicitly enables the type post-intake. All other document types (contrato, proposta, comprovante_pagamento, comprovante_endereco, planta_imovel, foto_imovel, outro) are unaffected and already ship LGPD-complete (object RLS, table-driven retention, append-only access log).
  - 🔴 *Scope WIDENED 2026-08-22 (migration 068 — birthdate auto-extraction)*: the intake must now also cover a SECOND processing purpose, distinct from storing the file. When an RG/CPF is uploaded, `app/modules/card_hub/identidade_extracao_service.py` reads the document's content and **derives a new personal-data field** (`clientes.data_nascimento`) from it. Deriving data from a sensitive document is its own processing activity and needs its own lawful basis and retention justification — enabling `ativo=true` now authorises both, not just the upload. Three controls ship with it and should be named in the intake: (1) only a HIGH-confidence, label-anchored read is written unattended — a low-confidence read stays on the document row as a suggestion for a human and never reaches the client record; (2) provenance is mandatory (`data_nascimento_origem` / `_documento_id` / `_em`), so every derived value stays attributable and correctable, and a human-entered value always outranks a machine read; (3) as of migration 069 a low-confidence read can only reach the record through an explicit human confirmation, which stamps `data_nascimento_confirmado_por`/`_em` — so an OCR-derived birthdate on a client record is always attributable either to high confidence (applied automatically, `confirmado_por` NULL) or to the named person who vouched for it, and a turned-down suggestion is recorded rather than erased; (4) the automated read appends to `cliente_documento_acessos` with `acao='extract'` (the CHECK was widened for it), never as a `view` by a null user — a machine read must stay distinguishable from a human one. The extraction path remains UNREACHABLE while `ativo=false`; nothing about this change flips it.
  - *Flagged*: 2026-08-18
- [ ] **Public validate endpoint discloses nome and tipo (proprietario/locatario role) without rate-budget separation from other public endpoints** at `products/erp-imobiliario/backend/app/routers/portal_externo.py:GET /api/portal/{portal_token} (validar)` — Token enumeration probe via /api/portal/{token} returns 200+{nome,tipo} on valid tokens vs 404 on invalid. Combined with leaked token = identity confirmation. Shared rate-limit budget (DEFAULT_PORTAL_RL) with data endpoints means a probe attack only costs the same per-token budget as a real fetch. Phase 6 wiring audit pin (lowest severity in this batch; rate-limit + token-length-48-urlsafe = ~280 bits make brute-force impractical).
  - *Mitigation*: ACCEPT-WITH-RATIONALE: 48-byte urlsafe entropy + slowapi DEFAULT_PORTAL_RL + 90d default validity = enumeration-cost adequate for the disclosed surface (just name + role, no contact/financial). Re-evaluate if disclosure expands.
  - *Flagged*: 2026-05-20
- [x] ~~**Public bearer-token endpoint surfaces full documentos rows without filter on document sensitivity / sharing-status flag** at `products/erp-imobiliario/backend/app/routers/portal_externo.py:GET /api/portal/{portal_token}/documentos` — select("*") on documentos table joined to cliente_id has no `compartilhado_portal=True` or sensitivity gate (the existing test fixture in test_portal_externo_router.py:TestPortalDocumentos sets `compartilhado_portal: True` but the production query does NOT filter by it). Documents can carry CPF, contract scans, ID copies. Phase 6 wiring audit pin — highest severity item in this batch.~~
  - ~~*Mitigation*: Phase 7 (highest-LGPD-sensitivity surfaces) — add `.eq("compartilhado_portal", True)` filter (matching the test fixture's implicit contract) + DTO whitelist + per-access tap into ai_audit; consider per-document sensitivity-class gate. This is the strongest "fix-on-contact-eligible" item but it's outside Phase 6's wire-audit scope per the brief — surfaced as a P0 follow-up.~~
  - *Flagged*: 2026-05-20
  - **RESOLVED 2026-05-20** by `projects/erp-portal-documentos-sharing-gate/` P0-P2 (Engineer D): migration `031_documentos_compartilhado_portal.sql` adds `compartilhado_portal boolean NOT NULL DEFAULT false` to `erp.documentos`; `portal_externo.py:portal_documentos` filter consumes it (.eq("compartilhado_portal", True)); admin opt-in via `PATCH /api/documentos/{id}/compartilhamento` role-gated to platform_admin/owner/admin/manager; every portal-side READ + every admin-side toggle logged via `log_action()` (Art. 18 audit trail). P3 (FE admin UI + admin-alert) + P4 (full tests rewrite) + P5 (verify keeper) deferred to next-wave dispatch per architect decision.
- [ ] **Public bearer-token endpoint surfaces full lancamentos table rows (valores, data_vencimento, contrato_id linkage) without per-access audit** at `products/erp-imobiliario/backend/app/routers/portal_externo.py:GET /api/portal/{portal_token}/financeiro` — Financial entries with cliente_id linkage = sensitive financial profile (LGPD Art. 5 II — combined dataset). select("*") returns ALL lancamentos columns; if migration adds future PII columns (e.g. cpf, conta_bancaria) they leak automatically. Same risk as portal-cliente/{token}/financeiro endpoint. Phase 6 wiring audit pin.
  - *Mitigation*: DEFERRED to follow-up `erp-imobiliario-dto-contract` (per PROJECT.md §7 Q-E) — wrap with a portal_lancamento_to_dto whitelist mapper (parallels Phase 3b lancamento_row_to_dto). N=2 with portal-cliente/financeiro → recurrence rule fires at next consumer = MUST formalize as seed primitive (`portal_dto_whitelist`). Current safety: rate-limit + token expiry + is_active gate.
  - *Flagged*: 2026-05-20
- [ ] **Public bearer-token endpoint surfaces owner property inventory (cidade, bairro, logradouro, numero, valor, fotos) without authenticated identity context** at `products/erp-imobiliario/backend/app/routers/portal_externo.py:GET /api/portal/{portal_token}/imoveis` — Property address + value + photos qualify as personal data of the proprietário (LGPD Art. 5 I — combined with token-leak the dataset is re-identifiable to the natural person). Surface is rate-limited (DEFAULT_PORTAL_RL) + token-gated (90-day default validity, admin-revocable). Phase 6 wiring audit — no auth context = no per-user audit log on access; only the issuing user.id is logged at gerar-link.
  - *Mitigation*: Phase 7 (highest-LGPD-sensitivity surfaces): add per-access tap into ai_audit log via standard_routers seam; consider shorter default validity for portal links (30d) + force-rotate on revocation; current dataset already filters to is_active=True + expires_at not-past at _validate_token.
  - *Flagged*: 2026-05-20
- [x] ~~**ERP Banco router (`/api/banco`) ingests bank-statement extratos (`POST /importar`) and runs reconciliation (`GET /conciliacao`, `POST /conciliar`) + remessa-bancária generation (`POST /gerar-remessa`) + retorno parsing (`GET /retorno/{id}`). Each statement line carries counterparty identity (payor/payee — typically the locatário CPF/CNPJ + nome + bank-account fragment) + nominal value + date. Remessas to the bank carry the org's outbound payment instructions including beneficiary bank data. This concentrates Art. 5 PII (counterparty bank identity) + Art. 7 financial data + Art. 18 access-right-relevant transactional history. Router uses `Depends(get_current_user)` only — not role-gated.**~~ *Resolved 2026-05-20 by erp-financial-surfaces-role-gate* — every endpoint in `app/routers/banco.py` now uses `Depends(require_role("platform_admin", "owner", "admin", "manager"))`; aggregate reads (`/extratos`, `/conciliacao`) write `log_action("ler", "banco_extratos"|"banco_conciliacao", …)`; gate contract pinned at `tests/routers/test_banco_role_gate.py`. (Original entry at) `products/erp-imobiliario/backend/app/routers/banco.py` — Phase 7 audit (erp-wiring): banco is the fourth high-LGPD ERP surface. Mutations are audit-logged (`/importar`, `/conciliar`, `/gerar-remessa` all hit `log_action()` — good). Reads (`/extratos`, `/conciliacao`, `/retorno/{remessa_id}`) are NOT audit-logged and NOT role-gated. Bank-extrato lines + retorno parsings can include 3rd-party counterparty data (the payor's identity) NOT directly under the platform's data-subject consent — Art. 7 §3 legítimo interesse basis applies for accounting purposes but the access concentration warrants the same role-gate as the financeiro/dimob/impostos cluster.
  - *Mitigation*: (a) Role-gate `/api/banco/*` to `("platform_admin", "owner", "admin", "contador", "manager")` via `Depends(require_role(...))`. (b) Audit-log reads on `/extratos` + `/conciliacao` + `/retorno/{id}` to close the read-access gap. (c) Retention: extrato lines are accounting records — 5y minimum (CTN); confirm `delete()` shape on `bank_movimentacoes` table is soft + reversible. (d) The 4 routers (financeiro/dimob/impostos/banco) share the same role-gate gap — file a single follow-up project `erp-financial-surfaces-role-gate` to land role-gating + read audit-logging across all four in one pass (N=4 DRY recurrence formalization candidate).
  - *Flagged*: 2026-05-20
- [x] ~~**ERP Impostos CRUD (`/api/impostos`) writes/reads `erp.impostos` rows that record tax obligations (IPTU/ITBI/ISS/etc.) tied to imóveis + clientes/contratos — natural-person fiscal-identifier data (CPF/CNPJ) joined with nominal values + due dates. Per-record `GET/POST/PATCH/DELETE` + the `/resumo` aggregator concentrate Art. 5 PII (counterparty identity via the joined contract) + Art. 7 fiscal data. Endpoints use `Depends(get_current_user)` only — not role-gated; any authenticated team member can list/edit/delete the org's full tax ledger.**~~ *Resolved 2026-05-20 by erp-financial-surfaces-role-gate* — read endpoints in `app/routers/impostos.py` now use `Depends(require_role("platform_admin", "owner", "admin", "contador"))` (fiscal-tier); `/resumo` writes `log_action("ler", "impostos_resumo", …)`; gate contract pinned at `tests/routers/test_impostos_role_gate.py`. (Original entry at) `products/erp-imobiliario/backend/app/routers/impostos.py` — Phase 7 audit (erp-wiring): impostos is the third of the four highest-LGPD-sensitivity ERP surfaces. Mutations are audit-logged via `log_action()` (good — criar/editar/excluir all hit the log). Reads (`GET /api/impostos`, `/resumo`, `/{id}`) are NOT audit-logged and NOT role-gated. Same shape as `financeiro` and `dimob` — three N=3 absorption candidates for a per-router role-gate convention.
  - *Mitigation*: (a) Role-gate financial/fiscal reads to `("platform_admin", "owner", "admin", "contador", "manager")` via `Depends(require_role(...))` — bound factory at `app/dependencies.py:121`. (b) Audit-log reads on `/resumo` (aggregate fiscal view). (c) Retention: tax records carry CTN Art. 174 5y minimum + Art. 173 5y prescription; LGPD Art. 16 §1 legal-obligation basis covers retention but the read-access concentration concern remains. (d) The three financeiro/dimob/impostos role-gate gaps are byte-identical pattern — file a follow-up `erp-financial-surfaces-role-gate` project to formalize once Phase 8 lands (N=3 DRY recurrence).
  - *Flagged*: 2026-05-20
- [x] ~~**ERP DIMOB router (`/api/dimob`) generates the Brazilian RFB tax declaration (Declaração de Informações sobre Atividades Imobiliárias) which by RFB IN 1115/2010 MUST contain landlord CPF/CNPJ + nominal income paid + property identifier per locação rendered during the year. `GET /api/dimob/preview`, `/validate`, and `POST /generate` concentrate per-natural-person tax-linked financial data (Art. 5 PII + Art. 7 fiscal-data) into a single download surface. The router uses `Depends(get_current_user)` only — not role-gated; any authenticated team member can preview/generate the org's full DIMOB.**~~ *Resolved 2026-05-20 by erp-financial-surfaces-role-gate* — every endpoint in `app/routers/dimob.py` now uses `Depends(require_role("platform_admin", "owner", "admin", "contador"))` (fiscal-tier; "manager" intentionally excluded); `/preview` and `/validate` write `log_action("ler", "dimob_preview"|"dimob_validate", …)` (the existing `/generate` log is preserved); gate contract pinned at `tests/routers/test_dimob_role_gate.py`. (Original entry at) `products/erp-imobiliario/backend/app/routers/dimob.py` — Phase 7 audit (erp-wiring): DIMOB is the single highest fiscal-PII concentration in the platform — by design it lists every landlord's full-year rental income with CPF. The mutation (`POST /generate`) IS audit-logged via `log_action()` (good), but `GET /preview` and `/validate` are NOT audit-logged. Worse, role-gating is absent — DIMOB previews should be restricted to (accountant/admin/owner) since the document IS the fiscal disclosure.
  - *Mitigation*: (a) Role-gate the entire `/api/dimob/*` router to `("platform_admin", "owner", "admin", "contador")` via `Depends(require_role(...))` — the bound factory exists at `app/dependencies.py:121`. (b) Audit-log read accesses on `/preview` + `/validate` (in addition to the existing `/generate` log). (c) Retention: DIMOB records have a 5y RFB retention floor; confirm no orphan preview cache. (d) Confirm the generated DIMOB XML/TXT file does NOT get persisted to storage without an explicit retention policy + access-log; if it does, surface a follow-up.
  - *Flagged*: 2026-05-20
- [x] ~~**ERP Financeiro CRUD (`/api/financeiro`) writes/reads `erp.lancamentos` rows that record receita/despesa values tied to contratos/clientes — financial data of identified natural and legal persons (Art. 5 PII: counterparty identity surfaced via the joined contract/client; Art. 7 financial-data category). The router ships unrestricted to any authenticated user via `Depends(get_current_user)` (NOT role-gated), so any team member can list/create/edit/delete financial entries for the whole org.**~~ *Resolved 2026-05-20 by erp-financial-surfaces-role-gate* — read endpoints in `app/routers/financeiro.py` now use `Depends(require_role("platform_admin", "owner", "admin", "manager"))`; aggregate reads (`/resumo`, `/fluxo-caixa`) write `log_action("ler", "financeiro_resumo"|"financeiro_fluxo_caixa", …)`; gate contract pinned at `tests/routers/test_financeiro_role_gate.py`. NOTE: write endpoints (POST/PATCH/DELETE) still use `Depends(get_current_user)` — these are already audit-logged via `log_action`; write-side role-gating is captured in the project's §11 as a phase-1 follow-up improvement. (Original entry at) `products/erp-imobiliario/backend/app/routers/financeiro.py` — Phase 7 audit (erp-wiring): financeiro is one of the four highest-LGPD-sensitivity surfaces in ERP. Mutations are audit-logged via `log_action()` (good), but the read endpoints (`GET /api/financeiro`, `/resumo`, `/fluxo-caixa`, `/{id}`) are NOT audit-logged and NOT role-gated — every authenticated user can read every org lancamento. This is a confidentiality/minimization concern under Art. 6.
  - *Mitigation*: (a) Role-gate financial-data reads to `("platform_admin", "owner", "admin", "manager")` using `Depends(require_role(...))` (the bound factory at `app/dependencies.py:121` already exists; same shape as `metas_digest`). (b) Audit-log read accesses on `/resumo` + `/fluxo-caixa` (aggregate financial views) at minimum — these are the most-likely-to-be-exfiltrated shapes. (c) Document retention policy for `lancamentos`: typical accounting retention is 5y in BR (CTN Art. 174 + LGPD Art. 16 §1); confirm soft-vs-hard delete behavior. (d) Consider response-shape minimization: the list endpoint returns full row including counterparty PII; admin-level views could return totals only.
  - *Flagged*: 2026-05-20
- [ ] **Rejection reason free-text columns may contain applicant PII / sensitive data** at `products/therapy-platform/backend/migrations/013_rejection_audit.sql` — Migration 013 adds `rejection_reason TEXT` to therapy.therapist_profiles + therapy.clinics. The reason is admin-authored free-text describing why a therapist or clinic application was rejected — may quote applicant CRP/CNPJ details, applicant statements, or LGPD Art. 11 sensitive context (mental-health credential issues, etc.). Per Phase 5 §7 Q5 (2026-05-03), the user-decided retention policy is 90 days post-rejection followed by null-out via a scheduled job. The scheduled job is NOT YET BUILT — recording the LGPD concern here so the retention sweep is tracked separately. `rejected_at` + `rejected_by` retention follows the same 90-day window for symmetry.
  - *Mitigation*: Build a scheduled retention job that nulls out `rejection_reason` (and optionally `rejected_at`/`rejected_by`) on rows where `rejected_at < now() - INTERVAL '90 days'`. Cron candidate: daily 02:00 UTC. Until that job ships, admins must manually purge stale rejection reasons. The audit chain lives in application logs regardless.
  - *Flagged*: 2026-05-11
- [x] ~~**imobi-scheduling.oauth_credentials stores plaintext Google OAuth refresh + access tokens for org-bound Google Calendar access. Refresh tokens are long-lived; a leak grants persistent calendar access until the user revokes consent. The DB column is marked `SHOULD be encrypted at rest by the application before INSERT` (migration 001 comment), but Phase 8 does NOT yet implement the application-side encryption (folded from sibling's `google-oauth-activation` runbook but encryption deferred). LGPD Art. 6 Princípio II + Art. 46 obrigatório de segurança requires technical measures proportional to the data's sensitivity — refresh tokens are a credential, not "regular" PII.** at `products/imobi-scheduling/backend/app/services/calendar.py:SupabaseCalendarCredentialResolver`~~ — **RESOLVED 2026-05-16 by `social-wiring-absorption` Wave 2.** `imobi-scheduling` was consolidated into `products/social-wiring` and retired in Wave 4. The absorbed scheduling code does **NOT** carry the plaintext vulnerability forward: `products/social-wiring/backend/migrations/001_social-wiring.sql:84-93` defines `social_wiring.credentials.encrypted_tokens TEXT NOT NULL` with the explicit invariant *"Plaintext never lives in the DB; the encryption key lives in .env (ENCRYPTION_KEY) so DB compromise alone cannot recover the refresh token"* (Fernet-encrypted JSON), and `products/social-wiring/backend/app/services/calendar/oauth_adapter.py` + `.../calendar/__init__.py` consume the Fernet-encrypted `app.services.credential_store.CredentialStore` instead of a plaintext-token resolver. The Wave-2 credential_store bridge IS the at-rest-encryption mitigation the original *Mitigation* block called for. Re-verified from migration + adapter source (unambiguous → `noctus.dev.lgpd_flag` not required). Durable record: `projects/social-wiring-absorption/.integration-holding/W4.0-preservation/README.md` + ledger slug `social-wiring-absorption-wave4-teardown`.
  - *Mitigation (historical, now satisfied)*: Short-term: enforce DB-level encryption-at-rest at the Supabase project (managed-Postgres TDE covers data at rest at the disk level — does NOT defend against a compromised service-role key reading rows). Mid-term: file `seed/lib crypto envelope helper for sensitive columns` project so the resolver can encrypt/decrypt refresh_token transparently. Long-term: per-org KMS keys + per-row envelope encryption. — *Satisfied by the absorbed `CredentialStore` Fernet envelope (social-wiring); the "mid-term" mitigation effectively landed via the consolidation.*
  - *Flagged*: 2026-05-11 · *Resolved*: 2026-05-16 (social-wiring-absorption Wave 4)
- [ ] **Admin support conversations endpoint surfaces "other-party" identity (patient or therapist user_id+name+email resolved via Phase 1 bulk identity) joined with last_message_preview and unread_count. The aggregation reveals which patients have open support tickets — a proxy signal for service-quality complaints that may be Art. 11 sensitive (mental-health platform context).** at `products/therapy-platform/backend/app/services/admin_service.py:list_support_conversations_for_admin` — Phase 3 Pattern-B add: support conversation listing for admin was 404 before Phase 3. Bulk other-party identity + preview text concentrate Art. 5 + Art. 6 minimization concerns. PROJECT.md §6 Phase 4 sub-task 6 mandates the flag.
  - *Mitigation*: (a) Audit-log every admin support fetch (action_log) with admin_id + filter; (b) consider truncating preview to <=60 chars in list mode; (c) document retention for support-conversation rows once tickets close (LGPD Art. 6 minimization).
  - *Flagged*: 2026-05-11
- [ ] **Admin-side patient list joins therapy.patient_profiles (Art. 5 PII: nome, current_therapist_id, clinic_id) with bulk session counts (a derived datum revealing engagement frequency — proxy for treatment intensity, hence Art. 11 sensitive-data-linked). The shape concentrates per-patient session counts in one admin view — outside the therapist-patient bond that normally gates this information.** at `products/therapy-platform/backend/app/services/admin_service.py:list_patients_for_admin + _resolve_session_counts` — Phase 3 Pattern-B add: session-count aggregation is a novel admin-visible derived field (prior admin views showed only patient profile fields). PROJECT.md §6 Phase 4 sub-task 6 mandates the flag. Admin role + service_role read; legitimate-interest basis (platform oversight, fraud detection). Concentrating session counts at admin level needs explicit retention + access-audit policy.
  - *Mitigation*: (a) Action_log write per fetch with admin_id + filter params; (b) document business-need justification in MASTER-PROMPT.md (legitimate interest under Art. 7-IX — platform integrity / commission verification); (c) consider replacing absolute counts with "active in last 90 days yes/no" if exact counts aren't needed for the admin workflow.
  - *Flagged*: 2026-05-11
- [ ] **Admin flagged-reviews endpoint reads BOTH therapy.reviews and therapy.clinic_reviews, stable-sorted across the union, and surfaces review_text (free-form patient text up to ~500 chars). Patient identities (display name, email-resolved-via-Phase-1) are also resolved bulk. Review text may contain Art. 11 sensitive content (the patient describing a mental-health experience with the therapist) — that's by design (the reason it's flagged), but the admin view concentrates pre-moderation sensitive text behind one HTTP call.** at `products/therapy-platform/backend/app/services/admin_service.py:list_flagged_reviews_for_admin` — Phase 3 Pattern-B add: prior to Phase 3, admin couldn't access flagged reviews at all (404). The aggregation shape is novel; PROJECT.md §6 Phase 4 sub-task 6 mandates the flag for "endpoints that aggregate personal data in novel shapes". Admin role-gated, service_role read — but the data category (Art. 11 sensitive + Art. 5 PII linkage via identity resolver) raises the bar above standard admin lists.
  - *Mitigation*: (a) Audit log entry on every fetch (action_log write); (b) review-moderation outcomes (dismiss/hide) already log to action_log per Phase 3 — verify; (c) confirm review_text not in INFO-level structured logs; (d) document retention for hidden-review action_log entries (Art. 11 carries 5-year health-data minimum).
  - *Flagged*: 2026-05-11
- [ ] **Admin report listing aggregates message previews (first 200 chars of reported messages) joined with reporter/reported user identities resolved via Phase 1 supabase_identity bulk resolver. Single GET /api/admin/reports response can surface dozens of message-body excerpts across patient/therapist/clinic conversations — concentrating LGPD Art. 5 personal data and possibly Art. 11 sensitive data (mental-health context, crisis-alert content) in one admin view.** at `products/therapy-platform/backend/app/services/admin_service.py:list_reports_for_admin + _resolve_message_previews` — Phase 3 added bulk-message-preview resolution that surfaces partial message bodies in the admin reports endpoint. Phase 4 LGPD pass per PROJECT.md §6 Phase 4 sub-task 6 ("noctus.dev.lgpd_flag calls on the new endpoints that aggregate personal data in novel shapes"). The endpoint is admin-only (service_role + _require_admin), but the AGGREGATION SHAPE is novel: prior to Phase 3, admin saw reports without inline message previews; consumers fetched the full conversation as a separate call. Concentrating preview text on the list shape is a measurable Art. 6 minimization regression.
  - *Mitigation*: (a) Confirm admin-action audit log records the access; (b) consider redacting preview to <=80 chars + clear "open conversation for full context" CTA in frontend; (c) document retention policy for action_log entries surfacing message bodies; (d) verify no preview text is logged at INFO level (only ERROR with redaction).
  - *Flagged*: 2026-05-11
- [ ] **Invoices contain CNPJ + financial totals + Stripe payment IDs + NF-e XML (which embeds distributor name, address, fiscal data)** at `products/adconnect/backend/app/services/financial_service.py` — AdConnect Phase 5 — financial_service orchestrates fatura issuance with NF-e generation (Focus NFe REST API) and Stripe customer/invoice creation. Each fatura row stores: distributor CNPJ (via FK), valor_total, stripe_customer_id, stripe_invoice_id, stripe_payment_intent_id, nfe_xml (full signed XML embedding distributor address + items), nfe_chave (44-char NF-e access key). Egress destinations: Focus NFe (BR fiscal authority proxy) + Stripe (payment processor, US-hosted). Need: retention policy for nfe_xml (Brazilian law mandates 5-year retention), encryption-at-rest verification for stripe_*_id columns, LGPD legal-basis classification (legitimate interest for fiscal compliance vs contractual for payment processing), and confirmation that webhook handler payload is not logged at INFO level (currently only structured log + error level — verified clean).
  - *Mitigation*: Phase 5 ships the surface; LGPD review is a Phase 8 close-out task. Mitigations already in place: (1) RLS on adconnect.faturas restricts SELECT to row's distributor + brand admin in org; (2) webhook signature verification via Stripe SDK; (3) external SDK calls go through idempotency_key to prevent duplicate charges. Open: encryption-at-rest column policy on stripe_* + nfe_xml columns; data-residency disclosure for Focus NFe egress.
  - *Flagged*: 2026-05-10
- [ ] **Brand admin sees cross-distributor PII + financials (CNPJ, contact_email, contact_telefone, faturas) aggregated in a single role-gated surface.** at `products/adconnect/backend/app/services/admin_service.py` — Admin role surface aggregates per-distributor data. The brand admin role is a within-org cross-tenant aggregator: rows belong to multiple distributor "tenants" inside the same brand org. RLS on adconnect.distributors / faturas / recompensas_acumuladas allows brand-org admins to read all rows in their org, by design — but the surface concentrates the egress at one endpoint (GET /api/admin/dashboard, GET /api/admin/distributors, GET /api/admin/invoices). Retention and access-audit need explicit policy.
  - *Mitigation*: Phase 6 V1 ships role-gated endpoints (require_role admin/owner). V2 follow-up: audit-log every brand-admin read via the seed's audit-writer; retention policy on cached responses; consent-check at distributor-onboarding time captures whether the brand may aggregate that distributor's data on a dashboard.
  - *Flagged*: 2026-05-10
- [ ] **Distributor PII at registration (CNPJ, business address, contact name/email/phone)** at `products/adconnect/backend/app/routers/auth.py:accept_distributor_invite` — Phase 1 of adconnect-mvp-implementation lands the adconnect.distributors table. Brazilian CNPJs are personal data when the entity is a sole proprietor / MEI; addresses + contact details are PII. Retention rules + data-subject access rights need product-owner sign-off before V1 ships to a real customer.
  - *Mitigation*: RLS already restricts visibility (distributor users see only their own row; brand owner/admin sees all in their org). Service-role bypass kept for backend authorization. Phase 4-5 add NF-e + Stripe payment data as separate flag sites.
  - *Flagged*: 2026-05-10
- [ ] **Admin-only live Vista CRM showcase exposes personal data (lead/client names, contact details, possibly CPF/CNPJ, addresses) fetched live through ERP backend proxy routes; retention, access-log sink, and export policy are not yet locked.** at `products/erp-imobiliario/projects/vista-crm-wiring/` — Vista payloads contain personal data under LGPD. Phase 1 is read-only and admin-only, but the access-audit log location, the no-personal-cache contract, and the no-LLM-on-personal-fields rule are written into PROJECT.md §2/§3/§5 as commitments, not as shipped controls. This flag stands as the open item until (a) the backend proxy actually writes access-audit records, and (b) the retention contract (how long raw Vista responses may sit in memory/temp files) is documented in MASTER-PROMPT.md. Admin-only is the mitigation, not the exemption.
  - *Mitigation*: Admin-only route gating via existing ERP role system; frontend blind to the Vista API key; no DB cache or cross-schema copy in v1; no LLM summarization of personal fields in v1. Resolution gate: before Phase 3 flips to completed, both (a) access-audit sink must be shipped and tested, and (b) retention contract must be written into products/erp-imobiliario/MASTER-PROMPT.md.
  - *Flagged*: 2026-04-23
- [ ] **patient-attachment-to-llm** at `products/therapy-platform/backend/app/services/attachment_service.py:81` — Patient-uploaded files (images + audio) flow to OpenAI Vision + Whisper. Clinical documents, handwritten notes, voice recordings — all Art. 11 sensitive.
  - *Mitigation*: Prompt user for explicit consent at upload time. Offer 'no-AI' upload flow for sensitive attachments. Revisit Whisper self-hosting.
  - *Flagged*: 2026-04-19
- [ ] **patient-audio-to-whisper** at `products/therapy-platform/backend/app/services/transcription_service.py:45` — Raw patient audio segments are uploaded to OpenAI Whisper. Voice is Art. 5 biometric data. OpenAI retains audio per its API data policies. Unavoidable for transcription feature.
  - *Mitigation*: Evaluate a self-hosted Whisper deployment for clinical audio. Document upload + retention in user-facing privacy disclosure. Offer a consent toggle at recording time.
  - *Flagged*: 2026-04-19
- [ ] **longitudinal-clinical-aggregation** at `products/therapy-platform/backend/app/services/longitudinal_service.py:66` — Service aggregates multiple session summaries (6-month windows) into a single OpenAI prompt — higher re-identification risk than individual sessions because combined context reveals patterns unique to one patient (Art. 11 sensitive data, Art. 5 re-identification risk).
  - *Mitigation*: Consider summarizing per-session first and only passing compressed abstracts to the longitudinal pass. Or offer users a toggle to opt-out of longitudinal AI analysis entirely.
  - *Flagged*: 2026-04-19
- [ ] **patient-clinical-text-in-llm-prompt** at `products/therapy-platform/backend/app/services/summary_service.py:73` — Full session transcript + therapist observations (Art. 11 sensitive data) are embedded in every OpenAI chat prompt. Request bodies travel unencrypted to 3rd-party infra and are retained per OpenAI's data policies. We mitigate caching (cache=False), but the outbound request itself is unavoidable for the feature.
  - *Mitigation*: Longer-term: self-host a clinical-grade LLM, or pre-redact patient-identifiable tokens (names, phone, addresses) before the outbound call. Document the OpenAI data-retention contract in user-facing privacy policy (Art. 9 transparency duty).
  - *Flagged*: 2026-04-19
- [x] **llm-cache-key-cross-org-leak** at `seed/lib/backend/noctusai_lib/llm/cache.py::build_cache_key` — Cache key shape `llm:{product}:{provider}:{model}:{prompt_version}:{sha256(messages_json)}` did NOT include `org_id`. Two organizations with identical (product, provider, model, prompt_version, messages) would share a cache entry — cross-org PII bleed via cache hit. Low-probability collision but architecturally leaky. Surfaced by ai-expansion Tier 1.5 G5 audit 2026-04-24.
  - *Mitigation*: **RESOLVED 2026-04-24**. Added `org_id` parameter to `build_cache_key`; new key shape `llm:{product}:{provider}:{model}:{prompt_version}:org={org_id_or___platform__}:{sha256}`. `chat.py` passes `org_id=org_id` from the call site. Three regression tests in `mcp/noctusai/tests/test_llm_cache.py::TestKeyBuilder` (`test_org_id_isolates_cache_entries`, `test_org_id_none_uses_platform_segment`, `test_no_org_id_collides_with_platform_segment`). 14/14 cache tests green.
  - *Flagged*: 2026-04-24
  - *Resolved*: 2026-04-24
