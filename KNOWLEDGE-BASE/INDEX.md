# Knowledge Base — Index

> **Purpose:** This is the structural catalog of the NoctusAI Knowledge Base. Agents and developers use it to answer: *"where is X?"*.
> For *"what is this place?"* — read `AGENT-CONTEXT.md` instead (prose onboarding).
>
> **Sync rule:** Kept in sync with `CLAUDE.md`'s map section. If you add, rename, or delete a file in the KB, update both.

---

## Layout

```
KNOWLEDGE-BASE/
├── INDEX.md                ← this file (the catalog)
├── AGENT-CONTEXT.md        ← "what is this place" (onboarding prose)
├── CONTEXT/                ← deep technical + architectural context
│   ├── 01-PHILOSOPHY.md    ← engineering principles (elaborated)
│   ├── 02-LANDSCAPE.md     ← products, schemas, ports, stack
│   ├── 03-SEED-ARCHITECTURE.md  ← the spine
│   ├── 04-SHARED-LIBRARY.md     ← reusable components catalog
│   ├── 05-INFRASTRUCTURE.md     ← deployment + self-hosted services
│   ├── 06-AGENTS.md        ← MCP dev toolkit (Claude-side agents)
│   ├── 07-GAMIFICATION.md  ← cross-product UX philosophy
│   ├── PATTERNS/           ← how-to technical patterns (organized by ownership 2026-05-26)
│   │   ├── common/
│   │   │   ├── absorbed-product-seed-shape-seam.md
│   │   │   ├── absorption-ships-consume-docs.md
│   │   │   ├── absorption-tracking.md
│   │   │   ├── accept-with-rationale.md
│   │   │   ├── agent-context-architecture.md
│   │   │   ├── agent-reading-discipline.md
│   │   ├── architect/
│   │   │   ├── ast.md
│   │   │   ├── auth-boundary-false-green.md
│   │   │   ├── auto-author-scaffolds.md
│   │   │   ├── auto-generated-merge-drivers.md
│   │   │   ├── autonomous-operator-via-subagent.md
│   │   │   ├── backend.md
│   │   ├── backend/
│   │   │   ├── background-engineer-safety-discipline.md
│   │   │   ├── base-image-dep-freshness.md
│   │   │   ├── boundary-contract-tests.md
│   │   │   ├── branch-tree-tracking.md
│   │   │   ├── branching-and-merging.md
│   │   │   ├── branching-dispatch.md
│   │   │   ├── branching.md
│   │   │   ├── brief-similarity-radar.md
│   │   │   ├── build-learn-cache-mindset.md
│   │   │   ├── bypass-rationalization-anti-patterns.md
│   │   │   ├── cache-as-agent-tool.md
│   │   │   ├── cache-auto-freshness.md
│   │   │   ├── cache-deploy-mirror.md
│   │   │   ├── cache-family-index.md
│   │   │   ├── cache-locking-discipline.md
│   │   │   ├── cache-portable-architecture.md
│   │   │   ├── cache-telemetry.md
│   │   │   ├── chatbot-operational-readiness.md
│   │   │   ├── ci-deploy-prod.md
│   │   │   ├── ci-embedding-cache-gate.md
│   │   │   ├── ci-security-gates.md
│   │   │   ├── claude-md-router-discipline.md
│   │   │   ├── code-embeddings.md
│   │   │   ├── code-recurrence-baseline.md
│   │   │   ├── compliance-regression-baseline.md
│   │   ├── compliance/
│   │   │   ├── component-bundle-tool.md
│   │   │   ├── component-list-and-validation.md
│   │   │   ├── consent-routes-mandate.md
│   │   │   ├── container-sanitization.md
│   │   │   ├── containerization-operations.md
│   │   │   ├── containerization.md
│   │   │   ├── core-url-routing.md
│   │   │   ├── corpus-embeddings.md
│   │   │   ├── database-rls.md
│   │   │   ├── defer-is-not-resolve.md
│   │   │   ├── deploy-config-contract.md
│   │   │   ├── dev-main-ci-gates.md
│   │   │   ├── dev-prod-parity.md
│   │   │   ├── dev-fleet-dormant.md
│   │   │   ├── dev-team.md
│   │   │   ├── dev-toolkit-scaffolders.md
│   │   ├── devops/
│   │   │   ├── di-test-seam.md
│   │   │   ├── digest-seed.md
│   │   │   ├── dispatch-budget-telemetry.md
│   │   │   ├── dispatch-engineer-tuning.md
│   │   │   ├── dispatch-token-log.md
│   │   │   ├── dispatch-warmup.md
│   │   │   ├── dispatch-with-project-and-notes.md
│   │   │   ├── doc-discipline-family-index.md
│   │   │   ├── doc-symbology.md
│   │   │   ├── doc-to-code-drift.md
│   │   │   ├── dont-block-on-background.md
│   │   │   ├── drift-fix-on-contact.md
│   │   │   ├── eight-way-sync.md
│   │   │   ├── engineer-output-linter.md
│   │   │   ├── environment.md
│   │   │   ├── extractor-correctness-vs-mirror.md
│   │   │   ├── fe-be-contract-first-dispatch.md
│   │   │   ├── frontend.md
│   │   ├── frontend/
│   │   │   ├── funnel-self-satisfies-preconditions.md
│   │   │   ├── gate-methodology-sync.md
│   │   │   ├── git-branch-model.md
│   │   │   ├── harness-overlay-worktree-divergence.md
│   │   │   ├── inbox-chat-surface.md
│   │   │   ├── kb-recurrence-radar.md
│   │   │   ├── kb-vector-search.md
│   │   │   ├── keeper-check-before-docing.md
│   │   │   ├── keeper-pattern-cache.md
│   │   │   ├── knowledge-lifecycle-family-index.md
│   │   │   ├── learn-before-archive.md
│   │   │   ├── learning-posture-family-index.md
│   │   │   ├── lenses-applied-trailer.md
│   │   │   ├── lgpd.md
│   │   │   ├── llm-bot-security.md
│   │   │   ├── llm-tool-audit.md
│   │   │   ├── llm-usage.md
│   │   │   ├── logging-at-except.md
│   │   │   ├── logging.md
│   │   │   ├── lossless-doc-refactor.md
│   │   │   ├── lying-loading-state.md
│   │   │   ├── master-tree-parallel-batches.md
│   │   │   ├── mcp-first-scripts.md
│   │   │   ├── mcp-tool-conventions.md
│   │   │   ├── memory-embeddings.md
│   │   │   ├── memory-index-topic-split.md
│   │   │   ├── metas-seed.md
│   │   │   ├── methodology-codification-pipeline.md
│   │   │   ├── methodology-execution-discipline.md
│   │   │   ├── methodology-gc.md
│   │   │   ├── migrate-product-mcp-tool.md
│   │   │   ├── minimum-viable-rebuild.md
│   │   │   ├── noc-graph.md
│   │   │   ├── notifications.md
│   │   │   ├── orchestration-family-index.md
│   │   │   ├── orphan-branch-sweeper.md
│   │   │   ├── outbound-rate-limiting.md
│   │   │   ├── parallelization-first-orchestration.md
│   │   │   ├── persistent-files-absorption.md
│   │   │   ├── invitation-acceptance.md
│   │   │   ├── phased-push-policy.md
│   │   │   ├── postgrest-row-cap.md
│   │   │   ├── postgrest-schema-targeting.md
│   │   │   ├── prod-cache-container.md
│   │   │   ├── prod-deploy-safety-gates.md
│   │   │   ├── prod-exposure-consent.md
│   │   │   ├── product-centroid-drift.md
│   │   │   ├── product-dev-learning-ground.md
│   │   │   ├── product-icon-registry.md
│   │   │   ├── product-internal-wiring.md
│   │   │   ├── product-lockfile-and-slug-drift.md
│   │   │   ├── product-working-scope.md
│   │   │   ├── tunnel-ingress-source-of-truth.md
│   │   │   ├── products-consume-canonical-organs.md
│   │   │   ├── project-execution.md
│   │   │   ├── proposals-and-improvements.md
│   │   │   ├── push-time-embedding-gate.md
│   │   │   ├── pydantic-strict-http.md
│   │   │   ├── realtime-sse-bus.md
│   │   │   ├── remediation-markers.md
│   │   │   ├── repetitive-task-skill-codification.md
│   │   │   ├── roadmap-tracking.md
│   │   │   ├── scan-repetition-semantic.md
│   │   │   ├── scheduling-seed.md
│   │   │   ├── scoped-auto-improvement.md
│   │   ├── security/
│   │   │   ├── seed-absorption.md
│   │   │   ├── seed-canonical-defaults.md
│   │   │   ├── seed-fake-real-adapter.md
│   │   │   ├── startup-hook-must-not-be-fatal.md
│   │   │   ├── seed-lib-layout.md
│   │   │   ├── seed-organ-canonical-set.md
│   │   │   ├── seed-workspace.md
│   │   │   ├── self-branching-mode.md
│   │   │   ├── session-end-sweep.md
│   │   │   ├── shared-library-conventions.md
│   │   │   ├── silent-test-failure-from-missing-dep.md
│   │   │   ├── spa-cache-and-service-worker.md
│   │   │   └── ssh-deploy-key-restrictions.md
│   │   │   ├── status-pagina-dev-visibility.md
│   │   │   ├── storage-hygiene.md
│   │   │   ├── surface-and-resume-tooling.md
│   │   │   └── svg-render-mode.md
│   │   │   ├── tmp-artifact-cleanup.md
│   │   │   └── two-session-architect-operator.md
│   │   │   ├── unified-query.md
│   │   │   ├── vector-baseline.md
│   │   │   ├── vector-calibration.md
│   │   │   ├── vector-cost-tracking.md
│   │   │   ├── vectorize-embed-cache-framework.md
│   │   │   ├── verify-seed-on-fork-base.md
│   │   │   └── versioning.md
│   │   │   └── webhook-signatures.md
│   │   │   └── whatsapp-chatbot-seed.md
│   │   │   └── testing.md
│   ├── GUIDES/             ← task-oriented guides
│   │   ├── setup.md
│   │   ├── new-product.md
│   │   ├── seed-first-design.md
│   │   ├── deploy-workspace-online.md  ← "put X online" drill: verify docker artifacts → fill .env → docker compose up → verify; trigger phrases
│   │   ├── absorb-seed-workspace.md    ← absorb a separately-developed seed-workspace into noc as one product; 10 gates (snapshot → in-home → audit → interrogate → scaffold → full seed-reconcile → port+pilot-first → consumer-adapt → teardown → container-refactor → user-gated retirement); written from the proven social-wiring absorption 2026-05-16
│   │   ├── google-oauth-setup.md       ← Google Cloud Console OAuth client + Calendar API + redirect URI + env wiring; first adopter therapy-platform; reusable for any future product
│   │   ├── production-deploy.md         ← fleet-grade prod deploy to a VPS: git deploy-key → build-on-VPS runtime images → noctus-net → edge (Caddy-on-real-subdomains OR CF named tunnel) + volume-preserving PaaS (Coolify) decommission + LE-negative-cache/compose/cloudflared/OpenAI-LLM lessons; from the 2026-05-21 noctusai.com migration; self-contained
│   │   └── product-body-caching.md     ← caching a product's body (the recipe learned through the seed pilot): the 5-wave per-product procedure (Audit → Cleanup forks → Register emerging → Populate 8-field knowledge bundle → Manual-validation co-loop) + 7 lessons from the seed pilot + 5 common gotchas + recommended fleet order; composes seed-organ-canonical-set + build-learn-cache-mindset + vectorize-embed-cache-framework + cache-as-agent-tool; born 2026-05-29
│   ├── INTEGRATIONS/       ← per-vendor integration references (auth, endpoints, error model, adapter contract)
│   │   ├── oauth-patterns.md ← cross-provider OAuth reference — 5-layer model + 5 patterns + G1-G6 gotchas + Meta token-chain/auth-matrix + Google↔Meta scope-discovery diff + setup-guide template + what's already in seed vs the residual gaps
│   │   ├── meta.md         ← Meta (Facebook Pages + Instagram Graph) consume-side reference — `noctusai_lib.integrations.meta` `__all__` + factory auth-resolution (system_user → user_oauth → Fake) + consume recipe + read-only-v1 gaps
│   │   ├── whatsapp.md     ← WhatsApp connector consume-side reference — `noctusai_lib.integrations.whatsapp` `__all__` + WAHA vs Meta-Cloud-API backends + factory + webhook-router seam + lid-auth/dedup
│   │   ├── google.md       ← Google integrations consume-side reference — Calendar/Maps/YouTube/Drive/Gmail `__all__` + factories + resolver injection + quota-cost docs + Gmail OAuth-only send+read v1 + seed-ahead consumer status
│   │   ├── vista.md        ← Vista CRM REST API — public docs + live-probe results + adapter contract folded into one
│   │   ├── olx.md          ← Grupo OLX portal leads (ZAP/VivaReal/OLX/ImovelWeb/Casa Mineira) — inbound-only webhook, Basic auth, no sandbox, 14-day discard; payload contract + the one documented 4xx + tenant-resolution chain + endpoint inventory; TRANSCRIBED-NOT-VERIFIED until Gate 1
│   │   ├── mailchimp.md    ← Mailchimp Marketing API v3 consume-side reference — `noctusai_lib.integrations.mailchimp` Protocol/Fake/httpx-Real/factory + subscriber_hash upserts + static-segments-as-Listas + error-hierarchy import-never-redefine + per-org Fernet creds + cited social-wiring consumer
│   │   ├── image-gen.md    ← Image generation consume-side reference — `noctusai_lib.integrations.image_gen` `__all__` + `get_image_gen_adapter` factory + Fake/Gemini-Real adapters + renderer-agnostic Protocol + cited social-wiring consumer + backend-extension recipe
│   │   └── openai-mcp.md   ← OpenAI connector MCP (mcp/openai_mcp/) — 9 tools: embed / chat / vision / transcribe + search.{kb,code,memory,corpus} + diagnostics. Composes `_kit`. Reuses noctusai venv (no separate install). v4.0 ship.
│   ├── backend/            ← per-product backend details
│   │   ├── 01-CORE.md
│   │   ├── 02-ERP.md
│   │   ├── 03-PF.md
│   │   ├── 04-DATABASE.md
│   │   ├── 05-AI-FEATURES.md
│   │   ├── 06-THERAPY.md
│   │   ├── 07-AUTH-SECURITY.md
│   │   └── 08-DAILY-LIFE.md
│   ├── frontend/           ← per-product frontend details
│   │   ├── 01-CORE.md
│   │   ├── 02-ERP.md
│   │   ├── 03-PF.md
│   │   └── 04-THERAPY.md
│   ├── PRODUCTS/           ← per-product design docs (multi-feature slices that don't belong in a single INTEGRATIONS/backend doc)
│       ├── core/
│       │   └── SIGNUP-ONBOARDING-JOURNEY.md ← e2e signup→onboarding→org→noctus_users→JWT-claim→RLS chain; org_type (individual/company) + slug-prefix model + number_of_users; where each piece lives; migration 039; test coverage map
│       └── social-wiring/
│           └── INTEGRATIONS-MULTI-ACCOUNT.md ← multi-account integration credentials (integration_accounts table; seed-shape-vs-primitive-consume rule; YouTube OAuth v1; 3-step extension recipe)
│   └── ABSORPTIONS/        ← absorbed foreign-platform knowledge (faithful diagnostics + NEUTRAL comparison + bad-code salvage + seed-first uplift strategy); durable copy so the knowledge survives the source workspace's deletion (persistent-files-absorption)
│       └── orbity/         ← Orbity (BR marketing-agency SaaS: React+Supabase, 73 edge fns, 296 migrations) — diagnosed + compared 2026-06-02; consumed when reconstructing orbity-noc
│           ├── README.md                              ← index + provenance + how-to-read
│           ├── 00-ARCHITECTURE-OVERVIEW.md            ← stack, 3-tier tenancy, the 4 businesses an agency runs, scale
│           ├── 01-PROCEDURES-AND-WORKFLOWS.md         ← ⭐ every automated procedure step-by-step (lead→qual→CAPI, automation engine, closure, dunning, digest, onboarding, contracts, PPR)
│           ├── 02-DATA-MODEL.md                       ← 121 tables, agency_id tenancy, RLS (trusted-table, no user_metadata), DB triggers/cron
│           ├── 03-BACKEND-EDGE-FUNCTIONS.md           ← 73 Supabase edge functions by capability + _shared lib
│           ├── 04-FRONTEND-FEATURE-MAP.md             ← app shell, routing, RBAC, ~27 FE domains, data layer
│           ├── 05-INTEGRATIONS.md                     ← Meta/UAZAPI/GCal/Stripe/Asaas/Conexa/SendPulse/Resend/FCM/Slack/Discord/AI + secrets
│           ├── 06-DOMAIN-GLOSSARY.md                  ← pt-BR agency vocabulary → meaning
│           ├── 07-GAP-MAP-AND-NOC-MAPPING.md          ← gaps/risks + Orbity-domain → noc-fleet overlap (consume/extend/port/net-new)
│           ├── 08-CAPABILITY-COMPARISON.md            ← ⭐ NEUTRAL Orbity⟷noc across 16 capabilities + ranked "what we can learn" (both ways)
│           ├── 09-DEEPDIVE-FINANCIAL-MANAGEMENT.md    ← DRE, month-end closure, recurring/installment, PPR, dunning (the domain noc hasn't matured)
│           ├── 10-DEEPDIVE-META-ADS.md                ← CAPI quality-feedback loop, Lead-Ads ingestion, pixel, OAuth, spend/balance
│           ├── 11-DEEPDIVE-CHATBOT-AI-AUTOMATION.md   ← actual AI prompts, automation-flow defaults, scoring rules, message copy
│           ├── 12-ABSORPTION-STRATEGY-SEED-FIRST.md   ← ⭐ the seed-first uplift loop: capability→seed organ→pilots→orbity-noc; no-drift guards
│           └── 13-SALVAGE-EVALUATION-BAD-CODE.md      ← ⭐ the idea-behind-the-bad-code evaluation (SALVAGE-FIX / ABSORB-AS-IS / SKIP-noc-supersedes)
├── INSTRUCTIONS/           ← agent development / skill design
│   ├── 00-MASTER.md
│   ├── 01-SKILLS.md
│   ├── 02-MCP.md
│   ├── 03-AGENTIC-WORKFLOWS.md
│   ├── 04-DESIGN-PHASES.md
│   ├── 05-TESTING-EVALS.md
│   ├── 06-TECH-STACK.md
│   └── 07-TEMPLATES.md
├── EVALS/                  ← eval config + cases
├── SKILLS/                 ← skill definitions
├── WORKFLOWS/              ← multi-step workflows
└── MCP-SERVERS/            ← MCP server references
```

---

## By topic — "where do I find…"

| Topic | File |
|---|---|
| Engineering rules (seed-first, no quick fixes, DRY, etc.) | `CONTEXT/01-PHILOSOPHY.md` |
| Product landscape (names, ports, schemas, stack) | `CONTEXT/02-LANDSCAPE.md` |
| Seed framework APIs (`create_product_app`, `createProductApp`, etc.) | `CONTEXT/03-SEED-ARCHITECTURE.md` |
| Reusable components catalog (before writing anything) | `CONTEXT/04-SHARED-LIBRARY.md` |
| Infrastructure (ports, Docker, WAHA, LiveKit, n8n) | `CONTEXT/05-INFRASTRUCTURE.md` |
| MCP dev toolkit (heal loop, proposals, tools) | `CONTEXT/06-AGENTS.md` |
| Gamification philosophy (ranks, points, subtle UX) | `CONTEXT/07-GAMIFICATION.md` |
| Backend patterns (auth, SSO, RLS, N+1, services) | `CONTEXT/PATTERNS/backend/backend.md` |
| Frontend patterns (mobile-first, TanStack Query, hooks) | `CONTEXT/PATTERNS/frontend/frontend.md` |
| Inbox / chat surface — the canonical shape for ANY chat UI: consume the `ChatWindow` organ + a provider **adapter** (never a fork), **DB-first** reads (the vendor API is ingest+send only — vendor latency is latency you don't own; ~13s → ~10ms, and it survives a dead session), **one** realtime stream patching the query cache (never `refetchInterval`), and a read **cursor** (not an "inbound since my last reply" heuristic that silently degrades to 0). Born from the 2026-08 social-wiring realtime-inbox slice; documents why three forks shipped (a missing `.organ.yaml` made the catalog report the organ as shelfware, so `noc-organ-consume-check` told each engineer to build one — **a missing registration manufactures forks**), plus the non-obvious rules each of which cost something: `markRead` may reach the user's real phone so assert the call COUNT; `staleTime: Infinity` once a stream owns the cache; recount unread, never increment (vendors double-deliver); and an id-deduped write can never be repaired by a later echo | `CONTEXT/PATTERNS/frontend/inbox-chat-surface.md` |
| Testing discipline (3 layers, mocking, auth boundary) | `CONTEXT/PATTERNS/compliance/testing.md` |
| RLS + DB rules (`auth.uid()` subquery, search_path) | `CONTEXT/PATTERNS/backend/database-rls.md` |
| Invitation acceptance — accepting is THREE writes (identity → `public.noctus_users` membership → invitation status), and the status flip is the least important; a status-only accept returns 200 while leaving the invitee unable to open any product, because core's `/api/sso/launch/{slug}` reads the `noctus_users` row to mint the SSO token and 404s "Perfil não encontrado" without it. Membership is `noctus_users.org_id + org_role` (single-org FK, no join table); org context lands in three places (`noctus_users` = truth, `user_metadata.org_id` for the backend's `get_org_id`, `user_metadata.org_role` for the FE's `resolveSSOContext`). Non-obvious decisions: link an existing identity but NEVER reset its password (else invite = account takeover) · 409 rather than move a user already in another org (single-org FK ⇒ "joining" evicts them) · the invited email wins over the body (a leaked token must not enroll a different address) · `email_confirm: True` · `role` stays `"user"` · compensating delete of a just-created identity on partial failure, leaving the invitation pending. Also: mounting a standard router is a schema commitment (3 products mounted `team` with no invitations table) | `CONTEXT/PATTERNS/backend/invitation-acceptance.md` |
| PostgREST row cap — an unbounded select silently caps at 1 000 rows (no error); `.in_()` values ride in the URL query string, so an unbatched ~1 000-item collection is a bare 400 ("JSON could not be generated"); SEVEN instances shipped to prod in one session because `MockSupabaseClient.range()` was a no-op with no ceiling, making the exact behavior under test untestable by construction; the 3-layer fix (mock made honest + `_paginate`/`_select_all`/`_batched` recipe + heuristic observe-first keeper `check_postgrest_unbounded_query`) and why the keeper SPLITS severity by shape — select-shape `info` (~700 candidates, risk is a row count pure AST cannot see) vs `.in_()`-shape `warning` (~90 candidates, structurally risky regardless of table cardinality) — so the high-signal half is not diluted by the audit-worklist half; the paging loop itself is the seed helper `noctusai_lib.integrations.persistence.iter_paged_rows` (formalized 2026-08-17 at N=5, replacing the per-service `_paginate`/`_select_all` copies) — it owns the dedupe-on-`id_key` progress guard, so termination never depends on the backend honouring `range()`, judges a non-advancing pager by page size (over-long page ⇒ warn+stop, full repeated page ⇒ raise because "complete" and "capped prefix" are indistinguishable), raises on the page ceiling, and refuses to run when `id_key` was not selected rather than degrading the guard to a no-op | `CONTEXT/PATTERNS/backend/postgrest-row-cap.md` |
| PostgREST schema targeting — the client is ALREADY schema-bound, so `.table(name)` is schema-RELATIVE and a qualified name (`f"{schema}.invitations"`) resolves as `<schema>.<schema>.<table>` → 500 "Could not find the table ... in the schema cache" on every call, phrased so it reads as a missing migration; why MockSupabaseClient cannot catch it (keys tables by the string it is handed ⇒ qualified fixture agrees with qualified caller = fixture-vs-real false-green); the 3-layer fix (bare-name constant + `_require_bare_table` boundary refusal + keeper `check_postgrest_schema_qualified_table`, AST-based so prose does not self-flag); live incident 2026-08-06 seed team router / all 9 mounting products / green in CI ~3 months, plus the sibling arity bug (`cancel_invitation` dropped `org_id`, losing the org scoping as well as crashing) | `CONTEXT/PATTERNS/backend/postgrest-schema-targeting.md` |
| Environment vars (single `.env`, VITE_ prefix, CORS) | `CONTEXT/PATTERNS/devops/environment.md` |
| Notifications (`public.notifications`, field mapping) | `CONTEXT/PATTERNS/backend/notifications.md` |
| Shared-library conventions (privatize / absorb / rename; catalog tool) | `CONTEXT/PATTERNS/architect/shared-library-conventions.md` |
| Project execution (phase-header ticks, improvements block, improvements.md retrospective tool) | `CONTEXT/PATTERNS/architect/project-execution.md` |
| Product working scope (`ativo` + `deploy_scope` = which products we touch and where; inactive never live; reactivation lands in dev) | `CONTEXT/PATTERNS/architect/product-working-scope.md` |
| Proposals & improvements (two systems — per-project folders, ONE bundled proposal per phase, promote boundary) | `CONTEXT/PATTERNS/common/proposals-and-improvements.md` |
| LGPD awareness (keeper principle, the five questions, noctus.dev.lgpd_flag tool) | `CONTEXT/PATTERNS/security/lgpd.md` |
| LLM usage tracking (SupabaseUsageSink, /api/llm/usage, cost estimation, RLS scoping) | `CONTEXT/PATTERNS/backend/llm-usage.md` |
| Logging convention (when-to-log, level guide, no-`# silent-ok` rule, correlation IDs) | `CONTEXT/PATTERNS/backend/logging.md` |
| Seed-lib layout (6 layers — primitives/config/testing/integrations/domain/api — where to put new helpers, where to find existing ones) | `CONTEXT/PATTERNS/architect/seed-lib-layout.md` |
| Agent reading & research discipline (narrow-read first; Explore delegation rule forthcoming) | `CONTEXT/PATTERNS/common/agent-reading-discipline.md` |
| Webhook signature verification (the four shapes: Hub-Signature / hex HMAC / Svix / Stripe SDK; constant-time compare; helper module in `noctusai_lib.security.webhook_signatures`) | `CONTEXT/PATTERNS/security/webhook-signatures.md` |
| Accept-with-rationale catalog (durable home for every legitimate divergence on the platform — survives project folder deletion; how to add / retire entries) | `CONTEXT/PATTERNS/common/accept-with-rationale.md` |
| Outbound rate limiting — pace every third-party call + back off on throttle, via the shared `noctusai_lib.integrations.rate_limit` primitive (token-bucket pacing keyed per provider + exponential-backoff-with-jitter honoring `Retry-After`; sync `acquire`/`retry_with_backoff` AND async `acquire_async`/`retry_with_backoff_async`). Root-level: one primitive, every integration adopts it — never a per-integration reinvented backoff. Motivated by the Meta ads-backfill throttle (2026-07-23) + the CF-MCP burst before it. Wired: Meta (sync) + Mailchimp (async); Vista/WAHA/YouTube/Google carry `NOC-REMEDIATE[rate-limit]` markers. Distinct from the INBOUND `api.rate_limit_policies` (slowapi, protects our endpoints). Tests inject a `VirtualClock` (autouse in the seed conftest) so pacing logic runs without wall-clock waits. | `CONTEXT/PATTERNS/common/outbound-rate-limiting.md` |
| Remediation markers — greppable in-code deferral for batch evaluation: the `NOC-REMEDIATE[<class>]: <what + why> — <date>` token (distinct from the ~1.7k noisy `TODO`s); a sanctioned NON-silent deferral channel (the marker is the named destination, satisfying defer-with-destination); NOT for fix-on-contact-able bugs, NEVER on an `except` (no error-suppression — the retired `# silent-ok` shape); batch sweep `grep -rn NOC-REMEDIATE` ∨ by class; recurrence `N≥3` ⇒ promote to a project/seed lift; Stage-4 candidate `noctus.dev.scan_remediation_markers` | `CONTEXT/PATTERNS/common/remediation-markers.md` |
| AST-driven code edits (libcst for Python / ts-morph for TypeScript / tree-sitter cross-language; recipes for rename / find-callers / codemods; anti-patterns; boundary rule) | `CONTEXT/PATTERNS/common/ast.md` |
| Auto-generated content → git merge drivers (don't hand-resolve machine churn): two shapes → two drivers — append-only logs (`project-history/*.ndjson`) use built-in `merge=union`; derived blocks (`<!-- kb-counts -->` inventory tables in `02-LANDSCAPE.md`/`06-AGENTS.md`/`AGENT-CONTEXT.md`) use a custom `merge=kb-counts` driver (`scripts/hooks/merge-kb-counts.sh`) that RE-DERIVES the block from the merged tree + a marker-guard that still surfaces real prose conflicts; `merge=ours` rejected (drops concurrent prose); `.gitattributes` ↔ `install-hooks.sh` (registers the local-config driver) ↔ this doc stay in sync; codified 2026-06-05 after two file-disjoint social-wiring branches conflicted on a bumped inventory row | `CONTEXT/PATTERNS/common/auto-generated-merge-drivers.md` |
| component-bundle-tool — `noctus.dev.component_bundle` returns the 8-field organ bundle (source, types, tests, deps, consumers, wiring_snippet, validation_status, last_touched); AST-equivalent col-0-anchored type extraction; two fallback seams: W1 consumes_component edge (falls back to imports + warning) + W3 derive_validation_status (falls back to "unknown" + warning); CLI `--component-bundle <name>` | `CONTEXT/PATTERNS/architect/component-bundle-tool.md` |
| MCP tool conventions (3-segment dotted naming `noctus.dev.* / noctus.business.* / google.* / openai.*`, Pydantic In/Out per tool, hierarchical registration, lazy `NoctusContext` for business-logic tools, settings shim, MCP-first principle) | `CONTEXT/PATTERNS/architect/mcp-tool-conventions.md` |
| MCP-first scripts — `scripts/` specialization of MCP-first: new automation defaults to a `noctus.dev.*` MCP tool (+ `cli.py` flag + colocated `Test*`), NOT a `scripts/*.sh\|*.py` one-off; three named structural carve-outs (`[carve:hook]` git-hook entry → thin dispatcher · `[carve:bootstrap]` pre-venv · `[carve:docker]` thin docker-orch), each requiring an accept-with-rationale entry; classification manifest §3 = durable single source of truth, parsed by `check_new_script_lacks_mcp_analog` (asserts every top-level `scripts/*.{sh,py}` has a bucket row — undecided new script = warning) | `CONTEXT/PATTERNS/architect/mcp-first-scripts.md` |
| Seed workspace (sibling-of-noc consume-only workspace; symlinks all 8 noc surfaces; pre-commit hook + chmod + KB rule = three-layer "templates can't modify noc" defense; promotion manifest for additions; bootstrap script + workspace.py resolver + `noctus.dev.promote_from_seed_workspace` MCP tool) | `CONTEXT/PATTERNS/architect/seed-workspace.md` |
| LLM tool-call audit (`tool_call_audits` per-product table; `noctusai_lib.domain.ai.tool_audit::AuditRecord` + `make_audit_writer`; best-effort write; LGPD redaction at consumer; common BI queries) | `CONTEXT/PATTERNS/backend/llm-tool-audit.md` |
| LLM bot security (defense trio: output sanitization + Pydantic-arg validation + rate-limit; confirm-then-execute for destructive tools; prompt-injection mitigation via instruction sandboxing + allowlists; baseline checklist) | `CONTEXT/PATTERNS/security/llm-bot-security.md` |
| Digest service primitive (`noctusai_lib.domain.digest`: `BaseDigestService` template-method base + `DigestWindow`/`DigestResult` types; 4-adopter cluster — core/audit, daily-life/weekly-review, mailing/campaign-debrief, PF/monthly-narrative; non-fits: ERP/metas-digest + daily-life/daily-brief documented) | `CONTEXT/PATTERNS/backend/digest-seed.md` |
| Scheduling primitive (`noctusai_lib.domain.scheduling`: engine + `TravelLookup`/`Conflict`/`Scorer` Protocols + `ZeroTravelLookup`/`DefaultConflict`/`DefaultScorer` defaults; wiring recipe; what stays consumer-side) | `CONTEXT/PATTERNS/backend/scheduling-seed.md` |
| Metas / goals primitive (`noctusai_lib.domain.metas`: `Goal`/`Target`/`Progress`/`Period`/`Contribution` value objects, `GoalStatus`/`PeriodKind` enums, `compute_progress` / `accumulate_contribution` / `period_bounds` / `proportional_target` / `next_status` pure functions, `GoalRepository` Protocol seam; lifted from PF/ERP/daily-life N=3 MUST-FORMALIZE; wiring recipe + status mapping + what stays consumer-side) | `CONTEXT/PATTERNS/backend/metas-seed.md` |
| WhatsApp connector + chatbot framework (`noctusai_lib.integrations.whatsapp` WAHA parser/sender/router + `noctusai_lib.domain.chatbot` buffer/worker/dispatcher/summary + `noctusai_lib.integrations.{google_calendar,google_maps}` adapters; wiring recipe; debounce-race documented; what stays consumer-side) | `CONTEXT/PATTERNS/backend/whatsapp-chatbot-seed.md` |
| Realtime SSE bus — the platform's first realtime transport (`noctusai_lib.realtime`): `RealtimeBus` Protocol + `FakeRealtimeBus`/`RedisRealtimeBus`(Redis Streams, not bare pub/sub — resume actually works)/`get_realtime_bus` factory, + `create_sse_router` FastAPI factory (`text/event-stream`, `Cache-Control`/`Connection`/`X-Accel-Buffering: no` headers, `Last-Event-ID`/`?since=` resume, periodic heartbeat). Why SSE over WS/Supabase Realtime, the stream-vs-pub/sub resume contract, the proxy-buffering trap, and the `TestClient`/`ASGITransport` streaming-test constraint | `CONTEXT/PATTERNS/common/realtime-sse-bus.md` |
| Master-tree parallel batches (multi-product orchestrator: same-shape phases across N children execute as synchronized batches; live patterns log + absorption catalog as shared scratchpad; sync-gates pre/mid/post; divergent-batch carve-out; agent collaboration mechanics) | `CONTEXT/PATTERNS/architect/master-tree-parallel-batches.md` |
| Git branch model — current phase + methodology/prod-code separation roadmap (the WHY each ref exists, not the mechanical how): `dev`(+`feat/*`)=development isolation · `main`=blessed GLOBAL-repo-scope (methodology + prod code, one coherent state) · `prod`+`prod-backup`=production refs + rollback, TODAY carrying the same blessed content as EXTRA safety-net layers while the dev methodology is still hardening. Methodology-only changes get blessed dev→main(→prod) even though they never run in prod containers (no redeploy) — to preserve methodology+code as one recoverable blessed state; only PROD-CODE changes trigger the deploy_pull→deploy_image runtime rollout. TARGET (once methodology is "round"): separate — `prod`=prod-code-ONLY+backup, `main`=global scope, `dev`=development; trigger to flip = the extra prod safety-net no longer earns its keep. User-ratified 2026-06-01. Composes branching + branching-and-merging §0/§2b + fe-be-contract-first-dispatch + production-deploy §0.1 | `CONTEXT/PATTERNS/architect/git-branch-model.md` |
| FE↔BE contract-first dispatch (DEFAULT for connected BE/FE product-organ work) — author the FE↔BE contract (the shared skeleton: per-endpoint path/method/request-fields/response-shape `envelope-vs-bare` + EXACT field names/types/status-codes/auth) FIRST, then dispatch BE + FE slices against that ONE contract → both sides fit by construction, drift caught at authoring not runtime; born 2026-06-01 from the multi-account-integrations drift (FE built `res.data`/`id`, BE emitted bare/`id`-keyed → 0% functional, all unit-tests green); flow = extract-contract (verify field-by-field, don't assume) → embed verbatim in BOTH briefs → parallel dispatch → each side tests against the contract → ONE E2E-shape check closes the loop; composes with parallelization-first-orchestration + dispatch-with-project-and-notes + framework-consume-wire-and-apply-gap + products-consume-canonical-organs | `CONTEXT/PATTERNS/architect/fe-be-contract-first-dispatch.md` |
| Branching — the unified methodology (FRONT-DOOR for all branching/worktree/dispatch): the one primitive (isolate writes in a worktree off `origin/dev` → integrate clean → never switch a shared `HEAD`) · decision spine (read→dev · solo→self-branch · parallel→dispatch · multi-product→master-tree) · the worktree-sensitivity MAP (tools that read the working tree ⇒ phantom regressions on a busy checkout; verify on a clean worktree before chasing a red) · known-errors bump catalog (B1–B13) · self-improvement loop (bumps → catalog → eight-way sync → Stage-4); routes into self-branching-mode / branching-dispatch / branching-and-merging / master-tree-parallel-batches | `CONTEXT/PATTERNS/common/branching.md` |
| Branching and merging methodology — end-to-end git workflow (when to branch, how to branch from `origin/main`, push semantics — branch-to-branch + branch-tip-to-main fast-forward, naming convention, mental-model upgrade, anti-patterns; non-FF integration, multi-branch convergence, conflict resolution discipline, long-running branch maintenance, recovery from bad merges) | `CONTEXT/PATTERNS/architect/branching-and-merging.md` |
| Branching-dispatch — the parallel-agent operations RUNBOOK (sibling of branching-and-merging.md's reference): when-to-dispatch (trigger phrases + inline cutoff) · 3-branch model (main frozen / `feat/<project>` integration / `feat/<project>-<slice>` worker — DASH form, slash collides with the leaf integration ref) · 10-step protocol (decompose file-disjoint → isolated worktrees → dispatch-in-one-message → collect signal [branch·HEAD·files + `/tmp` patch overlay-divergence safety] → detect collisions: (a) path-overlap git-flags + (b) SEMANTIC-DUPLICATE different-paths-same-content architect-must-catch → merge `--no-ff` provenance → dedicated honest reconciliation commit keeping agents' commits → verify → cleanup → gate `main` at 100%) · Worker Contract paste-in · safety rules; absorbed from the knowledge-extractor sibling repo 2026-05-23, reconciled with noc's collision-class/wave/overlay-safety/dispatch_preflight additions | `CONTEXT/PATTERNS/architect/branching-dispatch.md` |
| Dispatch-engineer tuning — making background engineers FAST ∧ CHEAP (per-engine efficiency layer under branching-dispatch.md): a live audit measured a dispatched engineer booting at ~65k tokens before any work, most of it waste (a ~400-name deferred-tool list it never calls; root: `engineer-seed.md` shipped without a `tools:` allowlist ⇒ inherited all tools); levers — L1 scope `tools:` (`Bash,Read,Edit,Write,Grep,Glob,mcp__noctusai__*` — removes the deferred bloat, confirmed real token cut + least-privilege + no `Agent` ⇒ engineers can't dispatch) · L2 `model: sonnet` default + architect Opus-per-dispatch escalation (the wall-clock lever, reversible 1-line knob) · L3 worktree env pre-wire (`task_branch … wire_env=True`) · L4 scoped verification (engineer narrowest-check; full gate = architect's single integration run) · L5 tight briefs (exploration not model-speed dominates a loose dispatch) · L6 stale-worktree hygiene; repeatable measurement = read the engineer's first-message `usage` in its task JSONL; born 2026-05-25 | `CONTEXT/PATTERNS/architect/dispatch-engineer-tuning.md` |
| Dispatch-budget telemetry — per-dispatch token-consumed audit ledger (manual log path; auto-capture deferred until harness lifecycle hooks land); NDJSON schema `{ts, agent, slug, input_tokens, output_tokens, total_tokens, model, source_ref}`; orchestrator calls `noctus.dev.dispatch_budget_log` after each engineer dispatch reading token counts from task JSONL `usage` block; `dispatch_budget_report(window_days=7)` aggregates by `(agent, model)` with sum/avg/p95; `dispatch_budget_summary()` one-shot totals; CLI `--dispatch-budget-log/report/summary`; durable ledger at `project-history/dispatch-budget.ndjson`; composes with `vector-costs.ndjson` sibling pattern + `dispatch-engineer-tuning.md` (token data feeds tuning levers) + `auto-improvement.ndjson` shape; F11 of automation-orchestration-followup-2026-06 roadmap | `CONTEXT/PATTERNS/common/dispatch-budget-telemetry.md` |
| Branch-tree tracking — the global live map of git-tree × claude-tree (enforcement + observability layer of the unified branching methodology): append-only `project-history/branch-tree.ndjson` (tracked, pushed to dev, `*.ndjson merge=union`); a `branch-pointer` row mirrors BOTH coordinates joined 1:1 by `branch` (git: branch/base/commit/worktree · claude: role/agent/parent/session) + the `paths` collision-zone + `status`(on_going/shipped/blocked/canceled/stale/deferred) + `brief`(one-liner) + `notes`(durable lessons); lifecycle = orchestrator contextualizes-on-dev's-map BEFORE dispatch → engineer creates+pushes pointer BEFORE self-branching → updates+pushes on every commit + mid-flight (never skip); global read always via dev's copy; MCP `noctus.dev.branch_pointer` (append/update/query/list, `from_dev`+`push_dev` defaults); keeper `check_branch_tree_mirror` (pre-push HARD-BLOCK on missing/stale pointer or broken git↔claude mirror); purpose = see collisions before they happen even against unshipped work + catch leftovers; composes branching + branching-dispatch + dispatch-budget-telemetry + eight-way-sync | `CONTEXT/PATTERNS/architect/branch-tree-tracking.md` |
| Seed Fake+Real adapter pattern — canonical shape (Protocol + Fake + Real + factory) for IO-touching seed modules; gold-standard reference modules; exemption test for pure-logic/pure-crypto modules; backfill audit trail | `CONTEXT/PATTERNS/backend/seed-fake-real-adapter.md` |
| A product `lifespan_startup` hook must never abort the boot — the seed wraps it, logs at ERROR with traceback, and reports `startup_hook_error` on `/api/health` while still serving (degradation is a FIELD, not a status code); erp-imobiliario was dead 13 days on dev because one transient PostgREST TLS reset escaped a recovery sweep | `CONTEXT/PATTERNS/backend/startup-hook-must-not-be-fatal.md` |
| Product migration automation — `noctus.dev.migrate_product`; reads `products/<slug>/backend/migrations/NNN_*.sql` sorted by numeric prefix; tracks applied files in `<schema>.schema_migrations`; dry-run (list pending) or `confirm=True` (apply); idempotent re-runs; exec mechanism = Supabase Management API `POST /v1/projects/{ref}/database/query` (can run DDL); requires `SUPABASE_ACCESS_TOKEN` PAT; `SqlExecutor` Protocol + `FakeSqlExecutor` + `SupabaseMgmtExecutor` + `make_sql_executor` factory | `CONTEXT/PATTERNS/backend/migrate-product-mcp-tool.md` |
| SVG render mode — deterministic brand-locked slides (the media-creator absorption residual): the seed **`noctusai_lib.integrations.svg_render`** primitive (SVG→PNG via `resvg-py` — self-contained Rust wheel, NO system libs ⇒ slim-image-safe — + **bundled OFL fonts** Cormorant+Inter passed `skip_system_fonts=True` for dev↔prod-parity text; Protocol+Fake+Real+factory) **and** its first consumer the **`svg` render mode** in `social-wiring/media_creation` (`render_post(mode="svg")`: `DesignTokens` generic premium/educational presets + per-brand `mc_brand_kits.design_tokens` JSONB override + 4 token-driven role builders cover/develop/insight/cta, XML-escaped; `svg_markup` persisted, PNG as `data:` URL or via `upload_url_resolver`); forward migration `002`; slim-smoke-verified | `CONTEXT/PATTERNS/frontend/svg-render-mode.md` |
| SPA cache & service workers — never pin a client to a STALE bundle after deploy: the SPA shell (`index.html`) **and** any service-worker script (`sw.js`/`registerSW.js`/`workbox-*.js`) are served `Cache-Control: no-cache` (stable filenames = the only update paths; hashed `/assets/*` stay immutably cacheable) via `serve_spa`'s `_SPAStaticFiles` + `_is_service_worker`; **products do NOT ship a precaching PWA service worker** (bypasses HTTP caching → a stale worker pins clients to an OLD bundle: the 2026-07-08 erp-imobiliario/"Marina" incident — pre-rename "Certificados" label + an already-fixed form-freeze); `check_product_service_worker` keeper (high) flags an undeclared `VitePWA`, allows `selfDestroying: true` (retirement worker: `unregister()`+reload+cache-purge, self-heals stuck clients) or a `// noc-allow: service-worker` declaration; SW behaviour is a dev/vitest false-green (jsdom has no SW) → verify in prod shape | `CONTEXT/PATTERNS/frontend/spa-cache-and-service-worker.md` |
| Component list and validation signal — organ catalog discoverability (W3, `seed-organs-cache`); `noctus.dev.component_list` MCP tool + `--component-list` CLI; `noctusai_lib.components.validation_signal` derivation function (cross-product reusable); 5-gate formula: consumers≥3 ∧ has_test ∧ CI-green ∧ no-NOC-REMEDIATE ∧ no-recent-bugfixes; `shelfware=0-consumers` named explicitly (anti-pattern surface, NOT "broken"); `emerging`/`unknown` semantics; threshold constants `CONSUMERS_THRESHOLD=3` / `BUGFIX_WINDOW_DAYS=14`; `consumes_component`-edges preferred, `imports`-edge fallback with WARNING; in-process result cache keyed by `aggregate_source_sha`; derived-first principle (no manual catalog entries that rot) | `CONTEXT/PATTERNS/architect/component-list-and-validation.md` |
| Seed organ canonical set — Phase-1 body-layer catalog (W4, `seed-organs-cache`); 5 validated organs (LoginForm 9 consumers / ForgotPasswordPage 8 / AcceptInvitePage 8 / ResourceManager 3 with test / DigestCard 3); 4 shelfware organs named explicitly (PageSkeleton / LLMSpendBadge / FakeModeBadge / ErrorBoundary); organ.yaml sidecar format (8 knowledge fields per `build-learn-cache-mindset.md`); embedding into `code-embeddings.sqlite` with `chunk_kind='organ'`; `noctus.dev.find_reusable_component` intent-based query tool + `noctus.dev.register_organ` / `register_all_canonical_organs`; keyword fallback when embed provider unreachable; Phase-2 = social-wiring pilot | `CONTEXT/PATTERNS/architect/seed-organ-canonical-set.md` |
| Products consume canonical organs — hard rule (Phase 2.1, 2026-05-29): organ-shaped FE components in `products/<slug>/` MUST import from `@noctusai/lib/...`; no local re-implementations; named-seam extensions ALLOWED when declared (`// @consumes-organ <Name>@<ver> +seam=<kind>`); 4 design decisions baked (strictness/evolution/versioning/syntax); 4 anti-patterns; `check_canonical_organ_consumption` keeper (severity high); `noc-organ-consume-check` skill; Phase-2 audit finding: 95% already canonical on disk, 1 erp-imobiliario orphan | `CONTEXT/PATTERNS/architect/products-consume-canonical-organs.md` |
| Seed absorption methodology + tools — the **absorption + fusion + optimization trio** (cross-product / cross-tool / intra-file scopes): `noctus.seed.scan_repetition` / `list_capabilities` / `audit_drift` / `absorb_file` / `specify_capability` / `report` rollup / `scan_fusions` meta-detector / `scan_optimizations` intra-file detector + **`noctus.hound.scan`** trio orchestrator (keeper-analog for code hygiene); four absorption strategies (delete dead code / move-and-re-export / factory / template + runtime substitution); per-candidate loop (scan → evaluate → self-audit → absorb → re-scan → build-verify); safety rules; relation to DRY recurrence rule + delete_product symmetry | `CONTEXT/PATTERNS/architect/seed-absorption.md` |
| Containerization (multi-layer Docker: per-product `docker-compose.yml` + root orchestrator with `include:` + shared `noctus-net` fabric + per-product isolation networks; canonical Dockerfile/compose pattern at `products/seed/`; `./start.sh` Docker-default with `tunnel <slug>` cloudflare quick-tunnel mode for OAuth/webhook/demo testing; native legacy mode preserved; build process / layering / image footprint / network security model / troubleshooting / improvement backlog) | `CONTEXT/PATTERNS/devops/containerization.md` |
| Containerization OPERATIONS (runbook + methodology sibling to containerization.md's architecture: source-of-truth chain · operational primitives · growing codified-bumps catalog · diagnostic flowchart A-G · 8-step safe-change methodology · meta-arc) | `CONTEXT/PATTERNS/devops/containerization-operations.md` |
| Chatbot operational readiness — production-hardening checklist for LLM chatbot products: 6 pillars (retries on transient external writes via `retry_call` composing seed `RetryPolicy` / structured logs via seed `configure_logging` / health endpoint via `standard_routers=["health"]` / `DEPLOYMENT.md` shape / Supabase backups + critical-tables enumeration / metrics-sink seam with `NoopCounter` default); first adopter imobi-scheduling; therapy/mailing/PF inherit at N=2 | `CONTEXT/PATTERNS/backend/chatbot-operational-readiness.md` |
| CI-dispatchable prod deploy — `.github/workflows/deploy-prod.yml` (`workflow_dispatch`) fast-forwards the VPS checkout to `origin/prod`/`origin/main` over CI, so deploys work from environments that CANNOT make outbound `:22` connections (sandboxed agent, filtered network). Forced-command deploy key (`command="…/deploy/prod-pull.sh",no-pty,no-port-forwarding`) + ref-whitelist = the key can ONLY run the §2a safe-pull script (FF-only, backup tag+tar, refuse non-FF/overlap; never reset/checkout/clean/force). DORMANT until `NOCTUS_VPS_DEPLOY_PROD_KEY`/`NOCTUS_VPS_HOST` set (secret→env→step-output gate, never `secrets` in `if:`). Composes with `noctus.dev.release` (bless/promote stays the source-of-truth gate) + `noctus.dev.deploy_pull` (same §2a drill, local transport) + `embedding-cache-gate.yml` (the vetted VPS-SSH-from-CI pattern). Activation runbook + security rationale in the doc. Born 2026-06-30 (the agent-cannot-SSH egress class) | `CONTEXT/PATTERNS/devops/ci-deploy-prod.md` |
| CI embedding-cache-gate — Trigger T3 of `cache-backend-portability-2026-05` roadmap; `.github/workflows/embedding-cache-gate.yml` connects CI runners to the shared prod pgvector cache so only the first runner pays the embed cost; graceful-degrade posture (continue-on-error on all gates until PostgresCacheBackend + secret NOCTUS_CACHE_POSTGRES_DSN are provisioned); cost characteristic ~$0.10 × (CI_runs−1) savings per cycle; composes with `cache-auto-freshness.md` + `ci-security-gates.md` + sibling slice `prod-cache-container.md` | `CONTEXT/PATTERNS/devops/ci-embedding-cache-gate.md` |
| Cache-deploy-mirror — `noctus.dev.cache_deploy_mirror` snapshots local SQLite caches (Tier-1) → prod Postgres+pgvector (Tier-2) at deploy time, vectors transferred verbatim (no re-embed). Atomic-per-cache TRUNCATE+INSERT; now VERIFIES `rows_written == source_total` and refuses (rollback, `ok=False`, named delta) on any shortfall instead of reporting success. 🔴 2026-08-14 cache-mirror-join-drift: the 4 chunk-embedding caches each split a chunk's embedding across ONE of two sibling tables (`*_vec` sqlite-vec / `*_embeddings_json` fallback) depending on which environment (`sqlite-vec` is an UNPINNED, opportunistically-installed dep) last refreshed it; the mirror used to read only the json sibling, silently shipping 3-83% of each corpus while reporting `ok: true`. Fix reads BOTH sibling tables; `noctus.dev.repair_embedding_cache_drift` is the companion LOCAL-only, lossless (zero re-embed) repair for caches already split before the fix. Composes with `cache-portable-architecture.md` (the Tier-1/Tier-2 model) + `vectorize-embed-cache-framework.md` (the embed pipeline whose output this transports) + `prod-deploy-safety-gates.md` | `CONTEXT/PATTERNS/devops/cache-deploy-mirror.md` |
| Cache-portable architecture — TWO-TIER persistent cache spanning machines + worktrees: Tier-1 LOCAL fast path at `<git-common-dir>/noctusai/cache/*.sqlite` (one physical SQLite per cache, ALL worktrees of this repo consume it directly because git's common-dir is shared by every worktree — no copies, no symlinks; WAL handles concurrency); Tier-2 REMOTE authoritative at prod pgvector `noctus-cache-pg:5432` (machine-portable, survives clones). Sync directions: `cache_deploy_mirror` (local → remote, on deploy) + `cache_pull` (remote → local, on fresh-clone bootstrap, both reuse the per-cache schema map). **Auto-pull on empty** default ON: when `cache_path()` finds the local file missing AND remote is reachable, pulls verbatim (no OpenAI re-embed) — opt-out via `NOCTUS_DISABLE_AUTO_CACHE_PULL=1`. One-time legacy migration copies (not moves) `<worktree>/.claude/cache/*.sqlite` from any existing worktree into the new shared location; sentinel-gated; safe for old code still on `.claude/cache/`. 11 cache-path resolution sites consolidated through `cache_backend.cache_path()` / `cache_dir()`. Composes with `cache-auto-freshness.md` (refresh boundaries) + `cache-locking-discipline.md` (WAL) + `prod-cache-container.md` (Tier-2 home) + `prod-deploy-safety-gates.md` (covers cache_deploy_mirror). Born 2026-05-28 after a 50-min pre-commit stall on a fresh worktree exposed per-worktree `.claude/cache/` as the root cause | `CONTEXT/PATTERNS/common/cache-portable-architecture.md` |
| Vectorize → embed → cache (the unified pipeline, META-RULE) — the canonical 3-leg shape every vectorize/embed/cache slice follows: (1) **caching-architecture decision** FIRST — reuse an existing keeper-mirror cache by adding a new `chunk_kind` value (default = EXTEND, not SPAWN); only spawn a 9th sibling cache when the SQLite shape genuinely differs (e.g. sqlite-vec vs plain sqlite) AND the recurrence-rule receipts (N≥3 same-shape consumers) land first; storage path resolves via `cache_backend.cache_path()` (Tier-1 local + Tier-2 prod pgvector); (2) **embed via `noctusai_lib.integrations.llm.generate_embedding`** ONLY — never inline OpenAI calls (IO modules ship Protocol+Fake+Real+factory per [[seed-fake-real-adapter]]); cost-log every API roundtrip to `project-history/vector-costs.ndjson` via `noctus.dev.vector_costs_log_batch`; deterministic `source_sha = sha256(canonical_chunk_text)` gates idempotency (re-embed of same content = no-op, no cost); (3) **cache persistence with the 3-leg mirror contract** — eager pre-commit refresh + lazy query-time rebuild + loud freshness keeper (`check_<cache>_cache_freshness`); auto-refresh chain extends across pull/checkout/edit/push boundaries per [[cache-auto-freshness]]; WAL mode + `acquire_refresh_lock()` per [[cache-locking-discipline]]. **6+ existing instantiations prove the recurrence**: kb-embeddings + code-embeddings + memory-embeddings + corpus-embeddings (the 4 vector caches) + organ chunk (seed-organs-cache W4) + build-learn-cache re-embed (knowledge-on-append). Anti-patterns: inline OpenAI outside the seed lib · embed-without-caching (one-shot waste) · cache-without-re-embed-on-source-change (silent stale-routing) · spawn a 9th cache without the promotion ritual · skip Leg 1 ("just add the embed call") and ship a fork. Composes with cache-portable-architecture + cache-locking-discipline + cache-auto-freshness + keeper-pattern-cache + cache-as-agent-tool + seed-fake-real-adapter + vector-cost-tracking + methodology-codification-pipeline. Born 2026-05-29 from user-surfaced META-RULE | `CONTEXT/PATTERNS/common/vectorize-embed-cache-framework.md` |
| CI security gates — 3-layer PR security pass (Trivy fs + image dep scans, bandit Python SAST, gitleaks secrets scan); allowlist patterns at repo root `.trivyignore` / `bandit.yml` / `bandit-baseline.json` / `.gitleaks.toml`; canonical job shape (action pin + SARIF upload `if: always()` + exit-code gate); baseline-grandfathering for pre-existing findings; relationship to accept-with-rationale catalog | `CONTEXT/PATTERNS/devops/ci-security-gates.md` |
| The dev→main process — consistent, CI-enforced gates (the consolidated map) — branch model (`dev`=integration, `main`=sacred); the CI gates table (what runs when): `test.yml` (product backend/frontend/e2e + Trivy/bandit/gitleaks + the new `mcp-toolkit-tests` ~2.6k hermetic suite) + `seed-typecheck.yml` on push/PR to main,dev · `embedding-cache-gate.yml` on PR · `build-and-push.yml` on main push (deploy leg); the green requirement + bless→promote via `noctus.dev.release` (status/bless/promote); the principle — every suite + baseline is CI-gated ("is it green?" = CI's answer, not a human's; an unenforced gate is silent debt), baselines grow only by accept-with-rationale (a real issue surfaced by a refresh is FIXED not accepted), caches stay fresh by automation. Composes with branching-and-merging §0 + branching + ci-security-gates + ci-embedding-cache-gate + deploy-config-contract + cache-auto-freshness. Born 2026-05-31 (consolidation leg of the unenforced-gate lesson) | `CONTEXT/PATTERNS/devops/dev-main-ci-gates.md` |
| Funnel self-satisfies its preconditions — a shared funnel / single-entry helper (the one place all callers route through) MUST satisfy its own config/env preconditions, not document "caller MUST call setup() first." An offloaded precondition is a DRIFT GENERATOR: it works for the one caller who remembers (usually the CLI, which set the precedent) and silently breaks every other path (MCP server, hook, test, fresh import). Provenance: the recurring "LLM not configured" drift (2026-05-30) — the embed funnel `_embedding_corpus.embed_sync` documented "caller MUST ensure configure_llm() was called", only the CLI did, so every MCP-server embed path (`refresh_memory_embeddings` / `kb_embeddings_refresh` / `code_embeddings_refresh` / `find_reusable_component`) raised it. Fix: `embed_sync` calls `ensure_llm_configured()` itself (single bootstrap `_llm_bootstrap.py`: idempotent, auto-sources `.env` from repo-root + primary-worktree since the MCP server launches without it, graceful-degrades to False); the CLI's `_ensure_llm_configured` delegates to the same bootstrap (DRY). The general rule (not just LLM): a docstring saying "Caller MUST …" for a config/bootstrap step is the smell — convert it to a self-call; verify on the path that BREAKS (MCP server), not the one that works (CLI). Regression: `test_llm_bootstrap.py::TestEmbedSyncSelfConfigures`. Composes with vectorize-embed-cache-framework (the embed funnel) + 01-PHILOSOPHY (no silent errors · fix-at-root). Born 2026-05-30 | `CONTEXT/PATTERNS/common/funnel-self-satisfies-preconditions.md` |
| Cache-as-agent-tool — converts the cache layer from passive storage to ACTIVE search engine; the headline rule: when researching ANYTHING (design / methodology / code / history / structure) REACH FOR A CACHE BEFORE grep / Read. Two flavors: SEMANTIC (vectors via `kb_search` / `code_search` / `memory_search` / `corpus_search`) + STRUCTURAL (graph via `noctus.graph.*` over 23k+ nodes / 38k+ edges). The 8 caches at-a-glance: kb-embeddings (KB patterns) / code-embeddings (AST symbols) / memory-embeddings (~290 docs) / corpus-embeddings (router CLAUDE.md + topic CLAUDE/*.md + orientation CONTEXTUALIZE.md + command .claude/commands + agent + skill + template + changelog + history) / noc-graph (whole-platform structural) / keeper-patterns (compliance rules) / agent-context (specialist bundles) / auto-improvement (drift ledger). Cost model: cache hit ~100ms; grep is fast but lexical-only; whole-file Read burns context. Anti-patterns: grep before search · re-reading a pattern doc when search returns the answer · re-asking the user what memory already knows. `grep` / `Read` are CONFIRMATION tools, not DISCOVERY tools — search to discover, grep/read to verify. Composes with cache-portable-architecture (storage layer) + cache-auto-freshness (refresh contract) + cache-locking-discipline (WAL) + noc-graph (structural lens) + kb-vector-search (markdown canonical, vectors enrichment) + agent-context-architecture (specialist L1 + cache depth). Born 2026-05-28 after user surfaced: "the caches exist, future agents should USE them to help their work, not just refresh them" | `CONTEXT/PATTERNS/common/cache-as-agent-tool.md` |
| Storage hygiene — the **`mole`** (3rd member of the keeper/hound/mole trio: regulatory/curatorial/custodial); three orthogonal scopes (**artifacts** regenerable caches/builds / **environments** venv+node_modules duplication — advisory-only / **worktrees** stale `.claude/worktrees/agent-*/`); single entry point `python mcp/noctusai/cli.py --mole scan`; destructive `sweep --force` (dry-run default); active triggers (pre-dispatch + pre-commit + bootstrap); safety constraints (never deletes uncommitted/main/sibling/unmerged/`.env`/migrations); severity grading; bash-3.x compat mandate (the `mapfile` silent-no-op gap that motivated the rule) | `CONTEXT/PATTERNS/common/storage-hygiene.md` |
| Learn-before-archive — mandatory pre-delete salvage gate for ANY artifact (branch / worktree / file / project / temp); asks "what would be LOST?" before any destructive op; five categories to preserve: code/diffs → `git format-patch` under `project-history/<archive>/` (skip when content patch-equiv on dev via git-cherry) · lessons/decisions → KB/memory · commands/runbooks → KB doc or `scripts/` (no dangling temp pointer) · pointers/refs → recovery pointer in `project-history/worktree-salvage.ndjson` (always write) · docs → absorb to canonical tree; enforcement ladder: (1) principle (KB doc + CLAUDE.md §1 rule) → (2) tool (`noctus.dev.salvage_before_delete(target, kind, dry_run=True)`) → (3) keeper (`check_dangling_remote_branches` flags unique-old remotes) → (4) strict keeper (deferred — needs scope + git-log pattern); scope: remote branches + worktrees + `projects/` + KB deletions; NOT regenerable artifacts (caches/builds/node_modules) or `.env`; born 2026-05-30 after 330 dangling remotes discovered; generalizes `persistent-files-absorption` + `salvage-before-delete` into ONE gate; composes with `storage-hygiene.md` + `tmp-artifact-cleanup.md` + `remote_branch_hygiene.py` (classifier + guarded deleter) + `session-end-sweep.md` (remote branch section, P3.4) | `CONTEXT/PATTERNS/common/learn-before-archive.md` |
| Tmp-artifact cleanup — automated sweep of retired engineer-dispatch patches under `/tmp/*.patch` (`noctus.dev.tmp_cleanup`); semantic retirement verdict (a) `landed-patch-id` via `git patch-id --stable` match on `origin/dev` (last 500 commits) → delete · (b) `aged-out` mtime ≥ `max_age_days` (default 14) AND no match → delete · (c) `malformed` parse-fail → delete · (d) `kept-fresh` otherwise; dry-run default; orchestrator-operator runs `dry_run=False` as standing end-of-tick step with one-line surface `Hygiene: <N> patches purged (<M> bytes freed); <K> kept-fresh.`; bounded scope (`/tmp/*.patch` only — NEVER `/tmp/claude-501/.../tasks/` harness state, NEVER worktree/repo files); composes with `dispatch-engineer-tuning.md` (the dispatch-tuning protocol patch-file-first is part of) + `storage-hygiene.md` (sibling — worktree/branch cleanup) + `scoped-auto-improvement.md` (standing-sweep shape); fail-soft on git unavailable (only age+malformed branches fire); fail-soft on permission-denied (captured in `delete_failed`, retried next tick) | `CONTEXT/PATTERNS/common/tmp-artifact-cleanup.md` |
| Autonomous operator via subagent — **Option D** (fusion of single-session continuity + specialized-subagent-per-tick): `ScheduleWakeup` fires → architect spawns `orchestrator-operator` subagent (defined at `.claude/agents/orchestrator-operator.md`) → subagent drains the `## Pending` queue in `.claude/dispatcher.md` (dispatch-engineer / validate-worktree / cherry-pick-and-push / archive-project) in **isolated context** → appends the `## Outbox` section audit log → returns single summary text → architect main context stays clean for user ideation. A vs B vs C vs D comparison; 8-step flow; adaptive cadence (15min idle / 5min dispatch-heavy); FF-merge-to-main remains architect-only; setup recipe; anti-patterns | `CONTEXT/PATTERNS/architect/autonomous-operator-via-subagent.md` |
| Two-session Architect/Operator pattern — split one Claude Code workspace into two concurrent sessions (A = Architect: conversation / planning / KB / memory writes / NO git; B = Operator: ALL git ops + engineer dispatch execution + hound/mole/verify sweeps); coordination via gitignored `.claude/dispatcher.md` (unified Inbox `## Pending`/`## Completed` + Outbox `## Outbox`); strict ownership maps (git / memory / KB / MCP); anti-patterns (concurrent git push, both editing outbox, operator surfacing to user, memory clobbers); setup recipe + smoke test + `/loop` variant + 5-question pilot-vs-defer rubric + Option-D (single-session subagent) trade-off table | `CONTEXT/PATTERNS/architect/two-session-architect-operator.md` |
| Methodology execution discipline — the BEHAVIORAL half of the prevention system (machinery catches drift after the fact; this is how agents behave so the net rarely fires). Three principles: (1) **close the loop** — "works"≠"done"; nothing dangles (branch integrated-or-salvaged · origin branch deleted post-integration · worktree removed · docs/caches synced · no orphan ref/pointer); whoever pushes a branch owns its cleanup; (2) **work within the grain** — know the gates before acting (CLAUDE.md word cap + pointer-only §1 · pre-commit keepers · change-gated pre-push · branch protection) so you execute cleanly instead of tripping them; a foreseeable trip is a planning miss; (3) **a fired gate is a learning event, never a bypass** — STOP → understand → fix-or-surface → codify if recurring; never `--no-verify`/fight-the-linter/blind-delete. The PROCESS axis of the complementary learning pair with `product-dev-learning-ground.md` (craft axis): neither alone is enough — perfect methodology + un-compounding craft stagnates; great craft + leaky process drifts; together the system improves on both axes every touch. Born 2026-05-30 after the 330-dangling-remote cleanup | `CONTEXT/PATTERNS/common/methodology-execution-discipline.md` |
| Product-dev is its own learning ground — the CRAFT axis complement to `methodology-execution-discipline.md`: same "friction is a learning event" mindset applied to how we BUILD products (development technique), not how we work (process). The methodology learning loop (s1→s4 pipeline) is mature; this names CRAFT improvement as a first-class compounding loop too. Rule: a product bug that recurs / an approach that failed / a pattern that proved out → (1) fix, (2) capture the technique lesson via `build-learn-cache` (sidecar/graph, 8 categories), (3) promote a recurrence (N≥2) into the dev-technique pattern library `KB § PATTERNS/backend\|frontend/*` — the craft analog of the methodology codification pipeline. Distinct destination: craft lessons → backend/frontend patterns; methodology lessons → common/architect + s1→s4. Standing user value, re-enforced 2026-05-30 as product dev resumes. Complementarity thesis: two flywheels, the org compounds only when both spin | `CONTEXT/PATTERNS/common/product-dev-learning-ground.md` |
| Methodology codification pipeline — the 4-stage path that turns conversation rules into deterministic keeper detectors (Stage 1 emerges → Stage 2 memory entry → Stage 3 KB pattern doc + CLAUDE.md pointer → Stage 4 `check_*` function with colocated test); names the keeper as the **codification layer** of the methodology rather than a regulatory silo; codification criteria (deterministic predicate + recurrence ≥3 + clear remediation); 5 worked examples walked through all 4 stages (LGPD-first, webhook 5-pin, status-code-assertion rule, slowapi-pep563 gotcha, hygiene-compliance in-flight); what legitimately stays at Stage 3 (judgment-dependent / context-dependent / methodology-in-pilot / aesthetic); how to promote a rule between stages; why naming the pipeline matters | `CONTEXT/PATTERNS/common/methodology-codification-pipeline.md` |
| Repetitive procedure → skill (N≥2 recurrence) — **DRY for procedures** (sibling of the code-DRY rule + the methodology-codification pipeline): when the same multi-step orient→act procedure runs a second time on the platform it becomes a skill candidate; at N≥3 it MUST be codified as a `.claude/skills/noc-<verb-noun>/SKILL.md` skill via the `skill-creator` skill; four-gate procedure qualifier (multi-step ∧ orient→act ∧ auto-triggerable-on-phrases ∧ generalizable-beyond-one-instance); recipe (recognize at N=2 announce-loudly → decide procedure-vs-incidental via the four gates → author via skill-creator → verify 8-way sync); 5 anti-patterns (skill-blooming = near-duplicate of existing skill — use `code_similar_to_text` / `noctus.graph.query kinds=["harness_skill"]` Leg-A discovery before authoring · skill-scope-creep = drift past trigger phrases → split-not-bloat · inline-only-convenience = gate-4-fails → findings.md not skill · procedure-without-trigger = description is "Use when…" not verbatim phrases · single-MCP-call-as-skill = document the tool better instead); worked example = the 2026-05-28 ship of `noc-wrap-up` + `noc-verify-seed` + `noc-triage` codified together (auto-improvement ndjson entry 2026-05-28T14:20:01.531684, same-commit s2→s3 compression sourced from pre-existing memory entries — overdue per this rule's threshold and the very pattern that motivated codifying the threshold); Stage 4 keeper `check_recent_skills_have_index_provenance` deferred (advisory-only, audits new noc-* dirs for `Born from N≥2 recurrence at <source>` provenance) | `CONTEXT/PATTERNS/common/repetitive-task-skill-codification.md` |
| Build-learn-cache mindset (not only during dev) — **body-DRY** (the third sibling closing the recurrence-codification triad with code-DRY + skill-DRY + methodology-codification-pipeline): every artifact we build or touch — component / page / functionality / integration / function / organ — accumulates KNOWLEDGE that gets CACHED alongside the artifact itself, in real-time AS WE BUILD (not at wrap-up), and continues to accumulate during refactor / bug-fix / integration / deploy. Eight knowledge categories (the schema): (1) `known_facts` load-bearing facts · (2) `errors_encountered` errors hit + resolutions (for the next builder to avoid) · (3) `drifts_surfaced` two-leg footer `drift-found:` entries indexed BACK to the artifact that triggered them · (4) `alternatives_considered` paths tried + rejection reason (so the next builder doesn't re-litigate) · (5) `manual_validation_log` user reports + agent manual probes (hybrid with automated tests — not noise, real signal) · (6) `integration_test_status` current pass/fail + last_run · (7) `e2e_test` path + last_run + result + triggers (every artifact ships AT LEAST one e2e path) · (8) `bugs_fixed_during_dev` paired with fixing commit (recursive — bugs found while populating the cache itself land here). Two storage shapes compose by lifespan: sidecar `<artifact>.knowledge.yaml` for stable artifacts (queryable by grep + `noctus.dev.unified_query`); noc-graph node fields + `project-history/build-knowledge.ndjson` event stream for emergent organs (queryable via `noctus.graph.query kinds=["artifact_knowledge"]`). The cache is **append-only across the artifact's lifetime** (refactor/bugfix/integration/deploy all append) — that's what makes it source-of-truth. Wrap-up becomes a **completeness check** ("did we capture all 8 categories for the artifacts we touched?"), not the capture activity itself. Anti-patterns: wrap-up-only capture (loses 80% of journey) · automated tests only (loses real manual-validation signal) · cache-artifact-lose-journey (alternatives + errors = 90% of dev cost) · knowledge in commit messages only (not queryable by intent) · per-session findings.md only (knowledge dies with worktree) · one-shot e2e with no status field. Worked example = the seed-organs-cache project active 2026-05-29 (the body-layer companion that materializes this rule's schema as a seed primitive); recursive proof = this codification's own journey IS this KB doc's own first knowledge-cache entry. Stage 4 keeper `check_artifact_has_knowledge_sidecar` deferred — lands after seed-organs-cache ships the body since the storage shape has to exist first. Composes with project-execution `findings absorption` (extends granularity from project-scoped + close-only to artifact-scoped + real-time) · repetitive-task-skill-codification (skill-DRY sibling) · methodology-codification-pipeline (s1→s4 cadence) · noc-graph (new `artifact_knowledge` node kind + `learned_from`/`surfaced_drift` edges) · scoped-auto-improvement (two-leg footer is the source stream for drifts_surfaced + bugs_fixed_during_dev) · persistent-files-absorption (same family, finer granularity) · cache-as-agent-tool (build-knowledge cache becomes a consultable cache) · dispatch-with-project-and-notes (dispatch brief carries the build-learn-cache obligation) | `CONTEXT/PATTERNS/common/build-learn-cache-mindset.md` |
| Dispatch with PROJECT — return with notes — the structural execution pattern: tech-lead writes PROJECT.md §4a Dispatch routing (slice→lens table + codification expectations per slice + routes-not-taken) BEFORE any dispatch; engineer/inline-lens reads PROJECT.md (not a bespoke brief), executes the slice, returns with a **delivery note** (`kind="delivery"` on `noctus.dev.file_proposal`); when alt route emerges mid-flight, engineer files a **surface note** (`kind="surface"`) and BLOCKS until tech-lead approves/rejects/adapts with rationale (durable trailer on the note); reuses existing `projects/<slug>/proposals/` infra (notes = concept layer over the proposal file format via the `kind` parameter — phase/surface/delivery — phase=legacy back-compat); composes with engineer-seed §3 short-form return + §7 two-leg footer (delivery note = the durable form) + scoped-auto-improvement + methodology-codification-pipeline §4a.2 + parallelization-first §inline-empersonate rule; born 2026-05-26 evening from user-surfaced "agents skip steps + get lost" pattern alongside the s4-without-s3 codification gap | `CONTEXT/PATTERNS/common/dispatch-with-project-and-notes.md` |
| Surface-and-resume tooling — ergonomic no-bypass round-trip for blocked agents; closes the friction gap that drove `--no-verify` rationalization (surface cost too high → bypass felt cheaper); 4 MCP tools: (1) `noctus.dev.surface_to_tech_lead` — agent calls when blocked, captures worktree state snapshot (diff bytes + dirty files + sha) + writes `.claude/dispatches/<wt-slug>/surface-<ts>.md` with YAML frontmatter + returns `SurfaceReceipt.exit_marker_msg` (agent prints verbatim as FINAL output); (2) `noctus.dev.list_pending_surfaces` — tech-lead read side, walks dispatches dir, filters pending-response, returns summaries (surface_id + worktree + reason + age); (3) `noctus.dev.respond_and_resume` — tech-lead writes response file + updates surface status=responded + returns `ResumeBundle` (original brief excerpt from ledger + surface + response + worktree_state_pointer + resume_preamble); (4) `noctus.dev.dispatch_resume_brief` — final leg, composes full brief text (preamble + original goal + surface + response + cache-first reflex + safety rules + worktree path) ready for `Agent()` dispatch; security: `surface_id` validated against path-traversal (alphanumeric + `-._@` only); file layout `.claude/dispatches/<wt-slug>/surface-<ts>.md` + `response-<surface_id>.md` in PRIMARY checkout (fleet-level view); anti-patterns: bypass / verbose proposal (≤200 lines) / empty rationale (rejected by tool) / dispatch_resume_brief before respond; composes with dispatch-with-project-and-notes (surface-note pattern) + dispatch-engineer-tuning (brief template safety section) + brief-similarity-radar (ledger query for original goal) + keeper-pattern-cache (cwd-drift worked example); born 2026-05-29 | `CONTEXT/PATTERNS/common/surface-and-resume-tooling.md` |
| Background-engineer safety discipline — auto-mode dispatched engineers MUST NOT bypass safety gates (`--no-verify`, `--force`, destructive ops) without per-action tech-lead authorization; wall hit ⇒ STOP + `noctus.dev.surface_to_tech_lead` (`kind="surface"`) + return blocked; N≥4 post-hoc catches codified; composes with `bypass-rationalization-anti-patterns.md` + `drift-fix-on-contact.md` Roles split | `CONTEXT/PATTERNS/common/background-engineer-safety-discipline.md` |
| Bypass-rationalization anti-patterns — closes the `--no-verify` commit loophole + catalogues the 5 typical rationalization shapes an auto-mode agent uses to talk itself into a bypass: (1) *"commit-only, not push"* (refuted: pre-commit hooks fire on commit NOT push, commit-no-verify is the LARGER bypass not the smaller one), (2) *"I'm fixing the broken tool"* (refuted: even tool-fixers surface; the broken tool is what the surface-note is for), (3) *"the hook is a false-positive / pre-existing drift"* (refuted: tech-lead decides false-positive vs real, not the engineer; surfacing a true false-positive still costs 90s + gets the durable record), (4) *"it's harmless / no functional impact"* (refuted: the rule is about role-split not impact; silent bypass breaks the safety-net-firing → learnings → methodology-evolution loop irreversibly), (5) *"I'll surface in scoped-improvement after the commit"* (refuted: surface-note IS the block mechanism; after-the-fact surfacing is missed-the-window); structural-wrongness section showing pre-commit hooks fire on commit NOT push so commit-no-verify skips strictly more guarantees than push-no-verify; inline §2.6 surface protocol (STOP → SURFACE kind="surface" → RETURN-blocked → WAIT) routing to `surface-and-resume-tooling.md` tool names; canonical worked example = `ea7514e7` build-learn-cache codification slip (2026-05-29) with full failure-mode walkthrough (the 8-way-sync HIGH on keeper-pattern-cache via documented harness-cwd-drift between primary REPO_ROOT compliance.py sha eb8c5c000eb4 and worktree's 6f302fa3f184 — agent rationalized "all other gates pass clean, surfacing as drift-found" then committed --no-verify) + 3 valid tech-lead resolutions the agent should have surfaced for (authorize-on-file ∨ refresh-keeper-cache-from-worktree ∨ re-scope-slice-to-fix-cwd-asymmetry); Stage 4 keeper `check_no_no_verify_in_recent_history` deferred (scans `git log -p` for `--no-verify` substring over last N=200 dev-tip commits, surfaces hits as scoped-improvement candidate; promoted at N≥2 post-codification drift); composes with `surface-and-resume-tooling.md` (the surface-note infra + tool names) + `drift-fix-on-contact.md` (Roles split applied to rationalization layer) + `scoped-auto-improvement.md` (explicitly NOT a substitute for safety-wall surface notes per anti-pattern 2.5) + `dispatch-engineer-tuning.md § 4c Mandatory brief language` (the brief-template clause baking "NEVER `--no-verify` commit OR push" into every dispatch) + `engineer-seed.md §9 Bash safety` (the standing protocol that IS the contract) + `01-PHILOSOPHY.md` (universal ancestor — rationalization-to-bypass IS the safety-net-firing-without-learning shape); born 2026-05-29 user-mandated after post-hoc detection of the ea7514e7 slip — *"close this loophole + the rationalization catalog so bg engineers don't do it again"* | `CONTEXT/PATTERNS/common/bypass-rationalization-anti-patterns.md` |
| Gate↔methodology sync — every gate ships WITH its compliance-by-construction mechanism (codified 2026-06-02 at N≥3). When you add a keeper that hard-blocks a platform standard, the SAME change ships the code path that makes compliance automatic — auto-fill/derive/normalize/write-both, refuse-loudly-not-null when it can't derive — so the normal way of producing the artifact IS the compliant way and the gate becomes a BACKSTOP (for out-of-band edits / legacy rows / foreign contexts), not the primary enforcement. Two halves of one contract; gate-without-mechanism is an incomplete commit. The authoring checklist: (1) write the gate (scan ALL artifacts incl. historical/terminal, hard-block, name the exact fix), (2) write the mechanism in the producing code path (refuse-not-null = no-silent-null corollary), (3) backfill existing artifacts, (4) ship the detector regression test, (5) 8-way-sync the rule same commit (CLAUDE.md §1 + KB + memory), (6) prove the loop (gate fires pre-fix → mechanism+backfill → gate passes). Worked N≥3: branch-tree mirror keeper + write-both; branch-tree session keeper + CLAUDE_CODE_SESSION_ID auto-fill (2026-06-02); detector-has-regression-test gate + scaffolded test; cache-freshness gate + heal-on-contact; seed Fake+Real+factory gate + scaffold. Anti-patterns: gate-only (drift recurs forever) · work-around-the-gate (`--no-verify` / monkeypatch-guard / weaken-assert / delete-row) · silent-null compliance · mechanism-only (no backstop). Composes with drift-fix-on-contact + methodology-execution-discipline + bypass-rationalization-anti-patterns + eight-way-sync + branch-tree-tracking | `CONTEXT/PATTERNS/common/gate-methodology-sync.md` |
| Doc symbology — lossless compression glossary defining a fixed-meaning symbol set (logic `∧ ∨ ¬ ⇒ ↔ ∈ ⊂ ≡ ≠ ≈`; status icons `✅ ⏳ ❌ 🔒 📋 🗑 ⭐ ⚠️`; codification stages `s1/s2/s3/s4`; triage outcomes `[F]/[R]/[A]`; counts `N≥3 N=2 Δ Σ`; prose-word abbreviations `DB/auth/config/req/res/fn/impl`); contract = lossless-swap test (reader with glossary recovers exact semantic of prose); caveman-skill provenance (`github.com/JuliusBrussee/caveman`, validated ~61-75% token cut, retrieved 2026-05-18) — adopts caveman prose-discipline layer (§6a) + lite/full/ultra intensity ladder (§3a.1), explicitly rejects caveman `→`=causal mapping (our `→`=routes is locked; divergence register in §1/§4); where to use (MASTER-PROMPTs / CLAUDE.md / KB patterns / PROJECT.md §6+§11) vs NOT (error messages / first-paragraph context / quoted user instructions / bug-fix code comments / commit messages); anti-patterns (stacking, inventing new symbols without adding here, symbol-loading low-traffic docs); reference patterns codify codification pipeline + 3-way triage + doc-code coherence + recurrence rule + status legend in 1-line forms | `CONTEXT/PATTERNS/common/doc-symbology.md` |
| Lossless doc-refactor — the standing framework for any change to the durable doc-set itself (CLAUDE.md / KB / MEMORY.md / `.claude/agents` / MASTER-PROMPTs); fires on "compact / token-optimize docs", "refactor doc/architecture structure", "merge/split rules", "fold in an external pattern"; prime directive = lossless **proven not asserted** via 5 diff gates (pointer-set / rule-section / line-structure / index-pointer / hook-grade — line-structure is a distinct gate per the 2026-05-18 newline-eaten-by-Edit lesson); gated-aggressiveness ladder (s-lossless no-approval · s-moderate show-diff-first · s-plan-only); source-of-truth trim rule (cut only what the pointer target provably carries; registers/catalogs/ledgers are data not prose); symbol-first caveman-aligned + `check_doc_symbology_drift` zero-drift enforcement; safe parallelization = strictly file-disjoint worktree off real-HEAD + architect true-disk 100%-certification before merge + inline-architect fallback if not disjoint; fix-on-contact during refactor; eight-way-sync close-out; the doc governs itself | `CONTEXT/PATTERNS/common/lossless-doc-refactor.md` |
| CLAUDE.md router discipline — CLAUDE.md is the always-on auto-loaded router; §1 carries PRINCIPLE + MAP (rule + one-clause why + `→` pointer, one line each), PROCEDURE lives in `.claude/skills/noc-*`, depth in `KB § …`; the v4.0 **synthesis** = moderate §1 (why-based, higher quality) + aggressive §2 (shortlist + `→ INDEX.md`, no duplicated roster → drift-free); 3 deterministic invariants (whole-file word budget ≤2500 / each §1 rule one-line carrying `→` ≤60 words / no §1 prose bodies) gated by Stage-4 keeper `check_claude_md_router` (registered in `check_all_products` + `--check-claude-md-router` pre-commit block; mirrors `check_doc_symbology_drift`; colocated `TestClaudeMdRouter`); three poles preserved in `backup/` (original/aggressive/moderate); born `harness-agents-skills` 2026-05-25 | `CONTEXT/PATTERNS/common/claude-md-router-discipline.md` |
| Methodology GC — retirement as a first-class leg; every capture pipeline needs an exhaust. A green gate on SHAPE does not bound ACCUMULATION: §1 grew 72→79 always-on rules while `check_claude_md_router` stayed green and the word cap was raised 2500→3500 to accommodate it. Two legs — `/codify` retires the promoted memory pointer by construction; `/gc` is the monthly backstop (measure budgets, retire strays, propose §1 family consolidation). §1 family consolidation = one family line + a family-index KB doc holding members VERBATIM, proven lossless (every original rule survives exactly once), never summarised. Budgets are RESTORED after a consolidation, not left ratcheted | `CONTEXT/PATTERNS/common/methodology-gc.md` |
| §1 family index — cache platform (6 members, verbatim): KB vector search · noc-graph · cache-locking · auto-freshness · portability · vectorize→embed→cache. Cache-first search + keeper-check stay standalone in §1 (per-turn behavioral defaults, not platform mechanics) | `CONTEXT/PATTERNS/common/cache-family-index.md` |
| §1 family index — orchestration & dispatch (9 members, verbatim): branching-first · parallelization-first · contract-first FE↔BE · don't-block-on-background · `task_branch` not Agent-isolation · inline-vs-dispatch cutoff · lenses trailer · wave/collision-class · dispatch-with-PROJECT. Git-workflow rules stay standalone | `CONTEXT/PATTERNS/common/orchestration-family-index.md` |
| §1 family index — knowledge lifecycle (5 members, verbatim): durable findings · scoped-auto-improvement · persistent-files absorption · learn-before-archive · roadmap tracking. The capture→absorb→archive chain; the learning-POSTURE family is about noticing, this one about what happens to what you noticed | `CONTEXT/PATTERNS/common/knowledge-lifecycle-family-index.md` |
| §1 family index — doc discipline (8 members, verbatim): doc-propagation sync · keeper-check-before-doc'ing · durable-surfaces-self-contained · symbol-first · lossless doc-refactor · CLAUDE.md router · MEMORY.md topic-router · 8-way sync. Editing any of them is methodology surgery on a surface that loads every session | `CONTEXT/PATTERNS/common/doc-discipline-family-index.md` |
| §1 family index — learning posture (4 members, verbatim): triage-at-decision-time · safety-nets-capture-failures · learn-from-friction-on-both-axes · always-hardening. The noticing-and-deciding loop | `CONTEXT/PATTERNS/common/learning-posture-family-index.md` |
| Memory index — router + per-topic files (THE default memory shape) — `MEMORY.md` is auto-loaded AND read-capped at ~24.4KB, past which the read returns NOTHING (silent recall loss, not truncation); the flat index crossed it at 24,799B / 414 pointers on 2026-07-30 and trimming could never fix it because the filenames alone exceeded the target; split into a ~4KB ROUTER of topics + `MEMORY-<topic>.md` files (one line per memory) + the memory file — same three-level shape as `CLAUDE.md → CLAUDE/<topic>.md → KB`; router grows by TOPIC not by memory; save appends to the topic file, NEVER to `MEMORY.md`; split must be mechanical + proven lossless (LOST/DANGLING/UNREACHABLE all empty); sibling of `claude-md-router-discipline.md` | `CONTEXT/PATTERNS/common/memory-index-topic-split.md` |
| Lenses-applied commit trailer — lightweight `Lenses: <name>[, <name>...]` convention recording which specialist lens(es) the architect inline-empersonated for a commit; valid lens names mirror `.claude/agents/` executor + advisor names + `tech-lead`; observability not enforcement (no keeper today, Stage 2 evaluation gated on N≥50 observed commits); composes with parallelization-first-orchestration `inline=empersonate` rule + Co-Authored-By trailer family; makes inline-empersonation auditable via `git log --grep "Lenses:"`; codified 2026-05-26 evening as the closure of the inline-empersonation lens observability gap | `CONTEXT/PATTERNS/common/lenses-applied-trailer.md` |
| Compliance regression-baseline gate — why `score==100` is aspirational not a regression detector; the Option-A re-spec (no NEW high/critical vs a committed fingerprint baseline + absolute score informational); line-churn-robust fingerprint; env-artifact exclusions; the deterministic `refresh_compliance_baseline.py` contract; where the 2 gate tests / fixture / regenerator live | `CONTEXT/PATTERNS/compliance/compliance-regression-baseline.md` |
| Auth-boundary false-green anti-pattern — `status_code in (401, 404)` disjunction in auth tests is a false-green escape hatch (route absent ⇒ 404 ⇒ test passes, auth never exercised); two failure classes (RED drift caught by test runner vs FALSE-GREEN caught only by static AST analysis); fix = strict `== 401` against a path the product actually serves; Stage-4 keeper `check_auth_boundary_false_green` (AST-first via `ast.Compare` + `ast.In` + integer constant walk; never silent; severity warning; fleet regression: dev-team clean after 9d6bf79c); canonical test runner for the paired RED-drift class | `CONTEXT/PATTERNS/compliance/auth-boundary-false-green.md` |
| DI-test seam — the named remediation convention for the `test_patch_target` / self-monkeypatch compliance class; the rule (no `patch`/`monkeypatch` of our own symbols) + the 3 legitimate seams (DI default · `MockRequestBuilder.inserted_payloads` read-side · external-boundary-only); routes to the full refactor playbook in `testing.md` | `CONTEXT/PATTERNS/backend/di-test-seam.md` |
| Logging-at-except — the named remediation convention for the silent-except compliance class; every `except` logs ∨ raises ∨ returns-error-bearing; no `# silent-ok` escape hatch (retired 2026-04-28); level guide; routes to the full when-to-log rationale in `logging.md` | `CONTEXT/PATTERNS/backend/logging-at-except.md` |
| Absorption tracking — the **9th keeper-mirror cache**: absorption *state* is a derived ndjson ledger (`project-history/absorptions.ndjson`) → sqlite cache (`<git-common-dir>/noctusai/cache/absorptions.sqlite`), NOT a hand-maintained INDEX row (closes the Gate-2 "derive, never hand-maintain a parallel count" rule for absorptions, which are N≥2: social-wiring + orbity); event-shaped (lifecycle `cloned→diagnosed→uplifting→porting→teardown→done` + per-capability `identified→building→pilot→fleet→native_doc→de_referenced`); MCP `noctus.dev.absorption_{log,status,query,refresh}`; STRUCTURAL auto-refresh (settle_structural_caches, zero-OpenAI) + keeper `check_absorptions_cache_freshness` (warning); the capability `status` chain IS the live uplift+de-reference ledger; composes keeper-pattern-cache + cache-portable-architecture + roadmap/branch-tree tracking + absorb-seed-workspace; born 2026-06-02 (orbity, user-flagged) | `CONTEXT/PATTERNS/common/absorption-tracking.md` |
| Absorption ships consume-docs (R1) — an absorption is not complete until its consume-side `KB § INTEGRATIONS/<x>.md` ships in the SAME project that lifts the code; doc carries what-ships / consume-recipe-via-named-seams / auth-modes / gaps; documentation sibling of "absorption = methodology-epoch merge — reconcile the derived surfaces"; evidence social-wiring-absorption lifted meta/whatsapp/google but shipped zero consume docs; completeness test + 4 anti-patterns; sibling of R5; s1→s3 with s4-keeper candidate noted | `CONTEXT/PATTERNS/common/absorption-ships-consume-docs.md` |
| Verify-the-seed-ships-it binds to the engineers' fork base (R2) — the architect's dispatch-time check runs `git ls-tree origin/main -- <path>` (the tree the worktree forks from), NOT `ls` in the architect's working tree; unmerged feature-branch lifts are invisible to engineers; the existence-in-some-tree vs existence-in-fork-base conflation; evidence META + DOCS-CONSUME both correctly STOPPED on stale origin/main (full re-dispatch wave cost); structurally cured by R4 (keep origin/main current); pairs with §16.7 preamble | `CONTEXT/PATTERNS/common/verify-seed-on-fork-base.md` |
| Phased-push policy for large commit backlogs (R4) — long-lived feature branches phase-merge to main at project/wave-close boundaries only when 100%-sure (touched-product builds ∧ pytest ∧ verify-kb-sync ∧ increment criteria); ≥1 closed-project unmerged is the phase-push signal; human-gated protocol (architect PRESENTS push cmd + range + evidence → user explicit per-increment go/no-go → architect executes; direct-to-main without presented+approved gate forbidden, harness-classifier-enforced); `scripts/merge-debt-monitor.sh` custodial sibling of disk-usage/mole/hound wired pre-dispatch + project-close; structural cure for the R2 stale-fork-base class; proven end-to-end A→B→C→D 2026-05-18 | `CONTEXT/PATTERNS/common/phased-push-policy.md` |
| Defer ≠ resolve (R5) — a follow-up project filed for work already inside the active project's explicit scope is a deferral slip, not a triage outcome; the recurrence/triage register + "file a follow-up" is for newly-discovered cross-cutting work, never de-scoping the active brief; scope test = "was this in the user's original explicit ask / stated scope?" (yes ⇒ resolve in-project); evidence gmail-seed-lift filed for Gmail though "google = …+gmail+…" was the original ask; sibling of R1 (both completeness ≠ deferral); legitimately stays s3 (scope test is judgment-dependent) | `CONTEXT/PATTERNS/common/defer-is-not-resolve.md` |
| Harness overlay ⊥ worktree divergence (R6) — the harness file overlay can report `Edit`/`Write`/`Read` success while the on-disk git worktree stays clean (work overlay-only, lost when agent ends); the agent's OWN git status/grep are served the same diverged overlay so naive self-verification passes falsely; engineer-side recipe (stage → git diff --cached + grep -c the ACTUAL file → re-author via Bash on divergence → paste proof lines); architect-side (verify every salvaged worktree from own separate Bash context; divergence-clean ⇒ apply architect-inline, never loop-redispatch; "nothing to commit" is the tell); ≥2 lost-work incidents (DOCS-CONSUME-2 recovered / SW-RLS vanished twice); strict instance of codebase-is-source-of-truth; engineer-seed §1a is the enforcement surface | `CONTEXT/PATTERNS/common/harness-overlay-worktree-divergence.md` |
| Dev↔prod parity — verify in the PRODUCTION SHAPE, not just dev-green (the DISCIPLINE; executable form = deploy-config-contract). "Works in dev" ≠ "works in prod": the slim prod image is STRUCTURALLY different (no `start.sh`/registry/`node`, baked `dist`, env-only config) ⇒ code deriving from a dev-only artifact or a dev-convenience default silently breaks in prod; the platform's **highest-recurrence drift class** (N≥3 — infra.tsx-localhost / nav→localhost / CORS-registry-empty-in-slim, apex login down); §2 dev↔prod difference checklist (open taxonomy) + §3 authoring-time parity question + §4 deploy-time live-probe; special cases = seed-canonical-defaults (wrong value) / boundary-contract-tests B4 (env propagation) / containerization §12b (stale container); born 2026-05-22 (nav-remap → prod-CORS) | `CONTEXT/PATTERNS/devops/dev-prod-parity.md` |
| 🔴 PROD-ONLY — the local Docker dev FLEET is DORMANT since 2026-08-11 (owner's decision: containers cost storage only prod earns back). The `dev` BRANCH, self-branching and `dev`→`main`→`prod` are UNAFFECTED — do not conflate them. Prod is now first-contact for a running image, so the gate moved: `predeploy_check` + CI-green are MANDATORY, `deploy_image` health-probe/auto-rollback is the backstop, post-deploy prod smoke closes it. Nothing was stopped or pruned; revival is the owner's cost decision, with named triggers | `CONTEXT/PATTERNS/devops/dev-fleet-dormant.md` |
| Deploy-config contract — the EXECUTABLE form of dev↔prod-parity §2/§4: every dev↔prod-divergent config knob routes through the seed deploy-config primitive (`noctusai_lib.config.deploy_config` — `resolve_config` / `require_prod_config` / `is_deploy_context` / `MissingProdConfigError`; canonical default ∧ aggregate-fail-loud-if-required-in-prod, no-op in dev) ⇒ a product cannot silently ship a dev value to prod (the nav→localhost / CORS-empty / infra.tsx-localhost class, N≥3); consume recipe + Wave-2 startup guard wired into `create_product_app` + the `check_derives_from_dev_only_artifact` keeper (flags seed code deriving from a dev-only artifact without an env fallback) + the parity-checklist→seam table; value-correctness sibling = seed-canonical-defaults; shipped via `projects/seed-deploy-config-contract` 2026-05-23 (parallel branching-dispatch: A primitive / B keeper / C docs) | `CONTEXT/PATTERNS/devops/deploy-config-contract.md` |
| Prod cache container — `pgvector/pgvector:pg16` service in the prod infra fleet (`compose.infra.prod.yml`); profile-gated (`--profile cache` / `--profile full`); why the pre-baked image beats manual extension install; network: in-fleet-only via `noctus-net` alias `noctus-cache-pg:5432` (never host-published); named volume `noctus-cache-pg-data`; credentials via `.env.fleet` (NOCTUS_CACHE_PG_PASSWORD required-or-fail); healthcheck start_period rationale; backup = Phase 2 pg_dump cronjob (deferred); dev uses local SQLite; composes with cache-auto-freshness + cache-backend-portability roadmap + container-sanitization | `CONTEXT/PATTERNS/devops/prod-cache-container.md` |
| Prod-deploy safety gates — composite of 4 keepers + 1 aggregator + 1 mirror tool that form the pre-deploy gate for the prod cache transformation (Phase 3.4 of cache-backend-portability roadmap); `check_prod_cache_reachable` (high — postgres-only) + `check_cache_backend_env_matches_environment` (warning — env↔backend mismatch advisory) + `check_drift_shield` (warning — OPEN auto-improvement entries touching deploy diff) + `check_slip_shield` (warning — ≥2 s2-memory codification-slip candidates touching deploy diff) + `check_pre_deploy_gate` (composite — blocks only on high) + `noctus.dev.cache_deploy_mirror` (atomic per-cache TRUNCATE+INSERT, vectors transferred verbatim NO re-embed) + `noctus.dev.init_prod_cache_schema` (DDL bootstrap); deploy runbook (bless→promote→deploy_pull→cache-up→init-schema→mirror→verify); shipped 2026-05-26 evening triggered by T1+T2+T3+T5 firing simultaneously | `CONTEXT/PATTERNS/devops/prod-deploy-safety-gates.md` |
| Prod-exposure consent — a product's FIRST arrival on `deploy/fleet/docker-compose.prod.yml` / `deploy/tunnel/ingress.yml` / `ALL_SLUGS` in `scripts/infra/build-and-push.sh` IS the production-promotion decision (once registered + promoted, no later PER-PRODUCT gate reviews "should this be public" again — independent of the same-day PROD-PIN fix to WHICH build moves `:latest`); `check_prod_exposure_consent` (high) fires ONLY on the slug SET-DIFFERENCE (declared-in-working-tree minus declared-at-HEAD), silent on routine `products/<slug>/**` commits + edits inside an existing service block + de-registration; requires `deploy/consent/<slug>.prod.yml` present in HEAD (isolated prior commit) with `consented_by`/`consented_on`/`consent_ref` (resolves to a ✅-marked roadmap milestone) + `dev_validated: true` + `consented_by` matching the commit author; `noctus.dev.prod_consent` status dashboard (honest `missing_pregate` for grandfathered products, never backfilled) + refuse-not-null `request` template generator; `scaffold_product` invariant locked by AST-scan test (never touches the three surfaces); born 2026-07-20 closing the orbity incident (prod-serving six weeks before validation, zero consent on record); also corrects `production-deploy.md § 2b`'s "prod branch gate" overclaim (later fixed the same day — `:latest` now moves only on a prod-ref build) | `CONTEXT/PATTERNS/devops/prod-exposure-consent.md` |
| Product lockfile & slug-set drift — a mirrored artifact (a product's `package-lock.json` snapshot of its `package.json`, or a hardcoded product-slug literal) stops tracking the real thing it mirrors, invisible in dev, discovered only in CI after promotion; N=3 in one session (2026-07-22): `@dnd-kit/*` (`acfef9bb`) → `recharts`/`@radix-ui/react-tabs` (broke 7 products) → `ALL_SLUGS` in `scripts/infra/build-and-push.sh` (a real-but-undeployed `knowledge-extractor` commit aborted the whole fleet build). Two axes: `check_product_lockfile_dep_sync` (high — a declared dep missing `node_modules/<dep>` in the lockfile ⇒ clean `npm ci` silently skips it; fix via SCOPED `npm install <dep>` or a surgical splice, NEVER a bare `npm install` — 535 unrelated version bumps measured from doing that naively) + `check_hardcoded_product_slug_set` extended to `scripts/infra/*.sh` bash arrays (warning); `ALL_SLUGS` now derives from `deploy/fleet/docker-compose.prod.yml` at run time, and a real-but-undeployed slug is deliberately skipped rather than aborting the build. Both gated locally in `scripts/hooks/pre-commit` §6d/§6e (staged-path scoped) — a CI-only gate is not acceptable for this class. Composes with [[base-image-dep-freshness]] (closes its `check_frontend_lockfile_sync` forward-reference) + [[prod-exposure-consent]] (shares the `build-and-push.sh` scan surface) | `CONTEXT/PATTERNS/devops/product-lockfile-and-slug-drift.md` |
| Tunnel ingress source-of-truth — `deploy/tunnel/ingress.yml` declared itself SINGLE SOURCE OF TRUTH since 2026-05-21 but nothing derived from it. `compose.tunnel.yml` mounts `./`, so the LIVE cloudflared config was whichever dir the tunnel was started from — a gitignored copy under `projects/production-deploy-migration/` — and adding igig's route to the canonical file changed nothing live, SILENTLY (2026-08-10); the committed `config.yml.template` snapshot had also drifted 2 routes behind (igig, orbity), and 5 products had no DNS CNAME at all. Three legs: `tunnel_config action='render'` (offline; catch-all last by construction), `check_tunnel_ingress_snapshot_sync` (pre-commit; the in-repo artifact), `action='check'` (SSH; the LIVE config) — the split is stated because a hook cannot see the VPS. `action='apply'` REFUSES to drop a live hostname ingress.yml doesn't declare. Footgun: cloudflared runs as uid 65532, so a root-owned credentials JSON crash-loops the tunnel into a fleet-wide CF 530. DNS stays human-gated (`route-dns.sh` needs the zone `cert.pem` from an interactive login). Same failure mode as [[product-lockfile-and-slug-drift]], different surface | `CONTEXT/PATTERNS/devops/tunnel-ingress-source-of-truth.md` |
| SSH deploy-key restrictions — `restrict` vs `permitopen` gotcha on Ubuntu OpenSSH_9.6p1: the intuitive `restrict,permitopen="127.0.0.1:5432"` shape silently produces "channel open failed: administratively prohibited" because `restrict` includes `no-port-forwarding` and overrides the `permitopen` allow-list at the policy layer; canonical pattern = explicit `command="/bin/false",no-pty,no-X11-forwarding,no-agent-forwarding,permitopen="127.0.0.1:5432"` (drop `restrict` entirely when permitting any forward); verified during GitHub Actions tunnel wiring for prod `noctus-cache-pg`; composes with [[ci-embedding-cache-gate]] (consumer) + [[prod-cache-container]] (the host-loopback 127.0.0.1 carve-out this key reaches) | `CONTEXT/PATTERNS/devops/ssh-deploy-key-restrictions.md` |
| Silent test failure from missing optional dep — a test suite reports green while N tests silently fail-to-import because the production code does `try: import X / except ImportError` (degrades gracefully at runtime) but the test file imports `X` to exercise both branches and `X` is NOT declared in `requirements.txt` / `pyproject.toml`; failing tests are <100% of suite so suite-level exit code stays clean; born 2026-05-26 from `pgvector` + `psycopg2-binary` un-declared deps causing 11 silent failures in `test_cache_backend_postgres.py`; right shape = declare dep in `requirements.txt` + `pyproject.toml` (lockstep) OR `@pytest.mark.skipif(not <dep_present>, ...)` when dep is genuinely optional; sibling to [[boundary-contract-tests]] (at the dep-declaration boundary instead of the wire-contract boundary); detection deferred to keeper at N=3 (currently N=2 — `pgvector` + `psycopg2-binary` same module) | `CONTEXT/PATTERNS/common/silent-test-failure-from-missing-dep.md` |
| Seed defaults are canonical-shared answers, not consumer-#1 coincidences — a seed fallback literal (`\|\| "X"` / `getenv(..., "X")`) must be the **architectural canonical answer** for the module's contract (e.g. same-origin `""` for the house single-container HTTP model), not a value that happens to work for consumer #1; the bug stays silent because consumer #1 works + consumers #2..N either mask-by-override or misroute-with-opaque-error; "no canonical answer at seed-level" carve-out (`""` / `None` / typed-error, never a literal pretending to know); **paired rule**: multi-stage Dockerfile env-inheritance (stage A's ENV doesn't reach stage B unless `B FROM A` — every build-running stage re-declares); worked examples table (HTTP URL · DB schema · auth · LLM model · storage · webhook · Redis); detection-by-keeper deferred to N=3; bit 2026-05-20 social-wiring (`infra.tsx` defaulted to `localhost:8000` → silent CORS misroute → "Servidor indisponivel" toasts) | `CONTEXT/PATTERNS/architect/seed-canonical-defaults.md` |
| Boundary-contract tests — named class for "tests-green-dashboard-red" bugs where unit tests cover each side of a seam but nothing covers the **contract crossing it**; five recurring boundaries (B1 build-injection via vite define / B2 HTTP schema FE↔BE caps / B3 third-party library contract — TanStack v5 `queryFn` return-type / B4 container env propagation across .env→stage→runtime / B5 library-default propagation from seed default to N consumers); each shape has the same anatomy (contract crosses process/build/wire/runtime boundary · each side independently tested · contract itself untested · bug surfaces only in production-shape execution); authoring-time discipline ("if this contract drifts, what existing test fails?" — none ⇒ file boundary test or accept-w/-destination); §4 shipped-detector status table by boundary; §5 detector specification for `check_query_fn_returns_undefined` (Stage-4 keeper, AST-light brace walk over `queryFn:` arrow bodies, escape hatch `query-fn-undefined-ok`, 0 live baseline at ship time); §7 anti-pattern (more unit tests = same class re-emerges; structural fix = static detector for B1/B3/B5 + contract-extraction or runtime smoke for B2/B4); recurrence-rule integration (N=1 fix in-flight + memory / N=2 triage / N=3 MUST formalize); bit chain 2026-05-20 social-wiring (`infra.tsx` localhost:8000 / vite-factory `\|\| 8000` / ENCRYPTION_KEY-empty / `le=20`-vs-top-50 / useLLMSpend-`return undefined`-data-is-undefined) | `CONTEXT/PATTERNS/backend/boundary-contract-tests.md` |
| Consent routes mandate — seed-first: `/consent`, `/consent/privacy-policy`, `/consent/terms-of-use` are mounted by `createProductApp` for EVERY product (LGPD/ABNT + Google OAuth verification + Meta App Review canonical URLs); 3 invariants enforced by `check_consent_routes_mounted` keeper (severity high): (1) `seed/framework/frontend/src/app.tsx` mounts all 3 routes + imports the 3 page components, (2) `seed/framework/frontend/src/index.ts` exports the 3 page components + consent content module, (3) no `products/<slug>/frontend/` file declares/re-implements `ConsentHubPage`/`PrivacyPolicyPage`/`TermsOfUsePage`; a product-local declaration shadows the canonical seed mount → Google+Meta verification URLs break fleet-wide; consume from `@noctusai/lib` | `CONTEXT/PATTERNS/frontend/consent-routes-mandate.md` |
| Core-URL routing — the **MANDATORY** system for how a product reaches core (SSO callback, "back to dashboard" nav, any product→core XHR): resolve core's URL through the canonical seed getter `env.CORE_URL` / `env.CORE_API_URL` (`@noctusai/lib`, source `seed/lib/frontend/src/env.ts`) — NEVER hand-roll `import.meta.env.VITE_CORE_* \|\| "<literal>"`; the recurrence it closes (N≥3, prod SSO outage 2026-05-25): a bare `localhost:8000` default → "Failed to fetch" for any product baking only VITE_CORE_URL, a stale `localhost:5173` default (dead pre-house vite port) → dead nav; getViteVar dynamic access proven prod-safe (createProductSupabase reads env.SUPABASE_* the same way); the ONE legitimate hand-roll = core's same-origin `lib/api.ts` (window.location.origin fallback); new products get working SSO+nav **by construction** (seed `app.tsx` SSOCallback + `products/seed` template pages + Dockerfile VITE_CORE_URL bake); two-layer system (FE→core getter + launcher→product `resolve_product_url`); enforced by the `check_handrolled_core_url` Stage-4 keeper (high; carve-out core api.ts); siblings boundary-contract-tests B1 / seed-canonical-defaults / dev-prod-parity | `CONTEXT/PATTERNS/frontend/core-url-routing.md` |
| Dev↔prod parity — UMBRELLA class: "works in dev" ≠ "works in prod"; a change is **not done** until verified in the **production shape** (slim image / live VPS), not just dev-green; the slim prod `runtime` image is structurally **not** a dev box — ships **no `start.sh`/PRODUCTS registry, no node, baked `dist`, env-only config** → code that derives behavior from a dev-only artifact (registry, build tool) or sets a dev-convenience default silently breaks in prod; the **noc dev↔prod difference checklist** (start.sh-absent · node-absent · `VITE_*`-baked-at-CI-build · BE-config-env-only-must-be-SET · DB-`url_base`-localhost-by-design-override-via-env · CORS-must-include-prod-origins-derive-from-`PRODUCT_URL_<SLUG>`-env-directly · `public.products`-rows-only-what-was-mirrored · CI-installs-root-superset-not-per-product-reqs · LLM-OpenAI/Gemini-no-Anthropic; **open taxonomy**); §3 authoring-time **parity question** ("does this hold in the slim prod container, not just my dev box?") + §4 deploy-time **live-probe** discipline (verify FE bake, OPTIONS-preflight the prod origin, keep the override safety during a risky cutover); [[seed-canonical-defaults]] (wrong value) / [[boundary-contract-tests]] B4 (env propagation) / [[containerization]] §12b (stale container) are **special cases**; detection — wrong-value sub-class already Stage-4 (`check_seed_canonical_default`), derives-from-dev-only-artifact sub-class Stage-3 deferred-to-N=2, verify-in-prod-shape judgment-dependent Stage-3-by-design; bit chain 2026-05-20 `infra.tsx`-localhost + 2026-05-22 nav→localhost + CORS-registry-empty-in-slim-container (apex login `Servidor indisponível`) | `CONTEXT/PATTERNS/devops/dev-prod-parity.md` |
| Rebuild only modified products, never the whole fleet — container rebuild scope = the products whose code was actually modified this session (`git diff --name-only origin/main..HEAD` → `products/<slug>/` prefixes), NOT the fleet (even when a seed-level change technically affects every product); other products catch up **lazily** on their next own-modify rebuild because every Dockerfile `FROM noctus-seed-*-base` (the structural cure); per-change-shape scope table (seed-only → base images only · 1 product → just that product · N products → just those N · fleet smoke-test → explicit-ask carve-out) + 2026-05-20 worked example + when-to-fan-out carve-outs (explicit user ask / fleet-blocking validation / CI) + composes with [[pilot-products-first]] (3-pilot bound for validating; modified-only bound for normal-dev building) + [[estimate-off-evidence]] (read the diff, don't assume from "seed touches the fleet"); bit 2026-05-20 — fleet-wide rebuild reflex repeated mid-session after parallel-build daemon crash despite the user having a learned-error tag on it | `CONTEXT/PATTERNS/common/minimum-viable-rebuild.md` |
| First clone + starting servers | `CONTEXT/GUIDES/setup.md` |
| Creating a new product | `CONTEXT/GUIDES/new-product.md` |
| Seed-first design checklist (cross-product projects — REQUIRED at authoring time) | `CONTEXT/GUIDES/seed-first-design.md` |
| Putting a workspace product online for testing — the "deploy" drill (verify docker artifacts → fill `.env` → `docker compose up` → verify); trigger phrases the agent should recognise | `CONTEXT/GUIDES/deploy-workspace-online.md` |
| Absorbing a separately-developed seed-workspace into noc as one product — the repeatable 10-gate procedure (Gate 0 snapshot-preserve + sanctioned `--no-verify` · Gate 1 bring-source-in-home BEFORE plan depends on it + worktree-base-vs-uncommitted-inputs trap · Gate 2 completeness audit / UNMAPPED-useful · Gate 3 interrogate disposition before deletion · Gate 4 scaffold house-container · Gate 5 full seed-reconcile sibling-validated-wins + verify-the-seed-ships-it 4 shapes + master-tree-parallel zero-git + git-stash-forbidden · Gate 6 port + pilot-first + pause-on-dependency · Gate 7 mechanical consumer-adapt + git-log-S-before-attribution · Gate 8 teardown preservation-FIRST + hazard-group commits + registry-derive + content-form dangling-ref verify + KB-count-autostage footgun · Gate 9 container-refactor + user-gated workspace retirement); written from the proven social-wiring absorption 2026-05-16; **+ the learning-absorption refinement (orbity 2026-06-02)** for a FOREIGN production platform: absorption = seed-capability-uplift NOT product-port (the absorbed product is the last consumer of organs it taught the seed to grow) · knowledge-absorption-first (deep diagnose + NEUTRAL compare) · salvage-the-idea-behind-the-bad-code (a "noc supersedes" verdict is on the CODE not the FUNCTIONALITY); self-contained | `CONTEXT/GUIDES/absorb-seed-workspace.md` |
| Orbity absorption knowledge set (BR marketing-agency SaaS diagnosed for absorption 2026-06-02) — faithful procedures/flows/automations/rules · 121-table data model + RLS · 73 edge functions · ~27 FE domains · integrations · NEUTRAL 16-capability Orbity⟷noc comparison · deep-dives (financial mgmt / Meta-Ads CAPI / chatbot-AI-automation) · bad-code salvage evaluation · seed-first uplift strategy; consumed when reconstructing orbity-noc + when uplifting the fleet (erp-imobiliario pilot) | `CONTEXT/ABSORPTIONS/orbity/` (README indexes the 15 docs) |
| Google Cloud Console OAuth setup — Calendar API (project + consent screen + Calendar API enablement + Web client + redirect URI registration + scopes + env-var wiring + smoke test + troubleshooting); first adopter therapy-platform per-therapist `/api/scheduling/gcal/*`; reusable for any future product needing user-delegated GCal | `CONTEXT/GUIDES/google-oauth-setup.md` |
| Production deploy of the noc fleet to a VPS (the *external server* deploy, NOT the local `./start.sh` drill) — git deploy-key code delivery · build-ON-VPS slim `runtime` images (VITE baked) · external `noctus-net` · edge **A** Caddy-on-real-subdomains (DNS-record-only path, auto-LE) **or B** Cloudflare named tunnel (zone-on-CF path) + A→B zero-URL-change migration · volume-preserving PaaS (Coolify) decommission · 6 evidence-tested lessons (LE DNS negative-cache · compose `--env-file` · `depends_on` over-start · cloudflared nonroot creds · local-resolver-lies · product-LLM=OpenAI/Gemini-no-Anthropic); from the 2026-05-21 `noctusai.com` migration; self-contained | `CONTEXT/GUIDES/production-deploy.md` |
| Caching a product's body (the procedural recipe for the next product after the seed pilot) — the **5-wave per-product recipe** (W_a Audit READ-ONLY → W_b Cleanup forks → W_c Register product-specific emerging organs → W_d Populate the 8-field knowledge bundle → W_e Acceptance + manual-validation co-loop) for caching components / pages / integrations as canonical reusable **organs** in `noc-graph` + `code-embeddings` (extend, never spawn a 9th cache); §1 prerequisites table (find_reusable_component / component_bundle / component_list / consumes_component edges / vectorize-embed-cache META-RULE / build-learn-cache mindset / canonical seed catalog); §3 the 7 lessons from the seed pilot worked example (gain-may-already-exist · re-export-attribution-must-be-honest-first · derived-validation-not-manual · shelfware-gets-its-own-tag · named-seam-declaration-only-legitimate-divergence · vectorize-embed-cache-universal-pipeline · surfacing-greater-than-bypassing); §4 common gotchas (cache cwd-drift during pre-commit on worktree · configure_llm() requirement · wrapping-with-different-name invisible to keeper · re-embedding cost · auto-improvement.ndjson write-after-pre-commit); §5 recommended fleet order (social-wiring → erp-imobiliario → therapy-platform → others); §6 build-learn-cache applied (the journey IS cached per artifact); composes — never duplicates — `seed-organ-canonical-set` / `build-learn-cache-mindset` / `vectorize-embed-cache-framework` / `cache-as-agent-tool` / `noc-graph` / `absorbed-product-seed-shape-seam` / `bypass-rationalization-anti-patterns`; self-contained; born 2026-05-29 from the seed pilot Phase 1 | `CONTEXT/GUIDES/product-body-caching.md` |
| OAuth integration patterns — cross-provider (5-layer model · scope auto-discovery · dual auth backends · single-consent bundling · post-consent introspection · CredentialStore Fernet · G1-G6 gotchas · Meta token-chain + auth-mode matrix · Google↔Meta scope-discovery diff · setup-guide template · in-seed-vs-gap audit) | `CONTEXT/INTEGRATIONS/oauth-patterns.md` |
| Meta Graph adapter consume-side (`noctusai_lib.integrations.meta` exact `__all__` · `get_meta_adapter` auth-resolution priority system_user→user_oauth→Fake · consume recipe with cited social-wiring consumer · `make_meta_router` seam · read-only-v1 out-of-scope: posting/ads/webhooks) | `CONTEXT/INTEGRATIONS/meta.md` |
| WhatsApp connector consume-side (`noctusai_lib.integrations.whatsapp` exact `__all__` · WAHA `get_whatsapp_client` vs Meta-Cloud-API `get_meta_cloud_client` backends · `create_whatsapp_webhook_router` seam · lid-auth + dedup + response-registry · cited ERP consumer) | `CONTEXT/INTEGRATIONS/whatsapp.md` |
| Google integrations consume-side — Calendar/Maps/YouTube/Drive/Gmail (each exact `__all__` · resolver/credential-store factory injection · YouTube quota-cost-documented Protocol · Drive dual download+read Protocols · Gmail OAuth-only send+read v1 with `OAuthGmailCredentials`/`GmailCredentialResolver`/`make_gmail_client` · seed-ahead consumer status · cited social-wiring + mcp/google/tools/gmail consumers) | `CONTEXT/INTEGRATIONS/google.md` |
| Mailchimp Marketing API v3 consume-side (`noctusai_lib.integrations.mailchimp` Protocol+Fake+httpx-Real+factory · subscriber_hash upserts · static-segments-as-Listas (audience plan caps) · error hierarchy import-never-redefine · AppException `{"error":{code,message}}` envelope · per-org Fernet-TEXT credentials · template admin deep-link · cited social-wiring consumer) | `CONTEXT/INTEGRATIONS/mailchimp.md` |
| Vista CRM REST API (auth, query convention, response envelope, error hierarchy, endpoint inventory, adapter contract, per-tenant calibration gap) | `CONTEXT/INTEGRATIONS/vista.md` |
| Grupo OLX portal leads — ZAP/VivaReal/OLX/ImovelWeb/Casa Mineira (inbound-only lead webhook · HTTP Basic `vivareal:<SECRET_KEY>` per-CRM · status-code-only delivery contract · 3 retries then 14-day discard with no replay · the ONE documented 4xx (listing lead without `clientListingId`) · MCMV leads carry no listing ids · `grupo-olx` single-slug attribution because the payload never names the portal · tenant-resolution chain ending in park-as-unresolved · outbound Gestor de Leads `SALE`/`RENTAL` vs inbound `SELL`/`RENT`) | `CONTEXT/INTEGRATIONS/olx.md` |
| Image generation consume-side (`noctusai_lib.integrations.image_gen` exact `__all__` · `get_image_gen_adapter` factory · `FakeImageGenAdapter` deterministic-URL signal · `GeminiImageGenAdapter` lazy-SDK Real backend · renderer-agnostic Protocol · cited social-wiring/media_creation consumer; v1 Gemini-only — OpenAI/Stability/Replicate extension recipe documented) | `CONTEXT/INTEGRATIONS/image-gen.md` |
| social-wiring multi-account integration credentials — product-local `integration_accounts` table + service (governs `feedback_seed_shape_vs_primitive_consume`: seed CredentialStore is single-row-per-(org,provider) ≠ multi-row, so product-local table consumes seed Fernet PRIMITIVE via `credential_vault.require_fernet`, no Protocol fork); v1 providers: youtube/google_drive/gmail/meta/n8n; WAHA excluded (whatsapp_connections is the existing solution); YouTube OAuth start/callback ships; 3-step recipe for adding new providers; REST surface under `/api/integrations/`; is_default atomic-set invariant; RLS org-scoped | `CONTEXT/PRODUCTS/social-wiring/INTEGRATIONS-MULTI-ACCOUNT.md` |
| Core backend (routers, services, tables) | `CONTEXT/backend/01-CORE.md` |
| ERP backend | `CONTEXT/backend/02-ERP.md` |
| PF backend | `CONTEXT/backend/03-PF.md` |
| Database inventory per schema | `CONTEXT/backend/04-DATABASE.md` |
| AI features (OpenAI, embeddings, summaries) | `CONTEXT/backend/05-AI-FEATURES.md` |
| Therapy backend | `CONTEXT/backend/06-THERAPY.md` |
| Auth + security deep-dive | `CONTEXT/backend/07-AUTH-SECURITY.md` |
| Daily Life backend | `CONTEXT/backend/08-DAILY-LIFE.md` |
| Core frontend | `CONTEXT/frontend/01-CORE.md` |
| ERP frontend | `CONTEXT/frontend/02-ERP.md` |
| PF frontend | `CONTEXT/frontend/03-PF.md` |
| Therapy frontend | `CONTEXT/frontend/04-THERAPY.md` |
| Skill design (composable agent units) | `INSTRUCTIONS/01-SKILLS.md` |
| MCP integration patterns | `INSTRUCTIONS/02-MCP.md` |
| Agentic workflows | `INSTRUCTIONS/03-AGENTIC-WORKFLOWS.md` |
| Eval strategy | `INSTRUCTIONS/05-TESTING-EVALS.md` |
| Tech stack details | `INSTRUCTIONS/06-TECH-STACK.md` |
| Artifact templates | `INSTRUCTIONS/07-TEMPLATES.md` |

---

## By situation — "when I'm doing X, read Y"

| Situation | Start here |
|---|---|
| Fresh agent, zero context | `AGENT-CONTEXT.md` → `CONTEXT/01-PHILOSOPHY.md` → `CONTEXT/02-LANDSCAPE.md` |
| About to write new backend code | `CONTEXT/PATTERNS/backend/backend.md` + product-specific `CONTEXT/backend/0X-*.md` |
| About to write new frontend code | `CONTEXT/PATTERNS/frontend/frontend.md` + product-specific `CONTEXT/frontend/0X-*.md` |
| Touching any DB migration | `CONTEXT/PATTERNS/backend/database-rls.md` + `CONTEXT/backend/04-DATABASE.md` |
| Adding a new product | `CONTEXT/GUIDES/new-product.md` + `CONTEXT/03-SEED-ARCHITECTURE.md` |
| Adding a shared component | `CONTEXT/04-SHARED-LIBRARY.md` (check existing first) |
| Working on tests | `CONTEXT/PATTERNS/compliance/testing.md` |
| Adding a `try/except` (production code) | `CONTEXT/PATTERNS/backend/logging.md` (level guide, the no-`# silent-ok` rule) |
| Adding a new keeper detector | `CONTEXT/PATTERNS/compliance/testing.md § Regression-test-the-detector` + `CONTEXT/06-AGENTS.md § Detectors` |
| Adding a helper to seed lib (deciding which layer it lives in) | `CONTEXT/PATTERNS/architect/seed-lib-layout.md § Where to put a new helper` |
| Looking for an existing seed-lib helper | `CONTEXT/PATTERNS/architect/seed-lib-layout.md § Where to look` + `CONTEXT/04-SHARED-LIBRARY.md` (catalog) |
| Working on env / deployment | `CONTEXT/PATTERNS/devops/environment.md` + `CONTEXT/05-INFRASTRUCTURE.md` |
| Touching UI with performance data / gamification | `CONTEXT/07-GAMIFICATION.md` |
| Reading a large/unfamiliar file (default — narrow-read first) | `CONTEXT/PATTERNS/common/agent-reading-discipline.md § Narrow-read first` |
| About to edit `.py` / `.ts` / `.tsx` source — rename, codemod, find-callers, anything beyond a 1-line targeted edit | `CONTEXT/PATTERNS/common/ast.md` (AST-first; never sed/regex on source) |
| Adding any helper or function an agent might want to call (Claude Code / future bot / future product agent) | `CONTEXT/01-PHILOSOPHY.md § MCP-first` (default surface is `mcp/noctusai/`) |
| Designing a new agent / skill / MCP | `INSTRUCTIONS/00-MASTER.md` |
| Touching Vista CRM (adapter, MCP server, endpoint surface, field-set calibration) | `CONTEXT/INTEGRATIONS/vista.md` |
| Touching Grupo OLX / ZAP / VivaReal / ImovelWeb portal leads (webhook receiver, payload contract, `mcp/olx`, attribution) | `CONTEXT/INTEGRATIONS/olx.md` |

---

## Sync with `CLAUDE.md`

`CLAUDE.md` at the repo root is the **outer map** — slim, loaded every Claude session, contains behavioral rules + pointers into this KB.

This `INDEX.md` is the **inner map** — the KB's own authoritative self-description. Kept in sync with CLAUDE.md's map section.

Sync enforcement (pick any/all):
1. **Rule** — when you change `CLAUDE.md`'s map or any KB file/folder, update both this INDEX and CLAUDE.md. Documented in `CONTEXT/01-PHILOSOPHY.md → Docs stay in sync`.
2. **Script** — `noctus.dev.kb_sync` validates that CLAUDE.md pointers resolve to real files and all KB files are indexed. Run pre-commit.
3. **MCP tool** — `python mcp/noctusai/cli.py verify-kb-sync` (same check, integrated into the heal loop).
