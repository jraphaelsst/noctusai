# CLAUDE.md · v3.0 — router

<!-- NEW-SESSION-CONTEXTUALIZATION -->
## 🧭 New session — read this only if you lack platform context

**Fresh / clean-context agent** AND the user says **"contextualize"** / **"please contextualize"** (or you have no idea what this platform is) → **STOP and read `/CONTEXTUALIZE.md` (repo root) FIRST**, then do the task. That file is the curated read-map (core files, order, the how-we-work mental model) — one read, then you're oriented.

**Already-working / already-contextualized agent → this section is a NO-OP. Skip it. Do NOT re-open `CONTEXTUALIZE.md`** (you're past onboarding; re-reading wastes tokens). Grep anchor: `NEW-SESSION-CONTEXTUALIZATION`.

---

> **What this file is.** Auto-loaded every session. Two jobs: (a) the universal behavioral rules Claude obeys every turn, (b) routing pointers into `CLAUDE/<topic>.md` (topical rules, on-demand) and `KNOWLEDGE-BASE/` (depth, on-demand). Kept lean on purpose — the auto-loaded budget compounds across every reply.
>
> **What this file is NOT.** A tutorial, changelog, spec sheet, manual, or rule body container. Bodies and depth go elsewhere. If you find yourself writing >80 words on a single rule here, push the long-form into KB and shorten the rule to a pointer.
>
> **When you can't find a rule:** open `CLAUDE/<topic>.md` (sibling of this file) for topical rules, or `KNOWLEDGE-BASE/INDEX.md` for depth.

---

## 0 · How this file is organized

| Section | Job | Use |
|---|---|---|
| **§1 Universal rules** | Behavioral rules that apply regardless of task. | Read every session. |
| **§2 The Map** | Pointers into `CLAUDE/<topic>.md` and `KNOWLEDGE-BASE/`. | Open *on-demand* — never pre-load. |
| **§3 When to read what** | Task → "open this first" lookup, including topical `CLAUDE/*.md` files. | Scan before starting any non-trivial work. |
| **§4 Sync rule** | How CLAUDE.md, topical files, KB, and memory stay aligned. | Enforced by pre-commit hook. |

---

## 1 · Universal rules

Each bullet is rule + one-clause why + pointer. Bodies / examples / slip-history live in the pointer target. Soft cap ≤80 words/bullet (`KB § PATTERNS/project-execution.md § 2.8`).

- **Vocabulary — methodology, not doctrine.** Use *methodology / rule / principle / convention / pattern / working agreement.* Avoid "doctrine / doctrinal" — hierarchical framing runs counter to how this team operates. → `KB § 01-PHILOSOPHY.md § Vocabulary — methodology, not doctrine`
- **Seed first. Always.** Every product inherits via `create_product_app()` (backend) / `createProductApp()` (frontend). Customizations flow through NAMED seams (`standard_routers=[...]`, `authProvider`, `lifespan_*`, …). A customization NOT through a named seam = structural fork — refactor or accept-with-rationale. **Don't ask whether to use the seed; the seed IS the approach.** Run the 4-question Practical Decision Test before any structural change. → `KB § 03-SEED-ARCHITECTURE.md § Seed as Skeleton` + `§ Practical decision test`
- **Verify the seed ships it — before locking any "consume the seed X" decision.** Read the module's `__init__.py` exports + the concrete adapter file; confirm runtime path covered, not just the Protocol or Fake. Gap + N=1 consumer → ship against Fake, surface follow-up. Gap + N=2+ consumers → DRY-recurrence, file the seed real-adapter project. Slip looks like *"the seed has Protocol X, so we can do Y"* when only the Fake ships. → `KB § 03-SEED-ARCHITECTURE.md § Verify-the-seed-ships-it test`
- **Seed IO modules ship Fake+Real with factory.** Every seed module touching IO (HTTP, DB, vendor SDK) ships in **Protocol + Fake + Real + factory** shape mirroring `integrations/google_calendar` + `google_maps`. Pure-logic / crypto / shaping modules exempt — exemption test: *"would a Fake here exercise different code than the Real?"* If no, exempt. Half-shipped (Protocol+Real only OR Protocol+Fake only) generates consumer-side forks at the seed level — fix at the seed before consuming. → `KB § PATTERNS/seed-fake-real-adapter.md`
- **Seed defaults = canonical-shared answer, not consumer-#1 coincidence.** Every seed fallback (`|| "X"` / `getenv(..., "X")` / `config.X if config else "X"`) MUST be the architectural canonical answer for the module's contract — *never* a literal that happens to match consumer #1. Consumer-#1-coincidence defaults stay silent (consumer #1 works; consumers #2..N either override-mask or misroute-with-opaque-error). No canonical answer at seed-level → `""` / `None` / typed-error, never a literal pretending to know. **Paired rule:** multi-stage Dockerfile envs in stage A don't reach stage B unless `B FROM A` — every build-running stage re-declares. Bit 2026-05-20: `infra.tsx` defaulted to `http://localhost:8000` (core's port) → every other product's FE silently routed at CORE → CORS → toasts. → `KB § PATTERNS/seed-canonical-defaults.md`
- **No incomplete commits.** Backend and frontend at the same maturity. "Scaffolded" is not "complete." If one side is real and the other is a placeholder, stop and flag.
- **No quick fixes.** A fix that touches multiple products for the same reason is at the wrong level — go up to seed / shared lib / config and let it propagate. Thirty minutes on the root beats five minutes on a patch that generates future work.
- **No workarounds — and no monkey-patching, in production OR tests.** Use the real API/SDK/framework. **The rule applies to test code too.** Never `monkeypatch.setattr(our_module, "our_guard", _noop)` — that test no longer exercises the guard. Right shape: seed real underlying data; use dependency injection for write side-effects; read inserts via `MockRequestBuilder.inserted_payloads`. `unittest.mock.patch.object(<external_integration>, ...)` for **external services** (LLM APIs, transcription, network) is fine. → `KB § 01-PHILOSOPHY.md § No workarounds` + `KB § PATTERNS/testing.md`
- **Estimate off evidence, not structure.** Before offering A/B/C, a session-size, or "this is quick" — open the files the change would actually touch. If it touches `seed/`, a shared lib, a factory, or any cross-cutting layer, read that code first. → `KB § 01-PHILOSOPHY.md § Estimate off evidence`
- **Codebase is source of truth; docs/summaries/memory are derived.** Docs / summaries / memory / another agent's report / PROJECT.md §11 are *derived* and drift. Before acting on any such claim — resuming, picking up another agent's work, acting on a recalled memory — **the first command verifies it against the tree** (`git status`/`git diff`/`Read`/`pytest`/ledger); state "grounding in actual tree state." Doc disagrees with code → code wins, fix the doc same change. → `KB § 01-PHILOSOPHY.md § Codebase is source of truth`
- **Fix-on-contact for pre-existing debt — fix THEN surface, never surface-only.** Bumping into a pre-existing failure/bug/drift while doing other work does NOT license leaving it. Always all three: (1) verify pre-existing vs baseline; (2) **fix it in-flight**; (3) surface problem + root-cause + *solution applied*. Surface-only = silent-error shape one level up. Exceptions (never silent): out-of-safe-scope (hard-to-reverse/security/needs-a-decision) → surface with recommendation + named destination + reason; or balloons into a project → file it. Default = fix on contact. → `KB § 01-PHILOSOPHY.md § Fix-on-contact for pre-existing debt`
- **DRY — the recurrence rule.** **N=2 → triage time** (formalize / refactor / accept-with-rationale; decision recorded; silently moving on forbidden). **N=3+ → MUST formalize** (extract into seed-lib / framework / shared library; minimum response = file a follow-up project; silently shipping the 4th instance forbidden). When the rule fires: STOP, name the pattern, decide the destination, file or apply, resume. → `KB § PATTERNS/project-execution.md § 2.7 The recurrence rule`
- **Componentize everything.** Check `KB § 04-SHARED-LIBRARY.md` before writing anything new. If another product will need it, build it shared from day one. → `KB § 04-SHARED-LIBRARY.md`
- **Reading & research discipline — narrow-read; delegate breadth.** **Narrow-read first**: structure before bodies for any file >200 lines or unknown range — outline via grep/small-`limit` `Read`, fetch bodies only for what you'll edit/cite/reason about; whole-file reads reserved for short files / full reviews / content-is-structure. **Explore-agent delegation**: delegate to the Explore subagent when answering needs **3+ targeted greps, multi-file walking, or open-ended discovery** (trigger = research breadth, not product count); direct `Read`/`grep` when the path/symbol is known; prefer dedicated `noctusai_*` scan tools. → `KB § PATTERNS/agent-reading-discipline.md § Narrow-read first` + `KB § PATTERNS/agent-reading-discipline.md § Explore-agent delegation`
- **Replication-to-seed symmetry — fires at READ/PLAN/DESCRIBE time.** *The trigger is LANGUAGE.* Phrasings like **"per-product X"**, **"mount across N products"**, **"for each product Y"** ARE the slip — wherever they appear (your reply, project docs, user prompt). The right per-product code count for a cross-product concern is **zero**. Authoring-time corrective: every `PROJECT.md` MUST include §3a Seed-first analysis BEFORE §6. → `KB § PATTERNS/project-execution.md § The replication-to-seed symmetry rule` + `KB § GUIDES/seed-first-design.md`
- **AST-first — never regex code edits.** Code changes go through an AST tool: `libcst` (Python), `ts-morph` (TypeScript), `tree-sitter` (cross-language). Regex/sed/awk only for prose, search, log inspection. Boundary: *if a compiler/interpreter/type-checker parses the file, use the AST tool.* **Structural-refactor corollary**: grep misses segmented construction — pytest + builds are the oracle. **Always-outline-able invariant**: pre-commit `check_files_outlined` blocks any staged un-AST-readable file (`cli.py --check-outlined`); `noctus.dev.scan_outlined` audits (baseline zero). → `KB § 01-PHILOSOPHY.md § AST-first` + `KB § PATTERNS/ast.md` (§ Always-outline-able platform)
- **Flag MCP-first / AST-first opportunities proactively.** Bystander rule: working elsewhere and you spot a missed exposure (a capability that should be MCP, or a sed/regex edit that should be AST) — **apply now** if cheap, or **defer with destination** (active project `**Improvements:**` / accept-with-rationale / follow-up project). Silent skipping forbidden — same shape as silent errors. → `KB § 01-PHILOSOPHY.md § Flag MCP-first / AST-first opportunities proactively`
- **MCP-first scripts — new automation defaults to a `noctus.dev.*` MCP tool, not a `scripts/` one-off.** A new automation script IS an agent-exposable capability → author it as an MCP tool (+ `cli.py` flag + colocated `Test*`), never a bare `scripts/*.sh|*.py`. Shell allowed ONLY for three named structural carve-outs — `[carve:hook]` git-hook entry (logic→MCP, shell=thin dispatcher) · `[carve:bootstrap]` pre-venv (creates the env the MCP runs in) · `[carve:docker]` thin docker-orchestration — each requiring a manifest row + accept-with-rationale entry. Every top-level `scripts/*.{sh,py}` MUST have a §3 manifest row; an undecided one is a keeper warning (`check_new_script_lacks_mcp_analog`). → `KB § PATTERNS/mcp-first-scripts.md`
- **Hygiene scanning — run the keeper-analog tools before walking away.** **Absorption-search (standing duty)**: editing a product's services/routers/hooks — *even unrelated work* — run the MCP scan sextet (`scan_cross_product_helpers` + `scan_within_product_helpers` + `scan_service_line_recurrence` + `scan_block_patterns`) before/after; within-product N=2 needs architect-eyes in Phase 0 (scanners threshold N≥3). **Code hygiene**: `noctus.hound.scan` = single entry for absorption (file-level) · fusion (cross-tool) · optimization (intra-file), emits `next_action` (default *"what cleanup is urgent?"*); keeper = regulatory, hound = curatorial. **Storage hygiene**: `noctus.dev.mole` / `python mcp/noctusai/cli.py --mole scan` over artifacts · environments (advisory) · stale `.claude/worktrees/agent-*/` (`sweep --force` destructive, dry-run default; never deletes uncommitted/main/siblings/`.env`/migrations); triggers pre-dispatch + pre-commit + bootstrap. → `KB § 06-AGENTS.md § Absorption-search sextet` + `KB § 06-AGENTS.md § Code-hygiene trio` + `KB § PATTERNS/seed-absorption.md § noctus.hound.scan` + `KB § PATTERNS/storage-hygiene.md`
- **Triage at decision time — formalize / refactor / accept-with-rationale.** Every divergence from ideals lands on one of three explicit outcomes: **formalize** (extend framework/seed), **refactor** (align with contract), or **accept-with-rationale** (catalog the entry in `KB § PATTERNS/accept-with-rationale.md` — durable register that survives project folder deletion). "Accept" is a real landing — paperwork keeps it from going silent. Recurrence flips prior `accept` outcomes toward `formalize`. → `KB § 01-PHILOSOPHY.md § Triage at decision time` + `KB § PATTERNS/accept-with-rationale.md`
- **Safety nets capture failures; failures become learnings; methodology evolves.** Methodology is incomplete by design. Safety nets — `git merge` (merging gap), drive-by-exception (hook false-positives), `git stash` (switch blockers) — keep the system working at a gap. **Capture the learning** + **update the methodology** (three-way sync) so the gap closes structurally. The safety net firing IS the methodology working. Anti-patterns: bypassing the net to hide the gap (`--force` instead of merging); learning lives only in §11 prose; same catch never formalized. → `KB § 01-PHILOSOPHY.md § Safety nets capture failures; failures become learnings; methodology evolves` + `KB § PATTERNS/project-execution.md § 2.11 Phase enrichment loop`
- **Always-hardening posture — every surfaced pattern is a methodology-improvement opportunity (proactive ∧ global).** The proactive generalization of safety-nets: the methodology is **never finished**, the agent is **always watching for hardening opportunities** every turn (not only when a net fires). "Pattern" is read widest — failure mode, recurrence, friction, new collision shape, **a success worth reproducing**, **and explanation-as-signal** (a concept you had to *explain* / a "these sound similar" or "how is X different from Y" moment = a doc/naming/abstraction/test gap — listen to your own explanations AND the user's confusion-shaped questions). Default = learn → absorb (route s1→s4 codification + three-way sync); uncodified surfaced pattern = silent-error shape. **Taxonomies ship open/self-extending** (a non-fitting instance → add the class, never force-fit/ignore). **Training contract — LOUD surfacing:** every execution is a training pass; a spotted improvement is (1) **announced loudly in-the-moment** (scannable marker `**Methodology improvement spotted**` so the user is always aware *when it happens* — never a silent fold-in), (2) **implemented before the in-flight work ships** (applicable-deferral = silent-error shape), (3) three-way-synced same session. Silent-but-correct self-improvement breaks the contract — visibility is a first-class deliverable. The umbrella that safety-nets, the codification pipeline, MCP/AST bystander-flagging, and the recurrence rule are all special cases of. → `KB § 01-PHILOSOPHY.md § Always-hardening — every surfaced pattern is a methodology-improvement opportunity`
- **`main` is production; `dev` is the integration layer — everyday work + pushes land on `dev`, never `main`.** `main` = the deployed/shippable line: NEVER push/merge without explicit per-action consent, only to deploy (`dev → main`, FF-only); the pre-push hook blocks `git push … main` unless `NOCTUS_ALLOW_MAIN_PUSH=1` (sanctioned-deploy override). `dev` = persistent integration branch ∧ GitHub default — commit/merge/push your own work freely. Dispatched worker branches fork FROM `dev`, merge back TO `dev`; **always return the primary checkout to `dev`** after inspecting any branch. Adopted from knowledge-extractor's `main`+`methodology-dev` split (2026-05-23). → `KB § PATTERNS/branching-and-merging.md § 0` + `KB § PATTERNS/branching-dispatch.md`
- **Branching-first orchestration — parallelize by default; orchestration STAYS with the orchestrator.** **The orchestrator IS the architect** (plans + dispatches + evaluates + stays available to the user); **subagents ARE engineers** (focused briefs, narrow context, isolated worktrees, build + report). Default mental model: "can this chunk into parallel branches?" — if yes, `git worktree add` per engineer + dispatch in a single `Task` turn + maintain findings.md; if no, serial (justified, logged) or master-tree-parallel-batches for N≥2 same-shape. **Inline cutoff (NEW 2026-05-20)**: split-by-default presumes the work amortizes the engineer's ~45–60k contextualization tax — **below `<100 LoC ∧ <3 files ∧ single-phase` the architect does it inline**; 2+ small file-disjoint tasks ride ONE compound brief. Estimate at *dispatch time* by opening the files (same evidence rule as session-sizing). **Engineers execute, never plan orchestration.** **Engineer findings evaluated locally + applicable fixes applied SAME SESSION** (three-way sync); deferring an applicable fix = silent-error shape. **Concurrent agents NEVER share one checkout** — one worktree+branch per concurrently-active agent; the shared primary tree's branch is owned by exactly ONE driver, a peer never `git switch`/`reset`s it under another (the 2-day-chaos root, 2026-05-19); reflog is truth, commits never lost (recover by switch-back/cherry-pick, never re-do). → `KB § 01-PHILOSOPHY.md § Branching-first orchestration` + `§ Roles: Architect + Engineers` + `§ Knowledge tracking — durable findings file` + `KB § PATTERNS/branching-dispatch.md` (the actionable parallel-agent RUNBOOK — 10-step protocol incl. **semantic-duplicate** collision detection + the honest `--no-ff` reconciliation commit; absorbed from knowledge-extractor 2026-05-23) + `KB § PATTERNS/branching-and-merging.md § 16` + `§ 18.2.1 Inline cutoff` + `§ 9a`
- **Knowledge tracking — durable findings file for any non-trivial work.** Any non-trivial project/feature/orchestration keeps a root `findings.md` (5 categories: slips/errors/mistakes/lessons/knowledge). Append in-the-moment; synthesize at close. Default-on for projects + master-trees + parallel-orchestration; trivial fixes may skip but **log the absence** to `phase_learnings` SQLite (silent skip forbidden). Distinct from `phase_learnings.db` + §11 (findings.md = what-we-LEARNED, curated). **Architect briefs MUST carry an explicit findings.md/PROJECT.md/proposal Write-authorization** — engineers inherit a no-`.md` default and refuse without it. **In-flight processing (¬ defer-to-retro):** findings.md ∧ every in-flight AI↔AI comms artifact (`.claude/dispatcher.md` Inbox/Outbox · `live-patterns-log.md` · engineer return-notes · in-flight `proposals/*`) is a capture surface, ¬ a parking lot — entries with a clear resolution/action path are processed in the **same commit/checkpoint** (commit-on-ship §21.4 / §2.13), never stale-for-later; *appended ≠ deferred*. → `KB § 01-PHILOSOPHY.md § Knowledge tracking` + `KB § PATTERNS/branching-and-merging.md § 17.6` + `KB § PATTERNS/project-execution.md § 2.13`
- **Wave-based dispatch + pause-on-dependency + scoped-team economics.** Group chunks by dependency-depth: Wave N+1 dispatches only after every Wave N chunk **FF-merges** (not just reports). Engineer surfaces a mid-flight gap → **pause** chunk, **dispatch a focused dependency team**, **resume** on merge (engineers never absorb dependencies). **Split rather than combine** — a 200-LoC focused brief beats a 2000-LoC one; token cost acceptable, quality is the constraint. **Collision-class branching (merge cleanliness decided at DISPATCH, not merge)**: classify the branch's edit-set vs the parallel-active file set — **C1 file-disjoint** → parallel-clean; **C2 same-file-additive** → brief the engineer additive-only (new files + appended lines, zero restructure) ⇒ §10.4 concat-clean by construction; **C3 substantive-overlap** → re-scope to a parallel-clean sibling file OR sequence. Class determined evidence-based (`git diff --name-only` the parallel set, not assumed). Base is parallel-liveness-dependent (active → off `origin/dev`; done → rebase worktree onto the stabilized tip) — `dev` is the integration base, not `main` ([[branching-and-merging]] §0.1). Commit-on-ship per isolated-branch deliverable; architect true-disk-verifies before each per-branch commit; converge least-conflict-first, gated on the last in-flight chunk. → `KB § PATTERNS/branching-and-merging.md § 18` + `§ 21`
- **Pilot-products-first refactor cadence.** A seed/shared-lib/framework change does NOT refactor all ~10 products in one wave. Prove it on 3 canonical pilots — **`erp-imobiliario` · `therapy-platform` · `social-wiring`** (+ `core` if control-plane) — then non-pilots extend in a **later wave gated on pilots-green**. Deferring a non-pilot here is the rule, not a silent error (destination = the extend wave); don't leave partial non-pilot adaptations. Recurrence/formalize-to-seed still fires; this only scopes validation fan-out. → `KB § PATTERNS/project-execution.md § 2.12`
- **No silent errors — always explicit fix opportunities.** No `except: pass`, no silent degraded fallbacks, no deferred items without a named destination, no "verification ✓" when the tail showed red. Ambiguity is a silent error — ask. Absence of findings is a claim — quote the command that confirms it. → `KB § 01-PHILOSOPHY.md § No silent errors`
- **Doc-propagation sync — KB ↔ CLAUDE.md ↔ memory ↔ tool-code move together.** Any rule/methodology/behavior change lives in **all three doc layers same session** — ordering: KB-first → CLAUDE.md (or topical) pointer → memory entry + MEMORY.md line; amending an existing rule touches all three. Extends to **tool-code ↔ doc-prose**: when a tool (script/MCP/keeper/CLI) changes behavior (flag/mode/rename/severity) every referencing doc updates in the **same commit** (KB patterns, Situation→Tool maps, CLAUDE.md pointers, INDEX.md, `--help`, product README/MASTER-PROMPT) — "later" forbidden; discovery `grep -rn "<tool-name>" KNOWLEDGE-BASE/ CLAUDE.md CLAUDE/ products/*/README.md`. `verify-kb-sync.sh` catches dangling KB↔CLAUDE.md pointers, not missing memory entries (agent discipline). → `KB § 01-PHILOSOPHY.md § Docs stay in sync` + `KB § PATTERNS/methodology-codification-pipeline.md § 8 (Situation → tool map)`
- **Durable docs are self-contained — never anchor knowledge to `projects/`/`archive/`.** KB/`CLAUDE.md`/memory outlive projects; `archive/` is not persisted long-term, so pointing a durable doc at `archive/projects/<…>` becomes a dangling pointer to lost knowledge. **Inline the durable substance**; reference code/files + dated facts, never project slugs or archive paths. Sanctioned durable indices: `project-history/ledger.ndjson` + the accept-with-rationale catalog. → `KB § 01-PHILOSOPHY.md § Durable docs are self-contained`
- **Symbol-first when authoring dense docs.** When authoring/refactoring dense docs **or AI-intended files** (MASTER-PROMPTs, CLAUDE.md, KB patterns, memory bodies; **AI scaffolding** — whole PROJECT.md + `proposals/*.md`, `findings.md`, the dispatcher coord-file, `live-patterns-log.md`, dispatch briefs + `.claude/agents/*.md`; §1 framing / §2 quoted-user stay prose; from-now-on, existing not retrofitted) use the doc-symbology glossary by default: `∧ ∨ ¬ ⇒ ↔` (logic) · `✅ ⏳ ❌ 🔒 📋 🗑 ⭐` (status) · `s1/s2/s3/s4` · `[F]/[R]/[A]` · `N≥3 Δ Σ`. One fixed meaning/symbol; lossless-swap test gates each swap. NOT for: error messages, first-paragraph, quoted user, bug-fix comments, commits. `→`=routes; `⇒`=implies (never interchange). **This is the standing pattern every future doc edit follows** — caveman-validated; `check_doc_symbology_drift` scans all symbol-eligible docs against the live glossary (platform baseline: zero-drift). → `KB § PATTERNS/doc-symbology.md`
- **Finish the session — verify, don't assume.** End-of-session checklist: `cd products/<touched>/frontend && npx vite build`; `cd products/<touched>/backend && pytest`; `cd mcp/noctusai && pytest tests/` if MCP-toolkit changed; report any regression. Don't mark "done" while a build or test is red. *Every in-session change must land on green.* **Dev↔prod parity (the layer above freshness):** a change isn't done until verified in the *production shape* (slim image / live VPS), not just dev-green — the slim prod image ships **no `start.sh`/registry/node + baked dist + env-only config**, so dev-green ≠ prod-correct (code deriving from a dev-only artifact, or a dev-convenience default, silently breaks in prod; the platform's highest-recurrence drift class, N≥3). → `KB § PATTERNS/dev-prod-parity.md`. **Container-freshness extension:** if a product container is running AND this session changed code that container serves, also confirm runtime parity (bind-mount source = edited repo; uvicorn watch set covers the changed dir; restart/rebuild if not; live-probe the fixed endpoint) — "tests green" ≠ "running container reflects the fix." Bit 2026-05-20 social-wiring (13h uptime + `noctus-fleet/` mount drift). → `KB § PATTERNS/containerization.md § 12b Validation-freshness contract`
- **Never auto-commit or push, except project gates; commit + push only your own work.** Two carve-outs: (a) end-of-phase local commit (no push); (b) final commit + `git push` **to `dev`** at project close (literal last step, after folder deletion) — never to `main` (deploy-only, §0.2). `git status` first; never `git add .` / `-A`. Stage only files YOU authored this session — explicit-path `git add` does NOT validate authorship. Verify via `git diff --cached --name-only` before commit, `git log origin/<branch>..HEAD` before push; surprise entries → unstage / ask. **Terminal commit-guarantee:** a session never ends with own-authored work uncommitted/unhanded-off in the shared primary tree (residue forces next-agent reconciliation) — `git status` sweep before "done"; every own entry committed ∨ handed-off-with-destination ∨ restored-with-rationale. → `CLAUDE/projects.md § Commit per phase, push at project close` + `KB § PATTERNS/project-execution.md § 2.10` + `.claude/agents/engineer-default.md § 2a`
- **Context budget discipline.** `CLAUDE.md` (this file) = router. `CLAUDE/<topic>.md` = topical rules, read on-demand by agent discipline (see §3). `KNOWLEDGE-BASE/` = depth. **MCP keep-list**: `noctusai` + `supabase` + `n8n` + `waha` (self-hosted workflow + WhatsApp ops, user-approved 2026-05-19) — anything else needs explicit user approval. **Skills keep-list**: `update-config` / `loop` / `schedule` / `security-review` only. New rule → KB-first → CLAUDE.md (or topical) pointer → memory. New §1 bullet >80 words → trim and push depth to KB. → `KB § 01-PHILOSOPHY.md § Context budget discipline`
- **Lossless doc-refactor — changing the doc-set itself is methodology surgery.** Any compact / restructure / merge-rules / fold-in-external on CLAUDE.md / KB / MEMORY.md / `.claude/agents` / MASTER-PROMPTs follows the framework: lossless **proven not asserted** via diff gates (pointer-set ∧ rule/section ∧ line-structure ∧ index-pointer ∧ hook-grade); gated-aggressiveness ladder (s-lossless no-approval / s-moderate show-diff-first / s-plan-only); trim only what the pointer target provably carries (registers ≠ prose); **always-doc-the-trim** — every trim/de-drift/de-dup leaves a tracked in-place provenance breadcrumb (when·why·what·source-now), never silent (else a future agent re-grows the drift — the AGENT-CONTEXT roster failure); hand-data duplicating an auto-source = drift generator ⇒ trim-to-pointer is the root fix; symbol-first scan-enforced (zero-drift); file-disjoint-or-inline parallelization w/ architect true-disk 100%-certification; fix-on-contact; three-way-sync close. The trigger is the task class, not the file. → `KB § PATTERNS/lossless-doc-refactor.md § 3a`
- **Sibling workspaces consume noc read-only, whole — never modify, never trim.** A template/seed workspace is a noc sibling consuming 8 noc surfaces via read-only symlinks. **Cannot modify noc** — three-layer defense: (1) PRIMARY template pre-commit refuses commits touching symlinked paths or non-sandbox additions without a `.promotions/<slug>.md`; (2) AGENT this rule + KB + memory; (3) SYMBOLIC chmod -h a-w. Edits to symlinked surfaces belong in noc; additions go through the promotion manifest + `noctusai_promote_from_template`; `PROMOTIONS.md` is **derived** (never hand-maintain). **Inherit whole, trim none** — auto-load is already minimal (`CLAUDE.md` router; KB on-demand; `MEMORY.md` index); trimming breaks seed-first analysis, recurrence scans, KB pointer integrity (`verify-kb-sync.sh`), three-way sync. Per-product focus → `products/<slug>/MASTER-PROMPT.md`; the workspace IS `noc + product`, not `subset(noc) + product`. → `KB § PATTERNS/template-workspace.md` + `KB § PATTERNS/seed-workspace.md § Promotion manifest` + `KB § PATTERNS/seed-workspace.md § Why the inherited surface is not trimmed`
- **Divergent-architecture absorptions refactor to the house container model.** An incoming externally-developed product whose container architecture differs from noc's single-container house model MUST be refactored to the house shape on absorption — one container, uvicorn serving API+SPA via `serve_spa`, `FROM noctus-seed-*-base` seam, two compose projects on external `noctus-net`. No fleet carve-out. Newly-scaffolded products inherit by construction (fires only for *absorptions*). Fold only still-relevant divergent infra. → `KB § PATTERNS/containerization.md § 12a` + `KB § GUIDES/absorb-seed-workspace.md`
- **Parallel-agent collision protocol — STOP, wait, continue.** When a shared-file edit is reverted by another agent's work AND re-applying would loop, do not loop-fight: STOP after the second revert. **Do NOT file a collision-report project** — wait for the parallel agent to finish. Continue with non-colliding deliverables; catalog the deferred work in `accept-with-rationale.md`. Surface in the end-of-work summary; re-apply when the user signals or the parallel project closes. N=2 collisions on different files same session → pause + surface to user. → `KB § PATTERNS/project-execution.md § 2.9`

---

## 2 · The Map

Pointers into `CLAUDE/<topic>.md` and `KNOWLEDGE-BASE/`. Open *on-demand*. If nothing here matches, open `KNOWLEDGE-BASE/INDEX.md`.

### Topical behavioral rules (sibling files; read by agent discipline)
- Backend rules — `CLAUDE/backend.md`
- Frontend rules — `CLAUDE/frontend.md`
- Project-execution rules — `CLAUDE/projects.md`
- Cross-cutting platform rules (MCP toolkit, MCP-first, clean folder, LGPD-first) — `CLAUDE/platform.md`

### Architecture & context (KB depth)
- Product landscape (products, schemas, ports, stack) → `KB § 02-LANDSCAPE.md`
- Seed framework (factories, layer split, how products inherit) → `KB § 03-SEED-ARCHITECTURE.md`
- Shared library catalog → `KB § 04-SHARED-LIBRARY.md`
- Infrastructure (ports, deploy, self-hosted services) → `KB § 05-INFRASTRUCTURE.md`
- MCP dev toolkit → `KB § 06-AGENTS.md`
- Gamification philosophy → `KB § 07-GAMIFICATION.md`

### Patterns (KB depth)
- Backend patterns → `KB § PATTERNS/backend.md`
- Frontend patterns → `KB § PATTERNS/frontend.md`
- Testing → `KB § PATTERNS/testing.md`
- Database & RLS → `KB § PATTERNS/database-rls.md`
- Environment / `.env` → `KB § PATTERNS/environment.md`
- Notifications → `KB § PATTERNS/notifications.md`
- Shared-library conventions → `KB § PATTERNS/shared-library-conventions.md`
- Project execution → `KB § PATTERNS/project-execution.md`
- Proposals & improvements → `KB § PATTERNS/proposals-and-improvements.md`
- LGPD awareness → `KB § PATTERNS/lgpd.md`
- LLM usage tracking → `KB § PATTERNS/llm-usage.md`
- Logging convention → `KB § PATTERNS/logging.md`
- Seed-lib layout (6 layers) → `KB § PATTERNS/seed-lib-layout.md`
- Seed canonical defaults (seed fallback literals must be the architectural canonical answer, never a consumer-#1 coincidence; paired multi-stage Dockerfile inheritance rule; worked examples table + N=2 codification status) → `KB § PATTERNS/seed-canonical-defaults.md`
- Dev↔prod parity — verify in the PRODUCTION SHAPE, not just dev-green (the DISCIPLINE; executable form = deploy-config-contract): the slim prod image is STRUCTURALLY different (no `start.sh`/registry/`node`, baked `dist`, env-only config) ⇒ code deriving from a dev-only artifact or a dev-convenience default silently breaks in prod; the platform's **highest-recurrence drift class** (N≥3); §2 dev↔prod difference checklist + §3 authoring-time parity question + §4 deploy-time live-probe; special cases = seed-canonical-defaults (wrong value) / boundary-contract-tests B4 (env propagation) / containerization §12b (stale container); bit chain 2026-05-20 infra.tsx + 2026-05-22 nav-localhost + CORS-registry-empty-in-slim → `KB § PATTERNS/dev-prod-parity.md`
- Deploy-config contract (EXECUTABLE form of dev↔prod-parity: every dev↔prod-divergent config knob routes through the seed primitive `noctusai_lib.config.deploy_config` — `resolve_config`/`require_prod_config`/`is_deploy_context`; canonical default ∧ fail-loud-if-required-in-prod, no-op in dev ⇒ a product can't silently ship a dev value to prod; startup guard in `create_product_app` + `check_derives_from_dev_only_artifact` keeper; value-correctness sibling = seed-canonical-defaults) → `KB § PATTERNS/deploy-config-contract.md`
- Boundary-contract tests (named class for "tests-green-dashboard-red" bugs — seven recurring boundaries B1-B7: build-injection / HTTP schema / third-party library contract / container env / library-default propagation / E2E-harness env injection / fixture-vs-real-schema; authoring-time discipline + per-boundary detector status; Stage-4 keepers — `check_query_fn_returns_undefined` covers B3 TanStack v5 contract (`queryFn` returning `undefined` → "data is undefined"); `check_playwright_supabase_env` covers B6 (playwright `webServer.env` must inject `VITE_SUPABASE_*` or the seed supabase client throws → blank SPA → every E2E spec fails; local↔CI parity gap, N=2 core+erp)) → `KB § PATTERNS/boundary-contract-tests.md`
- AST-driven code edits → `KB § PATTERNS/ast.md`
- Agent reading & research discipline → `KB § PATTERNS/agent-reading-discipline.md`
- Webhook signature verification → `KB § PATTERNS/webhook-signatures.md`
- Accept-with-rationale catalog → `KB § PATTERNS/accept-with-rationale.md`
- MCP tool conventions (dotted naming, Pydantic schemas, hierarchical registration, lazy context) → `KB § PATTERNS/mcp-tool-conventions.md`
- MCP-first scripts (new automation → `noctus.dev.*` tool not a `scripts/` one-off; 3 named carve-outs: git-hook entry / pre-venv bootstrap / thin docker-orch; keeper-parsed classification manifest) → `KB § PATTERNS/mcp-first-scripts.md`
- LLM tool-call audit (`tool_call_audits` per-product table, `AuditRecord` + `make_audit_writer`, LGPD redaction) → `KB § PATTERNS/llm-tool-audit.md`
- LLM bot security (sanitize / validate / rate-limit trio, confirm-then-execute, prompt-injection mitigation) → `KB § PATTERNS/llm-bot-security.md`
- Template workspace (sibling consume-only workspace; "templates can't modify noc" rule + 3-layer defense + promotion manifest) → `KB § PATTERNS/template-workspace.md`
- Scheduling primitive (`noctusai_lib.domain.scheduling` — engine + Conflict/Scorer/TravelLookup Protocols + defaults + wiring recipe) → `KB § PATTERNS/scheduling-seed.md`
- Metas / goals primitive (`noctusai_lib.domain.metas` — Goal/Target/Progress/Period value objects + GoalStatus/PeriodKind enums + compute_progress / accumulate_contribution / period_bounds / proportional_target / next_status pure fns + GoalRepository Protocol; lifted from PF/ERP/daily-life N=3) → `KB § PATTERNS/metas-seed.md`
- WhatsApp connector + chatbot framework (`noctusai_lib.integrations.whatsapp` + `noctusai_lib.domain.chatbot` + `noctusai_lib.integrations.{google_calendar,google_maps}` — WAHA parser/sender/router + Redis-backed buffer/worker/LLM-dispatcher + Calendar/Maps adapters; wiring recipe) → `KB § PATTERNS/whatsapp-chatbot-seed.md`
- Digest service base class (`noctusai_lib.domain.digest.BaseDigestService` template-method base — window → fetch → aggregate → narrate → render → result orchestration shared; 4-adopter cluster — core/audit, daily-life/weekly-review, social-wiring/email-marketing-debrief, PF/narrative; non-fits documented) → `KB § PATTERNS/digest-seed.md`
- Master-tree parallel batches (multi-product orchestrator: same-shape phases across N children execute as synchronized batches; live cross-pollination via shared scratchpad; sync-gates pre/mid/post; divergent-batch carve-out) → `KB § PATTERNS/master-tree-parallel-batches.md`
- noc-graph — queryable knowledge graph of the platform (`noctusai_lib.graph` + `noctus.graph.*` MCP umbrella + `.noc-graph/{graph.json,graph.html,REPORT.md}` derive-only cache; AST + authored-prose only, **no LLM-inference layer** in v1 — rationale already lives as durable prose; vis-network interactive UI with search/filters/click-to-expand/path-mode/animated effects; ~6-7s end-to-end on the full noc workspace, ~20k nodes / ~32k edges; inspired by [graphify.net](https://graphify.net/), noc-native because the implicit graph is already authored across KB + memory + AST) → `KB § PATTERNS/noc-graph.md`
- agno multi-agent dev team (engine at `dev_team/` + product at `products/dev-team/`; MCP exposure `noctus.team.*` — run/status/route/metrics/agent_metrics/configure; 11 specialists + 3 sub-teams; per-turn telemetry; "switch flip" UX gated by `ANTHROPIC_API_KEY`) → `KB § PATTERNS/dev-team.md`
- Containerization (**SINGLE container per product** — uvicorn serves API + built SPA on one port via the seed factory `serve_spa`/`SERVE_SPA_DIR` seam; **shared seed base images** `noctus-seed-{backend,frontend}-base` + **thin per-product Dockerfiles** that `FROM` them, `FROM base` IS the named seam — built by `scripts/infra/build-base-images.sh`, propagated by `scripts/propagate-{dockerfiles,composes}.sh`; **two compose projects** — `noctusai-products` (`docker-compose.yml`, one container/product = one Docker-Desktop 1-click row) + `noctusai-infra` (`docker-compose.infra.yml`, Redis/WAHA/Postgres); `noctus-net` is **external** (created once by `start.sh`; the prior "NOT external:true" rule is INVERTED by the two-project split); same-origin SPA via vite-factory `window.location.origin` define-injection (tunnel/deploy-correct, zero consumer changes); **mandatory profile-gated `<slug>-tunnel`** on every product (wired-in, run-on-demand) — quick-tunnels MUST pin `--protocol http2`; **NO dev/prod split — ONE container, ONE shape always**: the local per-product compose builds the Dockerfile `runtime-watch` target with source bind-mounted → `vite build --watch` + `uvicorn --reload` give live feedback in the *same single container* (no sidecar, no `dev` command, no separate project); deploy/CI build the default slim `runtime` target (baked dist, node-absent — the shippable image); `./start.sh` whole fleet · `./start.sh <slug>...` subset · `tunnel`/`native` retained; canonical at `products/seed/`; **cold-boot is staggered** — `staggered_up()` in `start.sh` brings products up in core-sized health-gated waves (`NOCTUS_BOOT_BATCH`/`NOCTUS_BOOT_WAVE_TIMEOUT`) so N parallel first-boot `vite build`s don't oversubscribe CPU, one-time tax, §6a; **container-first §1a** — develop INSIDE the container via the `runtime-watch` target is the default loop, containerize-last is the dev↔prod-parity #1-drift anti-pattern, applies at product birth [scaffold copies the seed template / absorption refactors at Gate 9]; **`check_product_container_shape` keeper** enforces every in-noc product carries the house Dockerfile/compose) → `KB § PATTERNS/containerization.md`
- Containerization **OPERATIONS** (runbook + methodology sibling to the architecture above: §1 source-of-truth chain (git→file→manifest→history→inspect-mounts→exec→logs; DD is NEVER truth) · §2 operational primitives (force-recreate after compose-volume edits; build/runtime verify; disk diagnostics; multi-arch check) · §3 the codified bumps (livelock · chown-R-inherited-base · SPA-race · amd64-emulation · DD-not-truth · compose-volume-silent-reuse · base-cascade · pip-vs-base-drift · clean-checkout-reproducibility · glibc-vs-alpine-FE · lockfile-platform-optionals · source-Python-deps · QUIC-tunnel · pre-commit-foreign-absorption-FIXED · MCP-fixed-CWD · §9a-shared-checkout · ECONNRESET-npm-retry · parallel-engineer-arch-worktree-slip — count grows as bumps surface, don't hand-cite a number) · §4 diagnostic flowchart A-G · §5 8-step safe-change methodology · §6 meta-arc (every bump = symptom→first-pass→recurrence→root→3-way-sync). **Reach here BEFORE re-diagnosing any container failure** — most of the 2-day-chaos session was the §1 chain skipped) → `KB § PATTERNS/containerization-operations.md`
- Pydantic strict-by-default at HTTP boundary (`noctusai_lib.api.StrictHttpModel` — `extra="forbid"` → 422 on unknown keys; defends against silent-drop misroutes; ConfigDict MRO-merge; carve-out via explicit `extra="allow"`; per-product migration recipe) → `KB § PATTERNS/pydantic-strict-http.md`
- Chatbot operational readiness (production-hardening checklist for LLM chatbot products: retries on transient external writes via `retry_call` composing seed `RetryPolicy` + structured logs auto-wired by `create_product_app` + health endpoint via `standard_routers=["health"]` + `DEPLOYMENT.md` shape + Supabase managed backups + metrics-sink seam with `NoopCounter` default; first adopter was imobi-scheduling, absorbed into `social-wiring/scheduling` 2026-05-16; therapy/PF inherit at N=2) → `KB § PATTERNS/chatbot-operational-readiness.md`
- Methodology codification pipeline — the 4-stage path that turns conversation rules into deterministic keeper detectors (Stage 1 emerges → Stage 2 memory entry → Stage 3 KB pattern doc + CLAUDE.md pointer → Stage 4 `check_*` keeper detector with colocated test). Names the keeper as the **codification layer** of the methodology (not a regulatory silo); codification criteria (deterministic predicate + recurrence ≥3 + clear remediation); 5 worked examples (LGPD / webhook 5-pin / status-assertion / slowapi-pep563 / hygiene-compliance); the rules that legitimately stay at Stage 3 (judgment-dependent / context-dependent / methodology-in-pilot / aesthetic). When a new rule emerges, route deliberately through the stages instead of letting it stall at memory. → `KB § PATTERNS/methodology-codification-pipeline.md`
- Dev toolkit — codegen scaffolders + orchestration tools (7-tool `noctus.dev.*` program; **reach for these before hand-writing the shape or re-running the ritual**): `scaffold_{seed_adapter,mcp_tool,keeper,memory}` emit consumable canonical boilerplate (return code, never write files; every emitted skeleton gated by an `ast.parse` test) · `salvage_worktree`/`dispatch_preflight`/`findings` operationalize R6 (overlay-divergence salvage) / R2+R4+K-1+K-3 (fork-base + collision + env-pin-drift + project-doc-phantom preflight; pass `project_slug=<slug>` to auto-include `projects/<slug>/PROJECT.md` in wrap_paths so locally-committed-but-unpushed docs fail loud; `check_env_pins=True` diffs `seed/lib/backend/pyproject.toml` strict caps against the active interpreter's installed versions) / the findings discipline. Stage-4-as-tooling for *process* shapes — the analogue of keeper `check_*` for *code* shapes. → `KB § PATTERNS/dev-toolkit-scaffolders.md`
- Doc symbology — lossless compression glossary (`∧ ∨ ¬ ⇒ ↔ ∈ ⊂ ≡ ≠ ≈`, status icons `✅ ⏳ ❌ 🔒 📋 🗑 ⭐`, codification stages `s1/s2/s3/s4`, triage outcomes `[F]/[R]/[A]`). Contract: every symbol = one fixed meaning; swap prose → symbol only when lossless. High-leverage on MASTER-PROMPTs / CLAUDE.md / KB patterns / PROJECT.md headers. NOT for error messages, first-paragraph context, quoted user instructions, or bug-fix code comments. **caveman-skill provenance** (validated ~61-75% token-cut): adds a prose-discipline layer + lite/full/ultra intensity ladder + `DB/auth/config/req/res/fn/impl` abbreviations; `→`=routes stays locked (rejects caveman `→`=causal). Conformance is **enforced** by `check_doc_symbology_drift` (live-parses the glossary; platform baseline: zero-drift). → `KB § PATTERNS/doc-symbology.md`
- Lossless doc-refactor — the standing framework for changing the durable doc-set itself (compact / restructure / merge-rules / fold-in-external on CLAUDE.md / KB / MEMORY.md / `.claude/agents` / MASTER-PROMPTs): 5 lossless-proof diff gates (pointer-set / rule-section / line-structure / index-pointer / hook-grade), gated-aggressiveness ladder, source-of-truth trim rule, symbol-first zero-drift enforcement, file-disjoint-or-inline parallelization with architect true-disk certification, fix-on-contact, three-way-sync close-out; the doc governs itself. Born from the 2026-05-18 compaction session. → `KB § PATTERNS/lossless-doc-refactor.md`
- Compliance regression-baseline gate — the platform-health gate is **regression semantics** (no NEW high/critical vs the committed `mcp/noctusai/tests/compliance_baseline.json` fingerprint set), NOT `score==100`; absolute score is informational (printed, never asserted) because every new keeper detector retroactively surfaces pre-existing debt; line-churn-robust `product|file|severity|symbol` fingerprint; env-artifact classes (seed-drift / archive- / dispatcher-staleness) excluded both sides; deterministic `refresh_compliance_baseline.py` refresh contract (shrink-on-resolve / grow-only-with-cited-triage, never to silence a regression). Option A of the 2026-05-18 platform-compliance decision. → `KB § PATTERNS/compliance-regression-baseline.md`
- DI-test seam — the named remediation convention for the `test_patch_target` / self-monkeypatch compliance class: never `patch`/`monkeypatch` our own symbols; 3 legitimate seams (DI default · `MockRequestBuilder.inserted_payloads` read-side · external-boundary-only); thin pointer to the full `testing.md` refactor playbook. Drains the regression baseline. → `KB § PATTERNS/di-test-seam.md`
- Logging-at-except — the named remediation convention for the silent-except compliance class: every `except` logs ∨ raises ∨ returns-error-bearing; no `# silent-ok` escape hatch (retired 2026-04-28); level guide; thin pointer to the full `logging.md` rationale. Drains the regression baseline. → `KB § PATTERNS/logging-at-except.md`
- GitHub connector MCP — the **methodology PR/CI surface** (`mcp/github` composes `mcp/_kit`, wraps the authenticated `gh` CLI: `github.pr.{list,view,diff,checks,create,ready}` + `repo.view` + `diagnostics.connection_status`; writes confirm-gated 412; `gh`-absent = typed never-faked 424). Raw `git commit`/`git push` deliberately NOT exposed — they stay in the commit-only-your-own-work git workflow. `.mcp.json` registration is **user-gated** (MCP keep-list rule). → `KB § MCP-SERVERS/github.md`
- Absorbed-product seed-shape-seam — the recurring "absorbed product's **pre-existing concrete shape** (DDL columns · registered external URL · security control · denormalized field) ⊥ seed primitive's default contract" pattern (N≥3 in `social-wiring-google-seed-consume` alone: `token_store` metadata seam + `oauth_router` prefix seam + `GoogleProvider` PKCE seam ⇒ MUST formalize per DRY recurrence). **Seed-first resolution**: extend the seed primitive with a **back-compat-defaulted shape-config parameter** (defaults reproduce today's exact behavior; Fake mirrors Real; pilot-gate verified no-op). **Forbidden anti-patterns**: degrade the consumer (drop PKCE / denormalized cols), fork in the product, silent-schema-migrate to fit the seed. Granular sibling of `KB § GUIDES/absorb-seed-workspace.md` — fires INSIDE an absorption when a specific shape mismatch is hit. Canonical body (no duplication elsewhere). → `KB § PATTERNS/absorbed-product-seed-shape-seam.md`
- n8n connector MCP — the **self-hosted n8n workflow-ops surface** (`mcp/n8n` composes `mcp/_kit`, wraps the n8n public REST API via the stdlib-`urllib` `n8n.api.request_json` seam: `n8n.workflow.{list,get,activate,deactivate,update,create,delete,set_tags}` + `n8n.execution.{list,get,delete}` + `n8n.tag.list` + `n8n.credential.{create,delete,schema}` + `n8n.diagnostics.connection_status`; writes confirm-gated 412; no-config/unreachable/rejected-key = typed never-faked 424/502/4xx). Exists because the `claude.ai`-managed n8n connector can't reach a self-hosted instance. API key (non-expiring JWT) lives in the connector's own `mcp/n8n/.env` (gitignored), NOT the product/root `.env` (every-connector-owns-its-auth-store). `n8n.workflow.update` is hard-to-reverse — always `get` first as a rollback snapshot. `n8n.credential.*` lets workflows stop hard-coding secrets inline — n8n stores secrets write-only by design so there is deliberately NO list/get-credential tool (gated-capability honesty). On the **MCP keep-list** (user-approved 2026-05-19) ∧ registered in `.mcp.json`. → `KB § MCP-SERVERS/n8n.md`
- WAHA connector MCP — the **self-hosted WhatsApp (WAHA) ops surface** (`mcp/waha` composes `mcp/_kit`, wraps the WAHA HTTP API via the stdlib-`urllib` `waha.api.request_json` seam, header `X-Api-Key`, no `/api/v1` prefix: `waha.session.{list,get,me,start,stop,restart,logout}` + `waha.message.{send_text,list}` + `waha.chat.list` + `waha.server.{version,status,ping}` + `waha.diagnostics.connection_status`; writes confirm-gated 412 — `send_text` strongest gate (outward-facing/irreversible), `logout` hard-to-reverse; `server.ping` unauthenticated). Drives the WhatsApp side of n8n flows; the diagnostic disambiguates the WAHA dashboard's conflated host-down-vs-bad-key error (configured/reachable/authenticated tri-state). Secret (`X-Api-Key`) in `mcp/waha/.env` (gitignored), NOT product/root `.env`. On the **MCP keep-list** (user-approved 2026-05-19) ∧ registered in `.mcp.json`. → `KB § MCP-SERVERS/waha.md`
- Hostinger connector MCP — the **VPS-ops surface** (`mcp/hostinger` composes `mcp/_kit`, wraps the Hostinger Developers API (Bearer, base `https://developers.hostinger.com`) via the stdlib-`urllib` `request_json` seam — which sends a **browser `User-Agent`** to clear Cloudflare's WAF: `hostinger.vps.{list,get,metrics,actions}` + confirm-gated `hostinger.vps.{start,stop,restart}` (`stop` strongest) + `hostinger.diagnostics.connection_status`; no-token/network/401 = typed never-faked 424/502/auth-false). Built during `production-deploy-migration` to operate the deploy-target VPS. Token in `mcp/hostinger/.env` (gitignored, every-connector-owns-its-auth-store). `.mcp.json` registration **user-gated**. → `KB § MCP-SERVERS/hostinger.md`
- Cloudflare connector MCP — the **DNS/zone/tunnel-ops surface the managed CF MCP lacks** (`mcp/cloudflare` composes `mcp/_kit`, wraps CF API v4 (`https://api.cloudflare.com/client/v4`, Bearer) via stdlib-`urllib`, CF-envelope-aware (`success:false`→typed `cf_code`): `cloudflare.zones.{list,get,create}` + `cloudflare.dns.{list_records,get_record,create_record,update_record,delete_record}` + `cloudflare.tunnel.{list,get,get_config,create,delete,update_config}` + `cloudflare.diagnostics.connection_status`; all 7 writes confirm-gated 412, `dns.delete_record`+`tunnel.delete` strongest/IRREVERSIBLE). Scoped token + `CLOUDFLARE_ACCOUNT_ID` in `mcp/cloudflare/.env` (gitignored); v2 = Email Routing + Registrar. `.mcp.json` registration **user-gated**. → `KB § MCP-SERVERS/cloudflare.md`
- Supabase connector MCP — the **Management-API run-SQL-from-home surface** (`mcp/supabase` composes `mcp/_kit`, wraps the Supabase Management API (`https://api.supabase.com`, Bearer PAT) via the stdlib-`urllib` `supabase.api.request_json` seam, NO envelope/NO WAF-UA: `supabase.project.{list,get}` + `supabase.db.{query,list_tables,list_schemas}` + `supabase.migration.{list,apply}` + `supabase.diagnostics.connection_status`; `db.query` read/write gate is a BEST-EFFORT leading-keyword heuristic — reads free, mutations confirm-gated 412 (NOT a security boundary, documented); `migration.apply` + non-read `db.query` confirm-gated; no-token/network/401 = typed never-faked 424/502/auth-false). Exists so the user runs SQL + operates their project from home without the `claude.ai`-managed `mcp__claude_ai_Supabase__*`. PAT (full-account-scope, secret) + default `SUPABASE_PROJECT_REF` in `mcp/supabase/.env` (gitignored). On the **MCP keep-list** ∧ `.mcp.json` registration **user-gated**. → `KB § MCP-SERVERS/supabase.md`
- Absorption ships consume-docs (R1) — an absorption is incomplete until its consume-side `KB § INTEGRATIONS/<x>.md` ships **in the same project that lifts the code**; what-ships · consume-recipe-via-named-seams · auth-modes · gaps; documentation sibling of "absorption = methodology-epoch merge"; evidence `social-wiring-absorption` shipped zero consume docs; sibling of R5 (completeness ≠ deferral) → `KB § PATTERNS/absorption-ships-consume-docs.md`
- Verify-seed-on-fork-base (R2) — the architect's dispatch-time verify-the-seed-ships-it binds to `git ls-tree origin/main` (the engineers' actual fork base), NOT the architect's working tree; unmerged feature-branch lifts are invisible to engineers; cured structurally by R4; evidence META/DOCS-CONSUME both correctly STOPPED on stale `origin/main` → `KB § PATTERNS/verify-seed-on-fork-base.md`
- Phased-push policy (R4) — large unmerged-to-`main` backlogs merge in **phased increments at project/wave-close**, only when 100%-sure; human-gated (architect PRESENTS {push cmd + range + evidence} → user explicit per-increment go/no-go → architect executes); `scripts/merge-debt-monitor.sh` pre-dispatch + close gate; structural cure for the R2 stale-fork-base class → `KB § PATTERNS/phased-push-policy.md`
- Defer ≠ resolve (R5) — a follow-up filed for work already inside the active project's explicit scope is a deferral slip, NOT a triage outcome; scope test = "was this in the user's original explicit ask?"; in-scope ∧ resolvable-now ⇒ resolve in-project; evidence `gmail-seed-lift` filed for in-original-ask Gmail; sibling of R1 → `KB § PATTERNS/defer-is-not-resolve.md`
- Harness overlay ⊥ worktree divergence (R6) — `Edit`/`Write` can report success while on-disk worktree stays clean (overlay-only, lost at agent end); the agent's own `git status`/`grep` are served the same diverged overlay so naive self-check lies; engineer re-authors via Bash + on-disk grep-proof, architect verifies salvage from separate Bash context; strict instance of codebase-is-source-of-truth → `KB § PATTERNS/harness-overlay-worktree-divergence.md`

### Guides
- First-time setup → `KB § GUIDES/setup.md`
- Creating a new product → `KB § GUIDES/new-product.md`
- Seed-first design checklist → `KB § GUIDES/seed-first-design.md`
- Putting a workspace product online for testing (the "deploy" drill) → `KB § GUIDES/deploy-workspace-online.md`
- Production deploy of the fleet to a VPS (real domain; git deploy-key → build-on-VPS → noctus-net → Caddy-on-real-subdomains OR CF named tunnel; volume-preserving PaaS decommission) → `KB § GUIDES/production-deploy.md`
- Absorbing a separately-developed seed-workspace into noc as a product (the 10-gate end-to-end procedure) → `KB § GUIDES/absorb-seed-workspace.md`

### Integrations (KB depth — vendor references)
- OAuth integration patterns — cross-provider (5-layer model, scope auto-discovery, dual auth backends, single-consent bundling, post-consent introspection, CredentialStore Fernet, G1-G6 gotchas, Meta token-chain + auth-mode matrix, Google↔Meta scope-discovery diff, setup-guide template, in-seed-vs-gap audit) → `KB § INTEGRATIONS/oauth-patterns.md`
- Meta Graph adapter consume-side (`noctusai_lib.integrations.meta` exact `__all__`; `get_meta_adapter` auth-resolution system_user→user_oauth→Fake; `make_meta_router` seam; read-only-v1 — posting/ads/webhooks out-of-scope) → `KB § INTEGRATIONS/meta.md`
- WhatsApp connector consume-side (`noctusai_lib.integrations.whatsapp` exact `__all__`; WAHA `get_whatsapp_client` vs Meta-Cloud-API `get_meta_cloud_client`; `create_whatsapp_webhook_router` seam; lid-auth + dedup) → `KB § INTEGRATIONS/whatsapp.md`
- Google integrations consume-side — Calendar/Maps/YouTube/Drive/Gmail (each exact `__all__`; resolver/credential-store factory injection; YouTube quota-cost Protocol; Drive dual download+read Protocols; Gmail OAuth-only send+read v1 via `OAuthGmailCredentials`/`GmailCredentialResolver`/`make_gmail_client` — `gmail.send` default scope, no API-key path, no DWD v1; seed-ahead consumer status) → `KB § INTEGRATIONS/google.md`
- Vista CRM REST API (auth, endpoints, error model, adapter contract, per-tenant calibration gap) → `KB § INTEGRATIONS/vista.md`
- Image generation consume-side (`noctusai_lib.integrations.image_gen` exact `__all__`; `get_image_gen_adapter` factory picks Real-Gemini vs Fake from key resolution; renderer-agnostic Protocol — nano_banana/galilai/midjourney prompt flavors; `FakeImageGenAdapter` host = loud "not configured" signal; lazy SDK import; first consumer `social-wiring/media_creation/render_post`) → `KB § INTEGRATIONS/image-gen.md`

### Per-product details — open only the product you're working on
- Backend specs → `KB § backend/{01-CORE, 02-ERP, 03-PF, 04-DATABASE, 05-AI-FEATURES, 06-THERAPY, 07-AUTH-SECURITY, 08-DAILY-LIFE}.md`
- Frontend specs → `KB § frontend/{01-CORE, 02-ERP, 03-PF, 04-THERAPY}.md`

### Agent / skill / workflow design
- Entry point → `KB § INSTRUCTIONS/00-MASTER.md`

> Throughout this file `KB § X` is shorthand for `KNOWLEDGE-BASE/X`.

---

## 3 · When to read what

| Situation | Read first |
|---|---|
| Fresh session, need orientation | `KB § AGENT-CONTEXT.md` + `KB § 02-LANDSCAPE.md` |
| Writing/editing backend code | `CLAUDE/backend.md` + `KB § PATTERNS/backend.md` + `KB § backend/0X-*.md` |
| Writing/editing frontend code | `CLAUDE/frontend.md` + `KB § PATTERNS/frontend.md` + `KB § frontend/0X-*.md` |
| Starting / executing / closing a project; handling a phase; touching `*-PROJECT.md` | `CLAUDE/projects.md` + `KB § PATTERNS/project-execution.md` |
| Cross-cutting platform work (MCP toolkit, MCP server design, LGPD data, root hygiene, KB depth) | `CLAUDE/platform.md` + relevant KB pattern |
| Writing a migration | `CLAUDE/backend.md` + `KB § PATTERNS/database-rls.md` + `KB § backend/04-DATABASE.md` |
| Wiring auth on a new product / route (`Depends(get_current_user_org)` shape; the `make_get_current_user_org` factory) | `KB § PATTERNS/backend.md § Auth — canonical pattern` |
| Adding a shared component | `KB § 04-SHARED-LIBRARY.md` (check first — it might exist) |
| User says "create a new noc product" / "scaffold a product" / "absorb a product" / "new noc product" / "add a product" — **TRIGGER PHRASES = seed-first contract** | `KB § GUIDES/new-product.md` (read OPENING block FIRST) + use `noctus.dev.scaffold_product` MCP tool — do NOT hand-author |
| Creating a new product (procedural reference) | `KB § GUIDES/new-product.md` + `KB § 03-SEED-ARCHITECTURE.md` |
| User says "put X online" / "bring it up" / "deploy for testing" / "let me test it" / "spin up the stack" | `KB § GUIDES/deploy-workspace-online.md` (the drill: verify docker artifacts → fill `.env` → `docker compose up` → verify) |
| User says "deploy to production" / "go live" / "put it on the domain / VPS" / "swap the live app" / "migrate off Coolify/Heroku/Replit to our server" — the EXTERNAL-server fleet deploy | `KB § GUIDES/production-deploy.md` (git deploy-key code delivery → build-ON-VPS slim `runtime` images, VITE baked → `noctus-net` → edge **A** Caddy-on-real-subdomains [DNS-record-only path] OR **B** CF named tunnel [zone-on-CF path], A→B zero-URL-change later; volume-preserving PaaS decommission; 6 lessons incl. **never add a host to Caddy/LE before its A-record exists** + product-LLM=OpenAI-no-Anthropic). Operate via `mcp/{hostinger,cloudflare,waha,n8n}` |
| User says "pull/sync the code to the VPS" / "update the deployed code" / "redeploy" / "deploy the latest" / refresh the production checkout from git | `KB § GUIDES/production-deploy.md § 2a` (the safe pull drill — the VPS checkout is PRODUCTION: **inspect → decide → `git merge --ff-only` → verify**; always preserve deploy-local files [`config.yml`/creds/`.env`]; rebuild only if the running container serves what changed; 🔴 NEVER `git reset --hard` ∨ `checkout-over` on the VPS, NEVER dev there) |
| User says "the container isn't picking up my fix" / "rebuild" / "containers are out of sync" / "my edit didn't take effect" / agent just shipped code while a product container was running | `KB § PATTERNS/containerization.md § 5c Sync runbook` (decision tree · 4-step freshness probe · consolidation drill · keep-in-sync table during active work · `start.sh audit_clone_alignment` structural guarantee) + `§ 12b Validation-freshness contract` (the WHEN) + `§ 12b.1 Multi-clone drift` (the special case) |
| Shipping env-sensitive code (URLs, CORS, origins, config-reads, registry-derived behavior) OR "it works locally but breaks on the VPS / in prod / in CI" OR just deployed and something silently no-ops | `KB § PATTERNS/dev-prod-parity.md` FIRST (the umbrella: dev and prod are structurally different — slim image has no start.sh/registry/node, baked dist, env-only config; §2 the dev↔prod difference checklist; §3 authoring-time parity question; §4 deploy-time live-probe). Special cases: `KB § PATTERNS/seed-canonical-defaults.md` (wrong-value default), `KB § PATTERNS/boundary-contract-tests.md` B4 (env propagation), `KB § PATTERNS/containerization.md § 12b` (stale container) |
| **Container won't go healthy / fix appears not applied / "the build is broken" / any container debugging or operational change** | `KB § PATTERNS/containerization-operations.md` FIRST (the §1 source-of-truth chain + §4 diagnostic flowchart A-G short-circuit most re-diagnoses; the §3 codified-bumps catalog covers the common cases — incl. `--force-recreate` after compose-volume edits, the alpine-vs-glibc-FE-deps split, the DD-is-never-truth rule, §9a worktree isolation for concurrent agents, ECONNRESET-npm-retry resilience, parallel-engineer-dispatch-serialize). Architecture depth: `KB § PATTERNS/containerization.md`. **Reach for operations BEFORE you start grepping** — most of the 2-day-chaos session was the §1 chain skipped |
| User wants to **operate the live VPS / containers** — "is the fleet up?", "restart X", "tail logs", "what's running", "disk space", "prune images" | `noctus.vps.*` MCP surface (`ps`/`health`/`logs`/`inspect`/`images`/`disk`/`stats` read-free; `restart`/`recreate`/`prune` confirm-gated, safe docker allowlist) — runs over SSH from the **local** MCP runtime (the prod key never enters CI). Methodology + tool list: `KB § PATTERNS/containerization-operations.md § 2a`. (Deploy = `noctus.dev.deploy_pull`/`deploy_image`; build/deliver = `.github/workflows/build-and-push.yml`.) |
| User says "absorb the X workspace" / "bring the sibling repo in" / "this funcs-dev repo is done, fold it into noc as a product" — externally-developed seed-workspace → noc product | `KB § GUIDES/absorb-seed-workspace.md` (the 10-gate procedure: snapshot → bring-source-in-home → completeness audit → interrogate-before-delete → scaffold → full seed-reconcile → port + pilot-first → consumer-adapt → teardown → container-refactor → user-gated retirement) |
| Starting a new project | `CLAUDE/projects.md` first; copy `templates/PROJECT-TEMPLATE.md` per `KB § PATTERNS/project-execution.md §1` |
| Writing tests | `KB § PATTERNS/testing.md` |
| Adding a `try/except` (production code) | `KB § PATTERNS/logging.md` (level guide, no-`# silent-ok` rule) |
| Editing `.py` / `.ts` / `.tsx` source (rename, codemod, find-callers, multi-file change) | `KB § PATTERNS/ast.md` (AST-first — never sed/regex on source) |
| Exposing a new capability to agents | `CLAUDE/platform.md` + `KB § 01-PHILOSOPHY.md § MCP-first` |
| Adding a new automation script / "write a script for X" / touching `scripts/` | `KB § PATTERNS/mcp-first-scripts.md` (default = `noctus.dev.*` MCP tool + `cli.py` flag + colocated test; bare `scripts/*.sh\|*.py` only via a named carve-out with a manifest row + accept-with-rationale entry) |
| "Can dispatched/bg subagents use the MCP tools?" / agent can't see `mcp__noctusai__*` / MCP container/tunnel question | `KB § 06-AGENTS.md § Subagent MCP access` (yes — stdio child of the session, subagents share the runtime; allowlist is the gate, not infra; container+tunnel only for out-of-runtime consumers) |
| User says "dispatch agents" / "branch this in parallel" / "run these in parallel" / "branching-dispatch" — actually dispatching parallel engineers onto file-disjoint slices, integrating, gating main | `KB § PATTERNS/branching-dispatch.md` (the 10-step runbook: decompose file-disjoint → isolated worktrees → dispatch-in-one-message → collect signal + `/tmp` patch overlay-safety → detect collisions [(a) path-overlap + (b) **semantic-duplicate** git can't see] → merge `--no-ff` → **dedicated honest reconciliation commit** → verify → cleanup → gate `main` at 100%); run `noctus.dev.dispatch_preflight` first; deep reference `KB § PATTERNS/branching-and-merging.md` |
| Dispatch-heavy project (3+ engineers / wave-based parallel batches / cherry-pick + push backlog); user wants to "keep working while we talk" | `KB § PATTERNS/autonomous-operator-via-subagent.md` (Option D — `ScheduleWakeup` tick → spawn `orchestrator-operator` subagent → drains the `## Pending` queue in `.claude/dispatcher.md` in isolated context → architect main context stays clean) |
| Adding / amending a keeper detector | `KB § PATTERNS/testing.md § Regression-test-the-detector` |
| A CI security gate (Trivy / Bandit / gitleaks) is red, or you're editing `.github/workflows/test.yml` security jobs — esp. a Trivy gate failing on a severity it claims not to gate on | `KB § PATTERNS/ci-security-gates.md` (§ 2a: `format: sarif` reports ALL severities → `exit-code:1` gates on MEDIUM unless `limit-severities-for-sarif: true`; raw SARIF is ground truth; CVSS-bucket ≠ vendor-severity; **scan-scope AND scan-config before severity** — `feedback_scan_scope_before_severity`) |
| Adding a helper to `noctusai_lib` | `KB § PATTERNS/seed-lib-layout.md` |
| End-of-phase polish / "what cleanup is most urgent?" / cross-product absorption / cross-tool fusion / intra-file dead code | `noctus.hound.scan` (single entry point) → drill into per-scope detail per `KB § PATTERNS/seed-absorption.md` |
| Relational research turns ("what consumes seam X and through which MCPs?" / "shortest path A → B" / "orient me on product Y" / want a visual map of the platform) | `noctus.graph.build scope="repo"` then `noctus.graph.{query,neighbors,path,explain,report}` — single MCP surface replaces composing `outline_*` + `refs` + manual KB/MEMORY reads. Visual: open `.noc-graph/graph.html`. Full pattern: `KB § PATTERNS/noc-graph.md` |
| Touching gamified UI | `CLAUDE/frontend.md` + `KB § 07-GAMIFICATION.md` |
| Designing an agent / MCP / skill | `KB § INSTRUCTIONS/00-MASTER.md` |
| Touching OAuth / scope discovery / credential storage / a Meta or Google integration (token chain, dual auth, scope auto-discovery, post-consent introspection, CredentialStore, redirect-URI gotchas) | `KB § INTEGRATIONS/oauth-patterns.md` (read BEFORE designing — most patterns already ship in `noctusai_lib.{security.oauth,security.token_store,integrations.meta,integrations.google_scopes}`; doc cites paths + the 3 residual gaps) |
| Wiring a product to **Meta** (Facebook Pages / Instagram Graph) — `get_meta_adapter`, factory auth modes, `make_meta_router`, what `__all__` actually ships | `KB § INTEGRATIONS/meta.md` (read BEFORE wiring — read-only-v1; posting/ads/webhooks out-of-scope-with-destination) |
| Wiring a product to **WhatsApp** — WAHA `get_whatsapp_client` vs Meta-Cloud-API `get_meta_cloud_client`, webhook-router seam, lid-auth/dedup | `KB § INTEGRATIONS/whatsapp.md` (connector API; chatbot composition recipe is `KB § PATTERNS/whatsapp-chatbot-seed.md`) |
| Wiring a product to **Google Calendar / Maps / YouTube / Drive / Gmail** — which `__all__` ships, factory + resolver/credential-store injection, YouTube quota math, Drive dual Protocols, Gmail OAuth-only send+read | `KB § INTEGRATIONS/google.md` (consume-side; Cloud Console setup is `KB § GUIDES/google-oauth-setup.md`) |
| Touching Vista CRM (showcase adapter, future MCP server, endpoint surface, field-set calibration) | `KB § INTEGRATIONS/vista.md` |
| Wiring a product to outbound **image generation** — `get_image_gen_adapter`, factory key-resolution, renderer-flavor prompts, `FakeImageGenAdapter` "not configured" signal, lazy `google-genai` SDK import | `KB § INTEGRATIONS/image-gen.md` (consume-side; v1 Gemini "Nano Banana"; OpenAI/Stability/Replicate require backend extension when N=2 surfaces) |
| User says "two sessions" / "second Claude" / "architect/operator split" / "run a second window" / wants the inbox+outbox coordination pattern | `KB § PATTERNS/two-session-architect-operator.md` (Session A = Architect, no git; Session B = Operator, owns ALL git + dispatch exec + tail sweeps; coordinate via gitignored `.claude/dispatcher.md` (unified Inbox `## Pending`/`## Completed` + Outbox `## Outbox`); setup recipe + `/loop` variant + 5-question pilot rubric) |
| Working a PR / CI / review loop, or wiring/registering the GitHub MCP (`github.pr.*`, `github.repo.view`, `github.diagnostics.connection_status`) | `KB § MCP-SERVERS/github.md` (composes `_kit`; `gh auth` owns creds; `.mcp.json` add is user-gated) |
| Managing / debugging a **self-hosted n8n** instance (workflow list/inspect, execution history, failed-execution error payload), or wiring/registering the n8n MCP (`n8n.workflow.*`, `n8n.execution.*`, `n8n.diagnostics.connection_status`) | `KB § MCP-SERVERS/n8n.md` (composes `_kit`; wraps n8n REST API via stdlib `urllib`; API key in `mcp/n8n/.env`; the `claude.ai` n8n connector can't reach self-hosted; `.mcp.json` add is user-gated) |
| Managing / debugging a **self-hosted WAHA** WhatsApp instance (session status, restart a stuck/STOPPED worker, send a message, host-down-vs-bad-key triage), or wiring/registering the WAHA MCP (`waha.session.*`, `waha.message.*`, `waha.diagnostics.connection_status`) | `KB § MCP-SERVERS/waha.md` (composes `_kit`; wraps WAHA HTTP API via stdlib `urllib`, `X-Api-Key`; secret in `mcp/waha/.env`; `send_text` strongest confirm gate; on keep-list + registered) |
| Managing a **Hostinger VPS** (specs, metrics, power on/off/restart) or wiring the Hostinger MCP (`hostinger.vps.*`, `hostinger.diagnostics.connection_status`) | `KB § MCP-SERVERS/hostinger.md` (composes `_kit`; Bearer API; browser-UA clears CF WAF; token in `mcp/hostinger/.env`; power ops confirm-gated; `.mcp.json` user-gated) |
| Managing **Cloudflare DNS / zones / Tunnel** (records, named tunnel + ingress — the ops the managed CF MCP can't do) or wiring the Cloudflare MCP (`cloudflare.dns.*`, `cloudflare.zones.*`, `cloudflare.tunnel.*`) | `KB § MCP-SERVERS/cloudflare.md` (composes `_kit`; CF API v4; scoped token + account-id in `mcp/cloudflare/.env`; 7 writes confirm-gated, delete strongest; `.mcp.json` user-gated) |
| Running SQL / inspecting / migrating a **Supabase** project from the noc runtime (self-owned, not the managed connector), or wiring/registering the Supabase MCP (`supabase.db.query`, `supabase.project.*`, `supabase.migration.*`, `supabase.diagnostics.connection_status`) | `KB § MCP-SERVERS/supabase.md` (composes `_kit`; Management API via stdlib `urllib`, Bearer PAT; PAT + default project ref in `mcp/supabase/.env`; `db.query` read/write gate is best-effort not a security boundary; `.mcp.json` add is user-gated) |
| Anything not listed | `KB § INDEX.md` |

---

## 4 · Sync rule

CLAUDE.md, `CLAUDE/<topic>.md` files, and `KNOWLEDGE-BASE/INDEX.md` stay in sync — when you add, rename, or delete any KB file or folder, every layer that references it gets updated.

**Pre-commit hook enforces it** (`scripts/hooks/pre-commit` → installed by `scripts/install-hooks.sh`). On every commit:
1. Syncs `products/seed/` → `templates/product-seed/` if staged.
2. Runs `noctus.dev.kb_sync` — regenerates auto-derived count blocks.
3. Runs `noctus.dev.kb_sync` — **blocks the commit** if any literal `KNOWLEDGE-BASE/…md` pointer in CLAUDE.md or `CLAUDE/*.md` doesn't resolve, any KB doc is missing from `INDEX.md`, **or any `products/<slug>/` on disk lacks a row in the `02-LANDSCAPE.md` `## Products` table** (§4 roster-vs-tree-parity keeper, Stage-4 codified 2026-05-18 — the social-wiring drift recurred 3× across clean-context self-tests; deterministic gate so the authoritative onboarding roster cannot silently drift; pure predicate `roster_tree_parity_gaps` + colocated `tests/test_kb_sync.py`).

Manual runs:
- `python mcp/noctusai/cli.py --verify-kb-sync`
- `python mcp/noctusai/cli.py --update-kb-counts [--check]`
- `python mcp/noctusai/cli.py --verify-kb-sync`

Install hooks in a fresh clone: `bash scripts/install-hooks.sh` (full setup: `bash scripts/setup.sh`).

Bypass (rarely correct): `git commit --no-verify`.
leak
