-e seed/lib/backend
-e seed/framework/backend
fastapi==0.115.5
uvicorn[standard]==0.30.6
supabase==2.9.1
pydantic==2.9.0
pydantic-settings==2.5.2
python-dotenv==1.0.1
# Transitive: pydantic EmailStr → email-validator; FastAPI Form/File → python-multipart
email-validator>=2.0.0
python-multipart>=0.0.9
httpx>=0.27.0
PyJWT==2.12.0
slowapi>=0.1.9

# Observability (optional)
sentry-sdk[fastapi]==1.40.0

# Caching (optional)
redis==5.0.1

# Testing
pytest-asyncio>=0.21.0

# Phase 1 of `youtube-crawler-domain-implementation`:
#   - `cryptography` ships transitively via `seed/lib/backend` (pyproject pins
#     `cryptography>=42.0`), so no add needed here.
#   - `httpx` is already required above (FastAPI / seed `GoogleProvider`).
#   - The seed `GoogleProvider` uses raw httpx for the token-endpoint dance;
#     we don't need `google-auth-oauthlib` for Phase 1.
#
# Phases 2+3 will add:
#   google-auth>=2.0.0                # for service-account / refresh handling
#   google-api-python-client>=2.0.0   # Drive readonly + YouTube Data API v3
#   google-auth-httplib2>=0.2.0       # transport
# Leaving as comments until those phases land.
