# Dev override compose — auto-merged by `docker compose up` (no -f).
# See KB § PATTERNS/containerization.md §11d for the full pattern.

services:
  youtube-crawler-backend:
    volumes:
      - ./backend:/app/products/youtube-crawler/backend
    command:
      - uvicorn
      - app.main:app
      - --host
      - 0.0.0.0
      - --port
      - "8008"
      - --reload
      - --app-dir
      - products/youtube-crawler/backend
    restart: "no"

  youtube-crawler-frontend:
    build:
      # `target: dev` picks the vite-dev-server stage from the multi-stage
      # frontend Dockerfile. See KB § PATTERNS/containerization.md §11d.
      target: dev
    volumes:
      - ./frontend:/app/products/youtube-crawler/frontend
    # `--host 0.0.0.0` is non-negotiable — vite's default `localhost`
    # binding inside a container = unreachable from the host. `--port`
    # MUST match the compose `ports:` mapping (8150:8150).
    command:
      - npm
      - run
      - dev
      - --
      - --host
      - 0.0.0.0
      - --port
      - "8150"
    environment:
      NODE_ENV: development
    restart: "no"
