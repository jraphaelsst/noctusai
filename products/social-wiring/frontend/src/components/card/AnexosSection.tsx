/**
 * AnexosSection — MOVED to the seed card hub (`@noctusai/lib/components`,
 * `seed/lib/frontend/src/components/card-hub/AnexosSection.tsx`, wave-a Slice
 * C/F — `project-history/roadmaps/cardhub-igig-crm-2026-09.wave-a-design.md`).
 *
 * The tipo picker, the four states and the extraction badges are the seed's.
 * This module is the stable SW import path `PessoaDocumentosPanel` and the
 * colocated suite (`AnexosSection.test.tsx`) use — a re-export, never a copy.
 */
export { AnexosSection } from "@noctusai/lib/components";
export type { AnexosSectionProps } from "@noctusai/lib/components";
