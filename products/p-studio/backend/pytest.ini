[pytest]
testpaths = tests
pythonpath = .

markers =
    ao_vivo: bate no sandbox real do provedor; exige ASAAS_API_KEY e rede.

# A execução padrão fica offline por construção, não por convenção. Para rodar
# os testes ao vivo:  ASAAS_API_KEY=... pytest -m ao_vivo
addopts = -m "not ao_vivo"
