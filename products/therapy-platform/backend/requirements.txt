# Therapy Platform — Backend Dependencies
# Installed via: pip install -r requirements.txt
# For full-stack dev, use the root requirements.txt instead.

# Shared package (editable install)
-e seed/lib/backend
-e seed/framework/backend

# Core framework
fastapi==0.115.5
uvicorn[standard]==0.30.6
supabase==2.9.1
pydantic==2.9.0
pydantic-settings==2.5.2
python-dotenv==1.0.1
httpx>=0.27.0
PyJWT==2.12.0
python-multipart>=0.0.9
slowapi>=0.1.9

# Video calls (LiveKit)
livekit-api>=0.6.0

# Payments (Stripe Connect)
stripe>=7.0.0

# Google Calendar integration
google-api-python-client>=2.120.0
google-auth-oauthlib>=1.2.0

# AI (OpenAI — transcription + summaries)
openai>=1.50.0

# Email
resend>=2.0.0
email-validator>=2.0.0

# Scheduling (recurring appointment auto-generation)
APScheduler>=3.10.0

# Observability (optional)
sentry-sdk[fastapi]==1.40.0

# Caching (optional)
redis==5.0.1

# Testing
pytest>=8.0.0
pytest-asyncio>=0.21.0
# LLM provider SDKs (multi-provider via noctusai_lib.llm — monolithic install)
anthropic>=0.40.0
google-generativeai>=0.8.0
