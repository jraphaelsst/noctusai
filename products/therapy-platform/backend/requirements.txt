# Therapy Platform — Backend Dependencies
# Installed via: pip install -r requirements.txt
# For full-stack dev, use the root requirements.txt instead.

# Shared package (editable install)
-e seed/lib/backend
-e seed/framework/backend

# Core framework
fastapi==0.121.0
uvicorn[standard]==0.30.6
supabase==2.9.1
pydantic==2.13.4
pydantic-settings==2.5.2
python-dotenv==1.2.2
httpx>=0.27.0
PyJWT==2.12.0
python-multipart>=0.0.9
slowapi>=0.1.9

# LLM tool-call audit writer — `app/services/audit_hook.py` +
# `app/models/tool_call_audit.py` use SQLAlchemy's typed-table-class shape (the
# seed audit writer's secondary surface; psycopg2-binary is opt-in via
# postgres_url). Imported at startup via the routers, so it MUST be declared
# here. See `KB § PATTERNS/llm-tool-audit.md`.
sqlalchemy>=2.0.0

# Video calls (LiveKit)
livekit-api>=0.6.0

# Payments (Stripe Connect)
stripe>=7.0.0

# Google Calendar integration
google-api-python-client>=2.120.0
google-auth-oauthlib>=1.2.0

# AI (OpenAI — transcription + summaries)
openai>=1.50.0

# Email
resend>=2.0.0
email-validator>=2.0.0

# Scheduling (recurring appointment auto-generation)
APScheduler>=3.10.0

# Observability (optional)
sentry-sdk[fastapi]==1.45.1

# Caching (optional)
redis==5.0.1

# Testing
pytest>=8.0.0
pytest-asyncio>=0.21.0
# LLM provider SDKs (multi-provider via noctusai_lib.llm — monolithic install)
anthropic>=0.40.0
google-generativeai>=0.8.0
