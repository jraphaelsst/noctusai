# Dev override compose — auto-merged by `docker compose up` (no -f).
# See KB § PATTERNS/containerization.md §11d for the full pattern.

services:
  personal-finance-backend:
    volumes:
      - ./backend:/app/products/personal-finance/backend
    command:
      - uvicorn
      - app.main:app
      - --host
      - 0.0.0.0
      - --port
      - "8002"
      - --reload
      - --app-dir
      - products/personal-finance/backend
    restart: "no"

  personal-finance-frontend:
    volumes:
      - ./frontend:/app/products/personal-finance/frontend
    environment:
      NODE_ENV: development
    restart: "no"
