-e seed/lib/backend
-e seed/framework/backend
fastapi==0.121.0
uvicorn[standard]==0.30.6
supabase==2.9.1
pydantic==2.13.4
pydantic-settings==2.5.2
python-dotenv==1.2.2
httpx>=0.27.0
PyJWT==2.12.0
slowapi>=0.1.9
APScheduler>=3.10.0
yfinance>=0.2.36

# LLM tool-call audit writer — `app/services/audit_hook.py` +
# `app/models/tool_call_audit.py` use SQLAlchemy's typed-table-class shape
# (imported → MUST be declared, even if latent at boot). KB § PATTERNS/llm-tool-audit.md.
sqlalchemy>=2.0.0

# Observability (optional)
sentry-sdk[fastapi]==1.45.1

# Caching (optional)
redis==5.0.1

# Testing
pytest-asyncio>=0.21.0
