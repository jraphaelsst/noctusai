-e seed/lib/backend
-e seed/framework/backend
fastapi==0.115.0
uvicorn[standard]==0.30.6
supabase==2.9.1
pydantic==2.9.0
pydantic-settings==2.5.2
python-dotenv==1.0.1
httpx>=0.27.0
PyJWT==2.9.0
slowapi>=0.1.9

# Observability (optional)
sentry-sdk[fastapi]==1.40.0

# Caching (optional)
redis==5.0.1

# SQLAlchemy — used by the seed `make_audit_writer` audit-hook contract
# (`app/services/audit_hook.py`). Routers/services use the Supabase admin
# client per PROJECT.md §2; SQLAlchemy is a SECONDARY surface for the
# typed-table-class shape the seed audit writer requires. Postgres driver
# (`psycopg2-binary`) is opt-in — only loaded when `postgres_url` is set.
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
openai>=1.50.0

# Testing
pytest-asyncio>=0.21.0
