-e seed/lib/backend
-e seed/framework/backend
fastapi==0.115.5
uvicorn[standard]==0.30.6
supabase==2.9.1
pydantic==2.13.4
pydantic-settings==2.5.2
python-dotenv==1.2.2
httpx>=0.27.0
PyJWT==2.12.0
python-dateutil>=2.9.0
slowapi>=0.1.9

# Observability (optional)
sentry-sdk[fastapi]==1.45.1

# Caching (optional)
redis==5.0.1

# SQLAlchemy — used by the seed `make_audit_writer` audit-hook contract
# (`app.services.audit_hook`). Primary data path is the Supabase admin
# client per app/config.py; SQLAlchemy is a SECONDARY surface for the
# tool-call audit-writer factory only. The Postgres driver
# (`psycopg2-binary`) is opt-in — only loaded when `postgres_url` is set.
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# Testing
pytest-asyncio>=0.21.0
