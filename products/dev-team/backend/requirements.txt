# dev-team product backend requirements.
# The shared root requirements.txt installs the heavyweight base; this file
# pins ONLY the seed editable installs (compliance scanner expects them
# co-located with the product) and any product-specific extras.
#
# Engine consumption: the dev_team engine (at the repo root) is installed via
#   pip install -e dev_team/
# at deploy time (Dockerfile + start.sh). Not pinned here because it's a
# sibling of `products/`, not a PyPI package.
#
# No extra deps beyond root for MVP — telemetry / API endpoints all rely on
# what the seed already ships.

-e seed/lib/backend
-e seed/framework/backend

# Server runtime — required for the `uvicorn` entry point. The other 9
# products pin this explicitly; seed/lib/backend's pyproject lists FastAPI
# but not uvicorn. Follow-up: consider absorbing uvicorn into
# seed/lib/backend deps so every product inherits it.
uvicorn[standard]==0.30.6
