-e seed/lib/backend
-e seed/framework/backend
fastapi==0.115.5
uvicorn[standard]==0.30.6
supabase==2.9.1
pydantic==2.9.0
pydantic-settings==2.5.2
python-dotenv==1.0.1
httpx>=0.27.0
# PyJWT + bcrypt removed Phase 1 of adconnect-mvp-implementation (custom-JWT
# scaffold retired — auth comes from noctusai_seed/Supabase SSO).
slowapi>=0.1.9

# Observability (optional)
sentry-sdk[fastapi]==1.40.0

# Caching (optional)
redis==5.0.1

# Testing
pytest-asyncio>=0.21.0

# Pydantic EmailStr requires this — runtime ImportError otherwise
email-validator>=2.0.0

# Form data — required by FastAPI when any endpoint uses Form() or UploadFile
python-multipart>=0.0.9

# defusedxml — XML parser hardening (bandit B313-B320). NF-e XML uploads
# from distributors are untrusted input; defusedxml.ElementTree.fromstring
# is a drop-in for stdlib xml.etree.ElementTree.fromstring.
defusedxml>=0.7.1
