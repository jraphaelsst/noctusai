# knowledge-extractor backend dependencies (absorbed into noc 2026-05-23).
#
# The shared seed base venv (noctus-seed-backend-base) carries the `-e seed/`
# editables + their transitive deps — but each product still PINS the app/server
# stack (fastapi/uvicorn/etc.), exactly like products/seed/backend/requirements.txt.
# The seed Dockerfile strips the `-e seed/` lines (inherited) and installs the rest.
#
# SYSTEM dependency (not pip): ffmpeg — added in the product Dockerfile.

-e seed/lib/backend
-e seed/framework/backend

# Shared app/server stack — pinned to the seed canonical (uvicorn is the server
# the base does NOT provide; the container CMD needs it on PATH).
fastapi==0.121.0
uvicorn[standard]==0.30.6
supabase==2.9.1
pydantic==2.13.4
pydantic-settings==2.5.2
python-dotenv==1.2.2
email-validator>=2.0.0
python-multipart>=0.0.9
httpx>=0.27.0
PyJWT==2.12.0
slowapi>=0.1.9

# Domain deps (knowledge-extractor specific) — not in the seed base.
openai>=1.50.0
google-api-python-client>=2.120.0
google-auth>=2.30.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
gdown>=5.2.0
python-docx>=1.1.0

# Testing (pytest itself is in the seed framework dev deps)
pytest-asyncio>=0.21.0
